<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Portal absensi digital untuk UPTD Puskesmas Sintang.">
    <title>Absensi Digital UPTD Puskesmas Sintang</title>
    <link id="appFavicon" rel="icon" href="https://cdn-icons-png.flaticon.com/512/2966/2966481.png" type="image/png">

    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: { extend: { colors: { 'pusk-blue': '#003366', 'pusk-yellow': '#FFD700' } } }
        }
    </script>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://unpkg.com/html5-qrcode" type="text/javascript"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf-autotable/3.5.28/jspdf.plugin.autotable.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>

    <style>
        @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800;900&display=swap');
        body { font-family: 'Plus Jakarta Sans', sans-serif; background-color: #f0f4f8; }
        .glass-panel { background: rgba(255, 255, 255, 0.95); backdrop-filter: blur(16px); box-shadow: 0 10px 25px rgba(0,0,0,0.05); }
        .page-section { display: none; animation: fadeIn 0.4s ease-out; }
        .page-section.active { display: block; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(15px); } to { opacity: 1; transform: translateY(0); } }

        .swal2-select, .swal2-input, .swal2-textarea { font-size: 14px !important; border-radius: 8px !important; }
        .action-card { transition: all 0.3s ease; }
        .action-card:hover { transform: translateY(-3px); box-shadow: 0 15px 30px rgba(0, 51, 102, 0.15); }

        /* ========================================= */
        /* DESAIN ID CARD MODERN (ANTI POTONG & PRESISI) */
        /* ========================================= */
        .universal-id-card {
            background-color: #ffffff !important;
            width: 6.5cm; /* Ukuran Standar Name Tag B1 */
            height: 10.5cm;
            box-sizing: border-box;
            position: relative;
            display: flex;
            flex-direction: column;
            overflow: hidden;
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            border: 1px solid #e2e8f0;
            font-family: 'Plus Jakarta Sans', sans-serif;
        }
        .id-header {
            background-color: #003366 !important;
            color: #ffffff !important;
            padding: 16px 12px 14px;
            text-align: center;
            border-bottom: 4px solid #FFD700 !important;
            flex-shrink: 0;
        }
        .id-header h3 {
            font-size: 12px;
            margin: 0 0 3px 0;
            font-weight: 900;
            line-height: 1.3;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .id-header p {
            font-size: 8px;
            color: #FFD700 !important;
            margin: 0;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .id-body {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 10px 15px;
            background-color: #ffffff !important;
        }
        .id-qr {
            margin: 0 auto 12px;
            padding: 8px;
            background: #fff !important;
            border-radius: 10px;
            border: 2px solid #e2e8f0;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
        }
        .nama-pegawai {
            font-weight: 900;
            font-size: 13px;
            color: #0f172a !important;
            text-transform: uppercase;
            line-height: 1.3;
            text-align: center;
            width: 100%;
            word-wrap: break-word;
            margin-bottom: 4px;
        }
        .nip-pegawai {
            color: #475569 !important;
            font-size: 11px;
            font-family: 'Courier New', Courier, monospace;
            font-weight: 700;
            text-align: center;
        }
        .id-footer {
            background-color: #f8fafc !important;
            padding: 10px;
            text-align: center;
            border-top: 1px dashed #cbd5e1;
            flex-shrink: 0;
        }
        .id-footer p {
            font-size: 8.5px;
            color: #64748b !important;
            margin: 0;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        /* Print Settings untuk ID Card Presisi di Kertas */
        @media print {
            body * { visibility: hidden; }
            #printArea, #printArea * { visibility: visible; }
            @page { margin: 15mm; size: A4 portrait; }
            #printArea {
                position: absolute; left: 0; top: 0; width: 100%;
                display: flex; flex-wrap: wrap; justify-content: center; align-content: flex-start; gap: 10mm; padding: 0;
            }
            .universal-id-card {
                page-break-inside: avoid;
                border: 1px solid #cbd5e1 !important;
                box-shadow: none !important;
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }
        }

        .custom-checkbox { width: 1.2rem; height: 1.2rem; border-radius: 0.25rem; accent-color: #003366;}

        /* Animasi Bouncing di Login */
        .animate-bounce-slow { animation: bounce 3s infinite; }
        @keyframes bounce { 0%, 100% { transform: translateY(-5%); animation-timing-function: cubic-bezier(0.8,0,1,1); } 50% { transform: none; animation-timing-function: cubic-bezier(0,0,0.2,1); } }

        /* Animasi Form Luar Radius */
        .pulse-glow { animation: pulseGlow 2s infinite; }
        @keyframes pulseGlow { 0% { box-shadow: 0 0 0 0 rgba(0, 51, 102, 0.4); } 70% { box-shadow: 0 0 0 15px rgba(0, 51, 102, 0); } 100% { box-shadow: 0 0 0 0 rgba(0, 51, 102, 0); } }

        /* Animasi Modern Header Teks Shimmer & Logo Glow */
        .text-shimmer {
            background: linear-gradient(90deg, #ffffff 0%, #FFD700 25%, #ffffff 50%);
            background-size: 200% auto;
            color: transparent;
            -webkit-background-clip: text;
            background-clip: text;
            animation: shimmer 3.5s linear infinite;
        }
        @keyframes shimmer { to { background-position: 200% center; } }

        .glow-border { position: relative; border-radius: 50%; }
        .glow-border::before {
            content: ""; position: absolute; inset: -2px; border-radius: 50%;
            background: conic-gradient(from 0deg, transparent, #FFD700, transparent 40%);
            animation: spin 3s linear infinite; z-index: 0;
        }
        @keyframes spin { 100% { transform: rotate(360deg); } }
        .glow-border-inner {
            position: relative; z-index: 10; background: white; border-radius: 50%;
            width: 100%; height: 100%; display: flex; align-items: center; justify-content: center;
        }
        .running-wrap { overflow: hidden; white-space: nowrap; position: relative; }
        .running-wrap::before, .running-wrap::after { content: ""; position: absolute; top: 0; bottom: 0; width: 54px; z-index: 2; pointer-events: none; }
        .running-wrap::before { left: 0; background: linear-gradient(90deg, rgba(0,51,102,.95), transparent); }
        .running-wrap::after { right: 0; background: linear-gradient(270deg, rgba(14,116,144,.88), transparent); }
        .running-text {
            display: inline-flex;
            align-items: center;
            gap: 1.25rem;
            padding-left: 100%;
            letter-spacing: .08em;
            text-shadow: 0 0 18px rgba(255,255,255,.35);
            animation: runningText 24s linear infinite;
        }
        .running-text::before, .running-text::after { content: "✦"; color: #FFD700; filter: drop-shadow(0 0 8px rgba(250,204,21,.9)); }
        @keyframes runningText { 0% { transform: translateX(0); } 100% { transform: translateX(-100%); } }

        .landing-orbit { position: absolute; inset: -35%; background: conic-gradient(from 120deg, rgba(0,51,102,.28), rgba(255,215,0,.28), rgba(30,64,175,.18), rgba(0,51,102,.28)); animation: orbitGlow 18s linear infinite; }
        @keyframes orbitGlow { to { transform: rotate(360deg); } }
        .float-soft { animation: floatSoft 4.5s ease-in-out infinite; }
        @keyframes floatSoft { 0%,100% { transform: translateY(0); } 50% { transform: translateY(-10px); } }
        .scan-grid-bg { background-image: radial-gradient(circle at 1px 1px, rgba(255,255,255,.28) 1px, transparent 0); background-size: 22px 22px; }
        .info-card-modern { position: relative; overflow: hidden; transition: transform .3s ease, box-shadow .3s ease; }
        .info-card-modern::before { content: ""; position: absolute; inset: 0; background: linear-gradient(135deg, rgba(255,255,255,.55), transparent 55%); pointer-events: none; }
        .info-card-modern:hover { transform: translateY(-4px); box-shadow: 0 20px 40px rgba(15, 23, 42, .10); }
        .distance-reveal-btn { transition: all .25s ease; }
        .distance-reveal-btn:hover { transform: translateY(-1px); box-shadow: 0 8px 18px rgba(15,23,42,.10); }

        .hero-title-modern {
            background: linear-gradient(90deg, #ffffff 0%, #FFD700 35%, #dbeafe 70%, #ffffff 100%);
            background-size: 220% auto;
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            animation: heroTextFlow 5s ease-in-out infinite;
            text-shadow: 0 10px 30px rgba(0,0,0,.18);
        }
        .hero-slogan-modern { animation: sloganPulse 3.8s ease-in-out infinite; }
        @keyframes heroTextFlow { 0%,100% { background-position: 0% center; } 50% { background-position: 100% center; } }
        @keyframes sloganPulse { 0%,100% { transform: translateY(0); filter: drop-shadow(0 0 0 rgba(255,215,0,0)); } 50% { transform: translateY(-2px); filter: drop-shadow(0 0 12px rgba(255,215,0,.38)); } }

        /* Mobile polish: halaman scan tidak mengunci layar dan panel panduan tetap terlihat */
        html { min-height: 100%; }
        body { min-height: 100vh; height: auto; overflow-x: hidden; }
        main { padding-bottom: max(2.5rem, env(safe-area-inset-bottom)); }
        .guide-panel-responsive { min-height: 100%; }
        .note-chip-modern {
            display: inline-flex;
            align-items: flex-start;
            gap: .45rem;
            max-width: 24rem;
            white-space: normal;
            overflow-wrap: anywhere;
            word-break: break-word;
            line-height: 1.45;
        }
        .note-chip-modern i { margin-top: .12rem; }
        .note-chip-empty { opacity: .75; }
        .gov-app-footer {
            position: relative;
            z-index: 20;
            overflow: hidden;
            border-top: 1px solid rgba(255, 215, 0, .45);
            background: linear-gradient(90deg, rgba(0, 32, 76, .98), rgba(0, 51, 102, .98), rgba(14, 116, 144, .98));
            box-shadow: 0 -10px 30px rgba(15, 23, 42, .14);
            backdrop-filter: blur(18px);
        }
        .gov-app-footer::before {
            content: "";
            position: absolute;
            inset: 0;
            background: linear-gradient(110deg, transparent 0%, rgba(255,255,255,.16) 42%, rgba(255,215,0,.22) 50%, transparent 62%);
            transform: translateX(-120%);
            animation: footerFreezeGlow 7s ease-in-out infinite;
            pointer-events: none;
        }
        .gov-footer-logo { box-shadow: 0 0 0 3px rgba(255,255,255,.18), 0 0 26px rgba(255,215,0,.38); }
        .gov-footer-dot { animation: footerDotPulse 1.8s ease-in-out infinite; }
        .gov-footer-chip {
            display: inline-flex;
            align-items: center;
            gap: .45rem;
            border: 1px solid rgba(255,255,255,.18);
            background: rgba(255,255,255,.1);
            border-radius: 999px;
            padding: .45rem .7rem;
            white-space: nowrap;
            box-shadow: inset 0 1px 0 rgba(255,255,255,.08);
        }
        .gov-footer-address { overflow-wrap: anywhere; }
        .login-ultra-card { background: rgba(255,255,255,.88); backdrop-filter: blur(24px); }
        .login-ultra-card::before { content: ""; position: absolute; inset: -35%; background: conic-gradient(from 140deg, rgba(255,215,0,.18), rgba(59,130,246,.18), rgba(14,165,233,.14), rgba(255,215,0,.18)); animation: orbitGlow 16s linear infinite; pointer-events: none; }
        .login-feature-chip { display: inline-flex; align-items: center; gap: .45rem; border: 1px solid rgba(255,255,255,.22); background: rgba(255,255,255,.14); border-radius: 999px; padding: .45rem .7rem; font-size: 10px; font-weight: 900; color: #dbeafe; }
        .login-input-modern { box-shadow: inset 0 1px 0 rgba(255,255,255,.8), 0 10px 24px rgba(15,23,42,.04); }
        .holiday-card-modern {
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(251, 146, 60, .22);
            background: linear-gradient(135deg, rgba(255,255,255,.98), rgba(255,247,237,.92));
            box-shadow: 0 12px 28px rgba(15, 23, 42, .06);
            transition: transform .25s ease, box-shadow .25s ease, border-color .25s ease;
            animation: holidayCardIn .42s ease both;
        }
        .holiday-card-modern::before { content: ""; position: absolute; inset: 0; background: linear-gradient(115deg, transparent 0%, rgba(255,255,255,.75) 46%, rgba(251,191,36,.22) 54%, transparent 68%); transform: translateX(-120%); animation: holidayShine 5.8s ease-in-out infinite; pointer-events: none; }
        .holiday-card-modern:hover { transform: translateY(-2px); border-color: rgba(249,115,22,.42); box-shadow: 0 18px 38px rgba(15, 23, 42, .10); }
        .holiday-date-pill { background: linear-gradient(135deg, #003366, #0e7490); box-shadow: 0 10px 24px rgba(0,51,102,.22); }
        @keyframes holidayCardIn { from { opacity: 0; transform: translateY(10px) scale(.98); } to { opacity: 1; transform: translateY(0) scale(1); } }
        @keyframes holidayShine { 0%, 42% { transform: translateX(-120%); } 72%, 100% { transform: translateX(120%); } }
        @keyframes footerFreezeGlow { 0%, 46% { transform: translateX(-120%); } 70%, 100% { transform: translateX(120%); } }
        @keyframes footerDotPulse { 0%,100% { transform: scale(.9); opacity: .72; } 50% { transform: scale(1.15); opacity: 1; } }

        @media (max-width: 640px) {
            main { width: 100%; padding-left: .85rem; padding-right: .85rem; padding-top: 1rem; }
            #scanView .min-h-\[75vh\] { min-height: auto; justify-content: flex-start; }
            #scanView .rounded-\[2rem\] { border-radius: 1.5rem; }
            #scanView .action-card { border-radius: 1.25rem; padding: .95rem .7rem; }
            #scanView .action-card i { font-size: 1.45rem; margin-bottom: .45rem; }
            .guide-panel-responsive { border-top: 1px solid rgba(191, 219, 254, .9); padding: 1rem; }
            .guide-panel-responsive .info-card-modern { border-radius: 1.25rem; padding: 1rem; }
            .note-chip-modern { max-width: min(72vw, 18rem); }
            .gov-app-footer .footer-secondary { display: none; }
            .gov-app-footer .footer-title { font-size: .72rem; }
            .gov-app-footer .footer-subtitle { font-size: .58rem; }
            .gov-app-footer .gov-footer-address { display: none; }
        }
    </style>
</head>
<body class="text-gray-800 min-h-screen flex flex-col">

    <header class="bg-pusk-blue shadow-lg sticky top-0 z-40 border-b-4 border-pusk-yellow">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16 items-center">
                <div class="flex items-center gap-3 cursor-pointer group" onclick="navTo('scanView')">
                    <!-- Logo App Dengan Efek Berputar -->
                    <div class="glow-border w-11 h-11 p-[2px] shadow-md">
                        <div class="glow-border-inner overflow-hidden">
                            <img id="appLogoImg" src="" alt="Logo" class="hidden w-full h-full object-contain transform group-hover:scale-110 transition duration-300">
                            <i id="appLogoIcon" class="fa-solid fa-hospital fa-lg text-pusk-blue transform group-hover:scale-110 transition duration-300"></i>
                        </div>
                    </div>
                    <!-- Teks Shimmer -->
                    <div>
                        <h1 id="appNameTitle" class="text-shimmer font-extrabold text-lg leading-tight tracking-wide">PORTAL ABSENSI</h1>
                        <p id="appHeaderName" class="text-pusk-yellow text-[10px] font-bold tracking-widest uppercase opacity-90 transition-opacity group-hover:opacity-100">Sistem Digital</p>
                    </div>
                </div>

                <div class="hidden md:flex space-x-1 items-center" id="adminMenu" style="display:none;">
                    <button onclick="navTo('dashView')" class="text-white hover:bg-blue-800 px-3 py-2 rounded-lg text-sm font-semibold transition"><i class="fa-solid fa-chart-line mr-1"></i> Dashboard</button>
                    <button onclick="navTo('harianView')" class="text-white hover:bg-blue-800 px-3 py-2 rounded-lg text-sm font-semibold transition"><i class="fa-solid fa-calendar-check mr-1"></i> Harian</button>
                    <button onclick="navTo('kegiatanView')" class="text-white hover:bg-blue-800 px-3 py-2 rounded-lg text-sm font-semibold transition"><i class="fa-solid fa-clipboard-list mr-1"></i> Kegiatan</button>
                    <button onclick="navTo('pegawaiView')" class="text-white hover:bg-blue-800 px-3 py-2 rounded-lg text-sm font-semibold transition"><i class="fa-solid fa-users mr-1"></i> Pegawai</button>
                    <button onclick="navTo('laporanView')" class="text-white hover:bg-blue-800 px-3 py-2 rounded-lg text-sm font-semibold transition"><i class="fa-solid fa-file-pdf mr-1"></i> Laporan</button>
                    <button onclick="navTo('settingView')" class="text-white hover:bg-blue-800 px-3 py-2 rounded-lg text-sm font-semibold transition"><i class="fa-solid fa-gear mr-1"></i> Pengaturan</button>
                    <button onclick="logoutAdmin()" class="bg-red-500 hover:bg-red-600 text-white px-3 py-2 rounded-lg text-sm font-bold shadow transition ml-2"><i class="fa-solid fa-power-off"></i></button>
                </div>

                <div class="md:hidden flex items-center">
                    <button onclick="navTo('loginView')" id="mobileLoginBtn" class="text-pusk-yellow hover:text-white px-3 py-2 rounded-md text-sm font-medium"><i class="fa-solid fa-lock"></i> Admin</button>
                </div>
            </div>
        </div>
    </header>

    <main class="flex-grow container mx-auto px-4 py-8 max-w-6xl">

        <!-- VIEW: SCANNER ABSENSI -->
        <section id="scanView" class="page-section active">
            <div class="flex flex-col items-center justify-center min-h-[75vh] relative overflow-hidden">
                <div class="w-full max-w-6xl rounded-[2rem] overflow-hidden shadow-2xl border border-blue-100 bg-white/95 backdrop-blur-xl relative">
                    <div class="landing-orbit opacity-80"></div>
                    <div class="relative grid grid-cols-1 lg:grid-cols-[1.1fr_.9fr] gap-0">
                        <div class="relative overflow-hidden bg-gradient-to-br from-pusk-blue via-blue-900 to-blue-700 text-white p-6 md:p-9 scan-grid-bg">
                            <div class="absolute top-8 right-8 w-28 h-28 rounded-full bg-pusk-yellow/20 blur-2xl float-soft"></div>
                            <div class="absolute bottom-8 left-8 w-36 h-36 rounded-full bg-cyan-300/20 blur-3xl"></div>
                            <div class="running-wrap -mx-7 md:-mx-10 -mt-7 md:-mt-10 mb-8 py-2 px-3 bg-white/10 border-b border-white/15 text-white text-[11px] font-extrabold tracking-wide backdrop-blur">
                                <span id="runningTextMsg" class="running-text">Info layanan digital UPTD Puskesmas Tanjung Puri • sehat bersama kami • masyarakat berprestasi • scan cepat, data akurat</span>
                            </div>
                            <div class="relative z-10">
                                <div class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-white/15 border border-white/20 text-[11px] font-extrabold uppercase tracking-widest mb-5">
                                    <span class="w-2 h-2 rounded-full bg-emerald-300 animate-pulse"></span>
                                    Smart Attendance Gateway
                                </div>
                                <h2 class="hero-title-modern text-3xl md:text-5xl font-black leading-tight tracking-tight mb-4">Absen DigitalUPTD Puskesmas Tanjung Puri Kabupaten Sintang</h2>
                                <p class="hero-slogan-modern text-sm md:text-lg text-blue-50/95 leading-relaxed max-w-2xl mb-7 font-extrabold">𝐒𝐞𝐡𝐚𝐭 𝐁𝐞𝐫𝐬𝐚𝐦𝐚 𝐊𝐚𝐦𝐢, 𝐌𝐚𝐬𝐲𝐚𝐫𝐚𝐤𝐚𝐭 𝐁𝐞𝐫𝐩𝐫𝐞𝐬𝐭𝐚𝐬𝐢.</p>

                                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 mb-6">
                                    <div class="bg-white/12 border border-white/20 rounded-3xl p-4 backdrop-blur-md">
                                        <p class="text-[10px] font-extrabold uppercase tracking-widest text-blue-100 mb-2"><i class="fa-solid fa-clock mr-1"></i>Waktu Server</p>
                                        <div id="liveTime" class="text-4xl font-extrabold font-mono tracking-wider">00:00:00</div>
                                        <div id="liveDate" class="text-xs text-blue-100 font-semibold mt-1 uppercase tracking-widest">Senin, 1 Januari 2024</div>
                                    </div>
                                    <div id="gpsStatusCard" class="bg-white/12 border border-white/20 rounded-3xl p-4 flex items-center gap-4 backdrop-blur-md transition-all duration-300">
                                        <div id="gpsIcon" class="w-12 h-12 rounded-2xl bg-white/20 text-white flex items-center justify-center text-xl shrink-0"><i class="fa-solid fa-spinner fa-spin"></i></div>
                                        <div class="text-left">
                                            <h4 id="gpsTitle" class="font-extrabold text-white text-sm">Menghubungkan Satelit...</h4>
                                            <p id="gpsDetails" class="text-[11px] text-blue-100 mt-0.5 leading-tight">Mohon tunggu dan izinkan akses lokasi.</p>
                                        </div>
                                    </div>
                                </div>

                                <div id="avgJamCard" class="hidden mb-6 rounded-3xl border border-white/20 bg-white/12 backdrop-blur-md p-4 shadow-xl">
                                    <div class="flex items-center justify-between gap-3 mb-3">
                                        <div>
                                            <p class="text-[10px] font-black uppercase tracking-[0.22em] text-blue-100">Rata-rata hari ini</p>
                                            <h3 class="text-sm font-extrabold text-white">Jam Masuk & Pulang Pegawai</h3>
                                        </div>
                                        <div class="w-10 h-10 rounded-2xl bg-pusk-yellow/20 text-pusk-yellow flex items-center justify-center float-soft"><i class="fa-solid fa-chart-line"></i></div>
                                    </div>
                                    <div class="grid grid-cols-2 gap-3">
                                        <div class="rounded-2xl bg-blue-400/15 border border-blue-200/20 p-3">
                                            <p class="text-[10px] font-black text-blue-100 uppercase">Masuk</p>
                                            <div id="avgJamMasuk" class="text-2xl font-black font-mono text-white mt-1">--:--</div>
                                            <p id="avgJamMasukCount" class="text-[10px] text-blue-50 font-bold">0 data</p>
                                        </div>
                                        <div class="rounded-2xl bg-pusk-yellow/20 border border-pusk-yellow/30 p-3">
                                            <p class="text-[10px] font-black text-yellow-100 uppercase">Pulang</p>
                                            <div id="avgJamPulang" class="text-2xl font-black font-mono text-white mt-1">--:--</div>
                                            <p id="avgJamPulangCount" class="text-[10px] text-yellow-50 font-bold">0 data</p>
                                        </div>
                                    </div>
                                </div>

                                <h3 class="text-left text-sm font-extrabold text-white mb-3 ml-1 uppercase tracking-wider">Pilih Tipe Scan</h3>
                                <div class="grid grid-cols-2 gap-4 relative z-10">
                                    <button onclick="mulaiLiveScan('MASUK')" title="Untuk pegawai yang hadir bekerja dan memulai jam kerja." class="action-card bg-gradient-to-br from-pusk-blue to-blue-700 text-white rounded-3xl p-5 cursor-pointer flex flex-col items-center justify-center border border-pusk-yellow/40 shadow-xl">
                                        <i class="fa-solid fa-camera-retro text-3xl mb-3 float-soft"></i>
                                        <span class="font-extrabold text-sm">Scan MASUK</span>
                                        <span class="text-[10px] text-blue-50 mt-1 text-center">Khusus hadir masuk di lokasi resmi</span>
                                    </button>
                                    <button onclick="mulaiLiveScan('PULANG')" title="Untuk pegawai yang sudah absen masuk dan mengakhiri jam kerja." class="action-card bg-gradient-to-br from-blue-500 to-pusk-blue text-white rounded-3xl p-5 cursor-pointer flex flex-col items-center justify-center border border-pusk-yellow/40 shadow-xl">
                                        <i class="fa-solid fa-camera text-3xl mb-3 float-soft"></i>
                                        <span class="font-extrabold text-sm">Scan PULANG</span>
                                        <span class="text-[10px] text-blue-50 mt-1 text-center">Menutup kehadiran hari ini</span>
                                    </button>
                                    <button onclick="mulaiLiveScan('IZIN')" title="Untuk pengajuan izin dengan alasan jelas dan persetujuan atasan." class="action-card bg-gradient-to-br from-amber-400 to-yellow-600 text-white rounded-3xl p-4 cursor-pointer flex flex-col items-center justify-center border border-amber-300/40 shadow-xl">
                                        <i class="fa-solid fa-file-signature text-2xl mb-2"></i>
                                        <span class="font-extrabold text-sm">Scan IZIN</span>
                                        <span class="text-[10px] text-amber-50 mt-1 text-center">Tidak perlu absen masuk/pulang</span>
                                    </button>
                                    <button onclick="mulaiLiveScan('SAKIT')" title="Untuk keterangan sakit saat tidak dapat bekerja." class="action-card bg-gradient-to-br from-blue-400 to-blue-700 text-white rounded-3xl p-4 cursor-pointer flex flex-col items-center justify-center border border-blue-200/40 shadow-xl">
                                        <i class="fa-solid fa-notes-medical text-2xl mb-2"></i>
                                        <span class="font-extrabold text-sm">Scan SAKIT</span>
                                        <span class="text-[10px] text-blue-50 mt-1 text-center">Catat status sakit</span>
                                    </button>
                                    <button onclick="mulaiLiveScan('TL')" title="Untuk pegawai yang melaksanakan tugas luar resmi." class="action-card bg-gradient-to-br from-pusk-blue to-blue-800 text-white rounded-3xl p-4 cursor-pointer flex flex-col items-center justify-center border border-pusk-yellow/40 shadow-xl col-span-2">
                                        <i class="fa-solid fa-briefcase text-2xl mb-2"></i>
                                        <span class="font-extrabold text-sm">Scan TL / Tugas Luar</span>
                                        <span class="text-[10px] text-yellow-50 mt-1 text-center">Tugas resmi di luar area, tidak perlu absen masuk/pulang</span>
                                    </button>
                                </div>
                                <p class="text-[10px] text-blue-100 mt-4"><i class="fa-solid fa-circle-info mr-1"></i> Scanner khusus Live Camera. MASUK/PULANG wajib valid lokasi; IZIN/SAKIT/TL memiliki tombol scan sendiri dan tidak dicampur dengan absen masuk.</p>
                            </div>
                        </div>

                        <div class="guide-panel-responsive block relative bg-gradient-to-br from-white via-blue-50 to-yellow-50 p-4 sm:p-6 md:p-8">
                            <div class="grid grid-cols-1 gap-4">
                                <div class="info-card-modern rounded-3xl p-5 bg-white border border-blue-100 shadow-sm">
                                    <div class="flex items-start gap-3">
                                        <div class="w-11 h-11 rounded-2xl bg-blue-100 text-pusk-blue flex items-center justify-center shrink-0"><i class="fa-solid fa-route"></i></div>
                                        <div>
                                            <h3 class="text-base font-black text-slate-900">Alur Absen Live Scan</h3>
                                            <ol class="mt-3 space-y-2 text-xs text-slate-600 font-semibold leading-relaxed">
                                                <li><b>1.</b> Aktifkan GPS presisi tinggi dan klik <b>Cek Lokasi Sekarang</b>.</li>
                                                <li><b>2.</b> Pilih tombol sesuai kebutuhan: <b>MASUK</b>, <b>PULANG</b>, <b>IZIN</b>, <b>SAKIT</b>, atau <b>TL</b>.</li>
                                                <li><b>3.</b> Arahkan kamera ke QR dengan jarak ±15–30 cm dan cahaya cukup.</li>
                                            </ol>
                                        </div>
                                    </div>
                                </div>

                                <div class="info-card-modern rounded-3xl p-5 bg-gradient-to-br from-blue-50 to-white border border-blue-100 shadow-sm">
                                    <h3 class="text-sm font-black text-pusk-blue mb-3 uppercase tracking-wider"><i class="fa-solid fa-user-shield mr-1"></i>Mekanisme Izin, Sakit & TL</h3>
                                    <div class="grid grid-cols-1 gap-3">
                                        <div class="rounded-2xl bg-white/80 border border-amber-100 p-3"><p class="text-[10px] font-black text-amber-600 uppercase">Izin</p><p class="text-xs text-slate-700 mt-1 font-semibold">Ajukan dengan alasan jelas dan persetujuan atasan .</p></div>
                                        <div class="rounded-2xl bg-white/80 border border-rose-100 p-3"><p class="text-[10px] font-black text-rose-600 uppercase">Sakit</p><p class="text-xs text-slate-700 mt-1 font-semibold">Laporkan kondisi kesehatan. Jika ada surat keterangan, simpan sebagai bukti pendukung sesuai kebijakan unit.</p></div>
                                        <div class="rounded-2xl bg-white/80 border border-blue-100 p-3"><p class="text-[10px] font-black text-blue-600 uppercase">TL / Tugas Luar</p><p class="text-xs text-slate-700 mt-1 font-semibold">Digunakan untuk tugas resmi di luar lokasi induk/Polindes dan dicatat oleh admin berdasarkan surat/perintah tugas.</p></div>
                                    </div>
                                </div>

                                <div class="info-card-modern rounded-3xl p-5 bg-gradient-to-br from-yellow-50 to-white border border-yellow-100 shadow-sm">
                                    <h3 class="text-sm font-black text-pusk-blue mb-3 uppercase tracking-wider"><i class="fa-solid fa-door-open mr-1"></i>Status yang Tidak Perlu Absen Pulang</h3>
                                    <div class="flex flex-wrap gap-2 mb-3">
                                        <span class="px-3 py-1 rounded-full bg-amber-100 text-amber-700 text-[10px] font-black">IZIN</span>
                                        <span class="px-3 py-1 rounded-full bg-rose-100 text-rose-700 text-[10px] font-black">SAKIT</span>
                                        <span class="px-3 py-1 rounded-full bg-blue-100 text-blue-700 text-[10px] font-black">TL</span>
                                        <span class="px-3 py-1 rounded-full bg-purple-100 text-purple-700 text-[10px] font-black">CUTI</span>
                                    </div>
                                    <p class="text-xs text-slate-600 font-semibold leading-relaxed">Pegawai dengan status <b>IZIN</b>, Sakit, TL, atau Cuti tidak wajib scan pulang pada hari yang sama karena diselesaikan melalui scan/status khusus. Pegawai yang sudah scan <b>MASUK</b> tetap perlu scan <b>PULANG</b> agar data lengkap.</p>
                                </div>

                                <div id="jamOperasionalInfo" class="text-xs font-extrabold text-pusk-blue bg-yellow-50 border border-yellow-100 rounded-2xl px-4 py-3">Jam Operasional Scan: memuat data...</div>
                            </div>
                        </div>
                    </div>
                </div>
                <button onclick="navTo('loginView')" class="mt-8 text-xs font-semibold text-gray-400 hover:text-pusk-blue transition py-2 px-4 rounded-full hover:bg-gray-200"><i class="fa-solid fa-shield-halved mr-2"></i> Akses Administrator</button>
            </div>
        </section>

        <!-- VIEW: LOGIN ADMIN MODERN -->
        <section id="loginView" class="page-section">
            <div class="flex flex-col items-center justify-center min-h-[75vh]">
                <div class="login-ultra-card rounded-[2rem] w-full max-w-5xl shadow-2xl border border-white/70 relative overflow-hidden flex flex-col md:flex-row">

                    <!-- Sisi Kiri: Gambar Cover Menarik -->
                    <div class="hidden md:block md:w-1/2 relative overflow-hidden bg-pusk-blue scan-grid-bg">
                        <img id="loginBgImg" src="https://images.unsplash.com/photo-1519494026892-80bbd2d6fd0d?q=80&w=1000&auto=format&fit=crop" alt="Login Background" class="absolute inset-0 w-full h-full object-cover opacity-60 mix-blend-overlay transition-transform duration-[10s] hover:scale-110">
                        <div class="absolute inset-0 bg-gradient-to-t md:bg-gradient-to-r from-pusk-blue/90 via-pusk-blue/40 to-transparent"></div>
                        <div class="absolute bottom-0 left-0 p-10 text-white z-10 w-full">
                            <div class="w-16 h-1 bg-pusk-yellow mb-4 rounded-full"></div>
                            <h2 class="text-3xl font-extrabold mb-2 leading-tight" id="loginCoverTitle">Sistem Terpadu Kepegawaian</h2>
                            <p class="text-sm text-blue-100 opacity-90 leading-relaxed pr-4">Digitalisasi manajemen absensi modern, cepat, dan presisi tinggi dengan pelacakan Geofencing.</p>
                            <div class="mt-5 flex flex-wrap gap-2">
                                <span class="login-feature-chip"><i class="fa-solid fa-shield-halved text-pusk-yellow"></i> Secure Admin</span>
                                <span class="login-feature-chip"><i class="fa-solid fa-satellite-dish text-emerald-300"></i> Realtime GPS</span>
                                <span class="login-feature-chip"><i class="fa-solid fa-wand-magic-sparkles text-cyan-200"></i> Smart Report</span>
                            </div>
                        </div>
                    </div>

                    <!-- Sisi Kanan: Form Login -->
                    <div class="w-full md:w-1/2 p-8 sm:p-10 lg:p-12 relative flex flex-col justify-center bg-white/75 backdrop-blur-xl">
                        <div class="absolute -top-16 -right-16 w-48 h-48 bg-pusk-blue opacity-10 rounded-full blur-3xl"></div>

                        <div class="text-center mb-8 relative z-10">
                            <div class="w-24 h-24 mx-auto mb-4 animate-bounce-slow">
                                <div class="bg-white p-2 rounded-full shadow-xl flex items-center justify-center border-[3px] border-pusk-yellow w-full h-full overflow-hidden">
                                    <img id="loginLogoImg" src="" alt="Logo" class="hidden w-full h-full object-contain">
                                    <i id="loginLogoIcon" class="fa-solid fa-hospital fa-3x text-pusk-blue"></i>
                                </div>
                            </div>
                            <h2 id="loginAppName" class="text-2xl font-extrabold text-pusk-blue tracking-wide">PORTAL ABSENSI</h2>
                            <p class="text-xs text-gray-500 font-bold uppercase tracking-widest mt-1">Administrator Command Center</p><div class="mt-3 inline-flex items-center gap-2 px-3 py-1 rounded-full bg-emerald-50 text-emerald-700 border border-emerald-100 text-[10px] font-black uppercase tracking-widest"><span class="w-2 h-2 rounded-full bg-emerald-400 animate-pulse"></span> Sistem Online & Terenkripsi</div>
                        </div>

                        <div class="space-y-5 relative z-10">
                            <div>
                                <div class="relative">
                                    <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none"><i class="fa-solid fa-user text-gray-400"></i></div>
                                    <input type="text" id="logUser" class="login-input-modern w-full pl-11 pr-4 py-3.5 bg-white/90 border border-blue-100 rounded-2xl focus:ring-2 focus:ring-pusk-blue focus:bg-white outline-none transition text-sm font-bold" placeholder="Username">
                                </div>
                            </div>
                            <div>
                                <div class="relative">
                                    <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none"><i class="fa-solid fa-lock text-gray-400"></i></div>
                                    <input type="password" id="logPass" class="login-input-modern w-full pl-11 pr-12 py-3.5 bg-white/90 border border-blue-100 rounded-2xl focus:ring-2 focus:ring-pusk-blue focus:bg-white outline-none transition text-sm font-bold" placeholder="Password">
                                    <button type="button" onclick="togglePassword()" class="absolute inset-y-0 right-0 pr-4 flex items-center text-gray-400 hover:text-pusk-blue transition"><i class="fa-solid fa-eye" id="eyeIcon"></i></button>
                                </div>
                            </div>
                            <button onclick="doLogin()" class="w-full bg-gradient-to-r from-pusk-blue via-blue-700 to-cyan-700 text-white font-extrabold py-4 px-4 rounded-2xl shadow-xl hover:shadow-2xl transition-all active:scale-95 text-sm uppercase tracking-wider mt-4">
                                Login ke Panel <i class="fa-solid fa-arrow-right-to-bracket ml-2"></i>
                            </button>
                        </div>
                        <button onclick="navTo('scanView')" class="w-full mt-6 text-xs font-bold text-gray-400 hover:text-pusk-blue relative z-10 transition"><i class="fa-solid fa-arrow-left mr-1"></i> Kembali ke Menu Awal</button>
                    </div>
                </div>
            </div>
        </section>

        <!-- VIEW: DASHBOARD ADMIN -->
        <section id="dashView" class="page-section">
            <div class="flex justify-between items-center mb-8 border-b-2 border-gray-200 pb-4">
                <div>
                    <h2 class="text-3xl font-extrabold text-pusk-blue">Overview Dashboard</h2>
                    <p class="text-gray-500 text-sm mt-1">Pilih menu untuk mengelola sistem absensi.</p>
                </div>
            </div>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-6 mb-8">
                <div class="bg-white rounded-2xl shadow-sm p-6 border border-gray-100 hover:shadow-xl hover:border-purple-200 transition-all cursor-pointer group" onclick="navTo('harianView')">
                    <div class="w-12 h-12 rounded-xl bg-purple-50 text-purple-600 flex items-center justify-center text-xl mb-4 group-hover:bg-purple-600 group-hover:text-white transition-colors"><i class="fa-solid fa-calendar-check"></i></div>
                    <p class="text-xs text-gray-400 font-bold uppercase tracking-wider mb-1">Pantau Realtime</p>
                    <h3 class="text-md font-bold text-gray-800">Absensi Harian</h3>
                </div>
                <div class="bg-white rounded-2xl shadow-sm p-6 border border-gray-100 hover:shadow-xl hover:border-indigo-200 transition-all cursor-pointer group" onclick="navTo('kegiatanView')">
                    <div class="w-12 h-12 rounded-xl bg-indigo-50 text-indigo-600 flex items-center justify-center text-xl mb-4 group-hover:bg-indigo-600 group-hover:text-white transition-colors"><i class="fa-solid fa-clipboard-list"></i></div>
                    <p class="text-xs text-gray-400 font-bold uppercase tracking-wider mb-1">Cetak Manual</p>
                    <h3 class="text-md font-bold text-gray-800">Kegiatan Dinas</h3>
                </div>
                <div class="bg-white rounded-2xl shadow-sm p-6 border border-gray-100 hover:shadow-xl hover:border-blue-200 transition-all cursor-pointer group" onclick="navTo('pegawaiView')">
                    <div class="w-12 h-12 rounded-xl bg-blue-50 text-blue-600 flex items-center justify-center text-xl mb-4 group-hover:bg-blue-600 group-hover:text-white transition-colors"><i class="fa-solid fa-users"></i></div>
                    <p class="text-xs text-gray-400 font-bold uppercase tracking-wider mb-1">Database & ID</p>
                    <h3 class="text-md font-bold text-gray-800">Data Pegawai</h3>
                </div>
                <div class="bg-white rounded-2xl shadow-sm p-6 border border-gray-100 hover:shadow-xl hover:border-green-200 transition-all cursor-pointer group" onclick="navTo('laporanView')">
                    <div class="w-12 h-12 rounded-xl bg-green-50 text-green-600 flex items-center justify-center text-xl mb-4 group-hover:bg-green-600 group-hover:text-white transition-colors"><i class="fa-solid fa-file-pdf"></i></div>
                    <p class="text-xs text-gray-400 font-bold uppercase tracking-wider mb-1">Unduh Rekap</p>
                    <h3 class="text-md font-bold text-gray-800">Laporan Bulan</h3>
                </div>
                <div class="bg-white rounded-2xl shadow-sm p-6 border border-gray-100 hover:shadow-xl hover:border-yellow-300 transition-all cursor-pointer group" onclick="navTo('settingView')">
                    <div class="w-12 h-12 rounded-xl bg-yellow-50 text-yellow-600 flex items-center justify-center text-xl mb-4 group-hover:bg-yellow-400 group-hover:text-white transition-colors"><i class="fa-solid fa-gear"></i></div>
                    <p class="text-xs text-gray-400 font-bold uppercase tracking-wider mb-1">Konfigurasi</p>
                    <h3 class="text-md font-bold text-gray-800">Pengaturan</h3>
                </div>
            </div>
            <div class="md:hidden mt-4">
                <button onclick="logoutAdmin()" class="bg-red-50 text-red-600 shadow-sm p-4 rounded-xl font-bold w-full flex justify-center items-center"><i class="fa-solid fa-power-off mr-2"></i> Logout</button>
            </div>
        </section>

        <!-- VIEW: KEGIATAN MANUAL PUSKESMAS -->
        <section id="kegiatanView" class="page-section">
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center mb-6 gap-3">
                <div>
                    <h2 class="text-2xl font-extrabold text-pusk-blue">Absensi Manual Kegiatan</h2>
                    <p class="text-sm text-gray-500">Cetak lembar tanda tangan manual (hitam-putih formal) untuk kegiatan.</p>
                </div>
            </div>

            <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                <!-- Form Filter -->
                <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 lg:col-span-1 h-fit">
                    <h3 class="text-md font-bold text-gray-800 mb-4">Informasi Kegiatan</h3>
                    <div class="space-y-4">
                        <div>
                            <label class="block text-xs font-bold text-gray-600 uppercase mb-1">Tanggal Pelaksanaan</label>
                            <input type="date" id="kegTgl" class="w-full px-4 py-2.5 bg-gray-50 border border-gray-200 rounded-xl" onchange="loadPegawaiKegiatan()">
                        </div>
                        <div>
                            <label class="block text-xs font-bold text-gray-600 uppercase mb-1">Nama Kegiatan</label>
                            <input type="text" id="kegNama" class="w-full px-4 py-2.5 bg-gray-50 border border-gray-200 rounded-xl font-bold text-pusk-blue" placeholder="Cth: Lokakarya Lintas Sektor">
                        </div>
                        <button onclick="loadPegawaiKegiatan()" class="w-full bg-indigo-50 text-indigo-600 border border-indigo-200 font-bold py-2.5 px-4 rounded-xl shadow-sm hover:bg-indigo-100 transition">
                            <i class="fa-solid fa-users-viewfinder mr-2"></i> Muat Pegawai Tersedia
                        </button>
                    </div>
                </div>

                <!-- List Pegawai Checkbox & Stats -->
                <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 lg:col-span-2">
                    <div class="flex justify-between items-center mb-4">
                        <h3 class="text-md font-bold text-gray-800">Daftar Pegawai Bisa Ikut</h3>
                        <span class="text-xs text-gray-400 italic">*(Tidak termasuk Izin/Sakit/TL/Cuti)*</span>
                    </div>

                    <!-- Statistik Card Kegiatan -->
                    <div class="grid grid-cols-5 gap-3 mb-4">
                        <div class="bg-indigo-50 border border-indigo-100 p-2 rounded-xl text-center"><p class="text-[10px] font-bold text-indigo-500 uppercase">Tersedia</p><p class="text-xl font-extrabold text-indigo-700" id="kegCountTersedia">0</p></div>
                        <div class="bg-blue-50 border border-blue-100 p-2 rounded-xl text-center"><p class="text-[10px] font-bold text-blue-500 uppercase">TL</p><p class="text-xl font-extrabold text-blue-700" id="kegCountTL">0</p></div>
                        <div class="bg-yellow-50 border border-yellow-100 p-2 rounded-xl text-center"><p class="text-[10px] font-bold text-yellow-600 uppercase">Sakit</p><p class="text-xl font-extrabold text-yellow-700" id="kegCountSakit">0</p></div>
                        <div class="bg-orange-50 border border-orange-100 p-2 rounded-xl text-center"><p class="text-[10px] font-bold text-orange-500 uppercase">Izin</p><p class="text-xl font-extrabold text-orange-700" id="kegCountIzin">0</p></div>
                        <div class="bg-purple-50 border border-purple-100 p-2 rounded-xl text-center"><p class="text-[10px] font-bold text-purple-500 uppercase">Cuti</p><p class="text-xl font-extrabold text-purple-700" id="kegCountCuti">0</p></div>
                    </div>

                    <div class="bg-gray-50 border border-gray-200 rounded-xl p-3 mb-4 flex flex-col md:flex-row gap-3 items-center justify-between">
                        <label class="flex items-center gap-3 cursor-pointer text-sm font-bold text-pusk-blue w-full md:w-auto">
                            <input type="checkbox" id="checkAllKeg" class="custom-checkbox" onchange="toggleAllKeg(this)">
                            Pilih Semua Pegawai
                        </label>
                        <div class="relative w-full md:w-1/2">
                            <i class="fa-solid fa-magnifying-glass absolute left-3 top-2.5 text-gray-400 text-sm"></i>
                            <input type="text" id="searchKegiatan" placeholder="Cari nama pegawai..." onkeyup="renderListKegiatan()" class="w-full pl-9 pr-3 py-1.5 bg-white border border-gray-200 rounded-lg text-sm outline-none focus:border-pusk-blue">
                        </div>
                    </div>

                    <div id="listPegawaiKegiatan" class="max-h-80 overflow-y-auto grid grid-cols-1 md:grid-cols-2 gap-2 p-2 border border-gray-100 rounded-xl">
                        <p class="text-sm text-gray-500 col-span-2 text-center py-4">Silakan pilih tanggal dan klik Muat Pegawai Tersedia.</p>
                    </div>

                    <div class="mt-6 flex justify-end">
                        <button onclick="cetakPDFKegiatan()" class="bg-gradient-to-r from-green-500 to-green-600 hover:from-green-600 hover:to-green-700 text-white font-extrabold py-3 px-6 rounded-xl shadow-lg transition">
                            <i class="fa-solid fa-file-signature mr-2"></i> Simpan & Cetak Daftar Hadir (B&W)
                        </button>
                    </div>
                </div>
            </div>
        </section>

        <!-- VIEW: ABSENSI HARIAN -->
        <section id="harianView" class="page-section">
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center mb-4 gap-3">
                <div>
                    <h2 class="text-2xl font-extrabold text-pusk-blue">Absensi Harian</h2>
                    <p class="text-sm text-gray-500">Monitor, set manual, dan kelola absensi per hari.</p>
                </div>
                <div class="flex gap-2 w-full sm:w-auto">
                    <button onclick="loadAbsenHarian(); if(document.getElementById('scanView') && document.getElementById('scanView').classList.contains('active')) cekLokasiSekarang();" class="bg-white text-gray-700 px-4 py-2 rounded-xl shadow-sm font-bold border border-gray-200 hover:bg-gray-50 transition"><i class="fa-solid fa-arrows-rotate"></i></button>
                    <button onclick="cetakPDFHarian()" class="flex-1 sm:flex-none bg-blue-600 text-white px-4 py-2 rounded-xl shadow-md font-bold hover:bg-blue-700 transition"><i class="fa-solid fa-download mr-1"></i> PDF Hari Ini</button>
                    <button onclick="cetakPDFStatusHarian()" class="flex-1 sm:flex-none bg-amber-600 text-white px-4 py-2 rounded-xl shadow-md font-bold hover:bg-amber-700 transition"><i class="fa-solid fa-filter-circle-xmark mr-1"></i> PDF Status</button>
                </div>
            </div>

           <div class="grid grid-cols-2 md:grid-cols-5 gap-4 mb-4">
    <!-- Total Pegawai -->
    <div class="bg-white p-4 rounded-2xl shadow-sm border border-gray-100 flex items-center justify-between">
        <div><p class="text-xs text-gray-500 font-bold uppercase">Total Pegawai</p><h4 class="text-2xl font-extrabold text-pusk-blue" id="statTotal">0</h4></div>
        <div class="w-10 h-10 bg-blue-50 text-blue-500 rounded-full flex items-center justify-center"><i class="fa-solid fa-users"></i></div>
    </div>
    <!-- Sudah Hadir -->
    <div class="bg-white p-4 rounded-2xl shadow-sm border border-gray-100 flex items-center justify-between">
        <div><p class="text-xs text-gray-500 font-bold uppercase">Sudah Hadir</p><h4 class="text-2xl font-extrabold text-green-600" id="statHadir">0</h4></div>
        <div class="w-10 h-10 bg-green-50 text-green-500 rounded-full flex items-center justify-center"><i class="fa-solid fa-check"></i></div>
    </div>
    <!-- Belum Absen (belum masuk sama sekali) -->
    <div class="bg-white p-4 rounded-2xl shadow-sm border border-gray-100 flex items-center justify-between">
        <div><p class="text-xs text-gray-500 font-bold uppercase">Belum Absen</p><h4 class="text-2xl font-extrabold text-gray-600" id="statBelum">0</h4></div>
        <div class="w-10 h-10 bg-gray-100 text-gray-500 rounded-full flex items-center justify-center"><i class="fa-solid fa-clock"></i></div>
    </div>
    <!-- BELUM ABSEN PULANG (kartu baru) -->
    <div class="bg-white p-4 rounded-2xl shadow-sm border border-gray-100 flex items-center justify-between">
        <div><p class="text-xs text-gray-500 font-bold uppercase">Belum Absen Pulang</p><h4 class="text-2xl font-extrabold text-amber-600" id="statBelumPulang">0</h4></div>
        <div class="w-10 h-10 bg-amber-50 text-amber-500 rounded-full flex items-center justify-center"><i class="fa-solid fa-person-walking-arrow-right"></i></div>
    </div>
    <!-- Izin/Sakit/TL/Cuti -->
    <div class="bg-white p-4 rounded-2xl shadow-sm border border-gray-100 flex items-center justify-between">
        <div><p class="text-xs text-gray-500 font-bold uppercase">Izin/Sakit/TL/Cuti</p><h4 class="text-2xl font-extrabold text-orange-500" id="statLain">0</h4></div>
        <div class="w-10 h-10 bg-orange-50 text-orange-500 rounded-full flex items-center justify-center"><i class="fa-solid fa-notes-medical"></i></div>
    </div>
</div>

            <div class="bg-white p-4 rounded-2xl shadow-sm border border-gray-100 mb-4 flex flex-col lg:flex-row gap-4 items-end">
                <div class="w-full lg:w-1/4">
                    <label class="block text-[10px] font-bold text-gray-500 uppercase mb-1">Pilih Tanggal</label>
                    <input type="date" id="filterTglHarian" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg text-sm" onchange="loadAbsenHarian()">
                </div>
                <div class="w-full lg:w-1/4 relative">
                    <label class="block text-[10px] font-bold text-gray-500 uppercase mb-1">Cari Pegawai</label>
                    <i class="fa-solid fa-magnifying-glass absolute left-3 top-7 text-gray-400 text-sm"></i>
                    <input type="text" id="searchHarian" placeholder="Ketik nama..." onkeyup="renderTableHarian(true)" class="w-full pl-9 pr-3 py-2 bg-gray-50 border border-gray-200 rounded-lg text-sm">
                </div>
                <div class="w-full lg:w-1/4">
                    <label class="block text-[10px] font-bold text-gray-500 uppercase mb-1">Saring Status</label>
                    <select id="filterKetHarian" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg text-sm" onchange="renderTableHarian(true)">
    <option value="ALL">Semua</option>
    <option value="HADIR">Hadir</option>
    <option value="BELUM ABSEN PULANG">Belum Absen Pulang</option>
    <option value="BELUM ABSEN">Belum Absen</option>
    <option value="SAKIT">Sakit</option>
    <option value="IZIN">Izin</option>
    <option value="CUTI">Cuti</option>
    <option value="TL">TL</option>
    <option value="ALPA">Alpa</option>
</select>
                </div>
                <div class="w-full lg:w-1/4">
                    <button onclick="terapkanBulkAlpa()" class="w-full bg-red-50 text-red-600 border border-red-200 font-bold py-2 px-3 rounded-lg text-sm hover:bg-red-100 transition"><i class="fa-solid fa-user-xmark mr-1"></i> Set ALPA Masal</button>
                </div>
            </div>

            <div class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden">
                <div class="overflow-x-auto">
                    <table class="min-w-full divide-y divide-gray-100">
                        <thead class="bg-gray-50/80">
                            <tr>
                                <th class="px-4 py-3 text-left text-[11px] font-extrabold text-gray-500 uppercase tracking-wider">Info Pegawai</th>
                                <th class="px-4 py-3 text-center text-[11px] font-extrabold text-gray-500 uppercase tracking-wider">Masuk</th>
                                <th class="px-4 py-3 text-center text-[11px] font-extrabold text-gray-500 uppercase tracking-wider">Pulang</th>
                                <th class="px-4 py-3 text-center text-[11px] font-extrabold text-gray-500 uppercase tracking-wider">Status</th>
                                <th class="px-4 py-3 text-left text-[11px] font-extrabold text-gray-500 uppercase tracking-wider">Catatan</th>
                                <th class="px-4 py-3 text-center text-[11px] font-extrabold text-gray-500 uppercase tracking-wider">Aksi</th>
                            </tr>
                        </thead>
                        <tbody id="tbHarian" class="bg-white divide-y divide-gray-100">
                            <tr><td colspan="6" class="text-center py-10 text-gray-500"><i class="fa-solid fa-spinner fa-spin fa-2x mb-3"></i></td></tr>
                        </tbody>
                    </table>
                </div>
                <!-- Pagination Controls Harian -->
                <div class="bg-gray-50 px-4 py-3 border-t border-gray-100 flex flex-col sm:flex-row justify-between items-center gap-4">
                    <div class="flex items-center gap-2 text-xs font-semibold text-gray-600">
                        <span>Tampilkan:</span>
                        <select id="selRowsHarian" onchange="rowsPerPageHarian=this.value; renderTableHarian(true)" class="border border-gray-300 rounded px-2 py-1 outline-none cursor-pointer">
                            <option value="10">10</option><option value="25">25</option><option value="50">50</option><option value="ALL">Semua</option>
                        </select>
                        <span id="infoPageHarian" class="ml-2">0 dari 0</span>
                    </div>
                    <div id="pagControlsHarian" class="flex gap-1"></div>
                </div>
            </div>
        </section>

        <!-- VIEW: KELOLA PEGAWAI -->
        <section id="pegawaiView" class="page-section">
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center mb-6 gap-3">
                <div>
                    <h2 class="text-2xl font-extrabold text-pusk-blue">Data Pegawai</h2>
                    <p class="text-sm text-gray-500">Kelola dan cetak ID Card Barcode Pegawai.</p>
                </div>
                <div class="flex gap-2 w-full sm:w-auto">
                    <button onclick="loadDataPegawai()" class="bg-white hover:bg-gray-50 text-gray-700 px-4 py-2 rounded-xl shadow-sm font-bold border border-gray-200 transition"><i class="fa-solid fa-arrows-rotate"></i></button>
                    <button onclick="bukaModalTambah()" class="flex-1 sm:flex-none bg-pusk-blue hover:bg-blue-800 text-white px-5 py-2 rounded-xl shadow-md font-bold transition"><i class="fa-solid fa-user-plus mr-2"></i> Tambah</button>
                </div>
            </div>

           <div class="flex flex-col md:flex-row gap-3 mb-4 items-end">
    <!-- Search Field -->
    <div class="flex-1 relative">
        <label class="block text-[10px] font-bold text-gray-500 uppercase mb-1 ml-1">Cari Nama / NIP</label>
        <div class="relative">
            <i class="fa-solid fa-magnifying-glass absolute left-4 top-1/2 -translate-y-1/2 text-gray-400"></i>
            <input type="text" id="searchPegawai"
                   placeholder="Cari nama atau NIP pegawai..."
                   onkeyup="renderTablePegawai(true)"
                   class="w-full pl-11 pr-4 py-3 bg-white border border-gray-200 rounded-xl focus:ring-2 focus:ring-pusk-blue shadow-sm text-sm">
        </div>
    </div>

    <!-- Filter Lokasi -->
    <div class="w-full md:w-72">
        <label class="block text-[10px] font-bold text-gray-500 uppercase mb-1 ml-1">Lokasi Tugas</label>
        <select id="filterLokasi"
                onchange="renderTablePegawai(true)"
                class="w-full px-4 py-3 bg-white border border-gray-200 rounded-xl text-sm focus:ring-2 focus:ring-pusk-blue shadow-sm">
            <option value="SEMUA">Semua Lokasi</option>
        </select>
    </div>
</div>

            <div class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden">
                <div class="overflow-x-auto">
                    <table class="min-w-full divide-y divide-gray-100">
                        <thead class="bg-gray-50/80">
                            <tr>
                                <th class="px-5 py-3 text-left text-[11px] font-extrabold text-gray-500 uppercase">Profil Pegawai</th>
                                <th class="px-5 py-3 text-left text-[11px] font-extrabold text-gray-500 uppercase">Pangkat / Golongan</th>
                                <th class="px-5 py-3 text-left text-[11px] font-extrabold text-gray-500 uppercase">Jabatan / Lokasi</th>
                                <th class="px-5 py-3 text-center text-[11px] font-extrabold text-gray-500 uppercase">Aksi</th>
                            </tr>
                        </thead>
                        <tbody id="tbPegawai" class="bg-white divide-y divide-gray-100">
                            <tr><td colspan="4" class="text-center py-10"><i class="fa-solid fa-spinner fa-spin text-pusk-blue fa-2x"></i></td></tr>
                        </tbody>
                    </table>
                </div>
                <!-- Pagination Controls Pegawai -->
                <div class="bg-gray-50 px-4 py-3 border-t border-gray-100 flex flex-col sm:flex-row justify-between items-center gap-4">
                    <div class="flex items-center gap-2 text-xs font-semibold text-gray-600">
                        <span>Tampilkan:</span>
                        <select id="selRowsPegawai" onchange="rowsPerPagePegawai=this.value; renderTablePegawai(true)" class="border border-gray-300 rounded px-2 py-1 outline-none cursor-pointer">
                            <option value="10">10</option><option value="25">25</option><option value="50">50</option><option value="ALL">Semua</option>
                        </select>
                        <span id="infoPagePegawai" class="ml-2">0 dari 0</span>
                    </div>
                    <div id="pagControlsPegawai" class="flex gap-1"></div>
                </div>
            </div>
            <div class="mt-5 flex justify-end">
                <button onclick="cetakSemuaID()" class="bg-white text-green-600 border border-green-200 hover:bg-green-50 px-5 py-2.5 rounded-xl shadow-sm font-bold text-sm transition"><i class="fa-solid fa-print mr-2"></i> Cetak Kartu (Batch 6/Halaman)</button>
            </div>
        </section>

        <!-- VIEW: PENGATURAN -->
        <section id="settingView" class="page-section">
            <h2 class="text-2xl font-extrabold text-pusk-blue mb-2">Pengaturan Sistem</h2>
            <p class="text-sm text-gray-500 mb-8">Konfigurasi branding aplikasi, nama instansi, dan kordinat GPS.</p>

            <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-6">
                <!-- Branding Aplikasi -->
                <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 lg:col-span-2 flex flex-col md:flex-row gap-6 items-center">
                    <div class="w-full md:w-1/2">
                        <h3 class="text-lg font-extrabold text-gray-800 mb-4"><i class="fa-solid fa-palette text-pusk-blue mr-2"></i> Branding Aplikasi</h3>
                        <div class="space-y-3">
                            <div><label class="block text-xs font-bold text-gray-600 uppercase mb-1">Nama Aplikasi Utama</label><input type="text" id="setAppName" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg text-sm font-bold" placeholder="PORTAL ABSENSI"></div>
                            <div><label class="block text-xs font-bold text-gray-600 uppercase mb-1">Teks Kecil di Bawah Nama (Header)</label><input type="text" id="setAppHeaderName" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg text-sm font-bold" placeholder="Sistem Digital"></div>
                            <div><label class="block text-xs font-bold text-gray-600 uppercase mb-1">URL Logo Aplikasi (Opsional)</label><input type="text" id="setAppLogo" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg text-sm" placeholder="https://link-gambar.com/logo.png"></div>
                            <div><label class="block text-xs font-bold text-gray-600 uppercase mb-1">URL Gambar Layar Login (Opsional)</label><input type="text" id="setAppLoginBg" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg text-sm" placeholder="https://link-gambar.com/bg-login.jpg"></div>
                            <div><label class="block text-xs font-bold text-gray-600 uppercase mb-1">Pesan Running Text Landing</label><input type="text" id="setRunningText" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg text-sm font-semibold" placeholder="Selamat datang..."></div>
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                                <div><label class="block text-xs font-bold text-emerald-700 uppercase mb-1">Pesan Berhasil Absen Masuk</label><textarea id="setPesanMasuk" rows="3" class="w-full px-3 py-2 bg-emerald-50 border border-emerald-100 rounded-lg text-sm font-semibold" placeholder="Gunakan {nama} dan {waktu}"></textarea></div>
                                <div><label class="block text-xs font-bold text-orange-700 uppercase mb-1">Pesan Berhasil Absen Pulang</label><textarea id="setPesanPulang" rows="3" class="w-full px-3 py-2 bg-orange-50 border border-orange-100 rounded-lg text-sm font-semibold" placeholder="Gunakan {nama}, {waktu}, dan {jamKerja}"></textarea></div>
                            </div>
                        </div>
                    </div>
                    <div class="w-full md:w-1/2 bg-blue-50 p-4 rounded-xl border border-blue-100 h-full flex flex-col justify-center">
                        <p class="text-xs text-blue-700 font-semibold mb-2"><i class="fa-solid fa-circle-info"></i> Tampilan di Header Atas:</p>
                        <div class="bg-pusk-blue p-4 rounded-xl flex items-center gap-3">
                            <!-- Logo App Dengan Efek Berputar di Preview -->
                            <div class="glow-border w-11 h-11 p-[2px] shadow-md">
                                <div class="glow-border-inner overflow-hidden">
                                    <i class="fa-solid fa-hospital fa-lg text-pusk-blue" id="previewIconOnly"></i>
                                </div>
                            </div>
                            <div><h1 class="text-white font-bold text-sm tracking-wide text-shimmer" id="previewAppText">PORTAL ABSENSI</h1><p class="text-pusk-yellow text-[10px] font-semibold" id="previewAppSubText">Sistem Digital</p></div>
                        </div>
                    </div>
                </div>

                <div class="bg-white p-6 rounded-2xl shadow-sm border border-red-100 lg:col-span-2">
                    <h3 class="text-lg font-extrabold text-red-700 mb-3 pb-3 border-b border-red-100"><i class="fa-solid fa-mobile-screen-button mr-2"></i> Keamanan Perangkat (Anti-Titip Absen)</h3>
                    <p class="text-xs text-gray-600 mb-4 leading-relaxed">Sistem ini mengunci 1 HP hanya untuk 1 Pegawai. Anda dapat mereset kunci perangkat pada HP ini dengan menekan tombol di bawah.</p>
                    <button onclick="resetDeviceBinding()" class="w-full md:w-auto px-6 text-xs font-bold text-red-700 bg-red-50 hover:bg-red-100 border border-red-200 py-3 rounded-xl transition"><i class="fa-solid fa-unlock-keyhole mr-2"></i> Reset Kunci HP Ini</button>
                </div>

                <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100">
                    <h3 class="text-lg font-extrabold text-gray-800 mb-5 pb-3 border-b border-gray-100"><i class="fa-solid fa-hospital-user text-pusk-blue mr-2"></i> Identitas Instansi</h3>
                    <div class="space-y-4">
                        <div>
                            <label class="block text-xs font-bold text-gray-600 uppercase mb-1.5 ml-1">Nama Unit/Puskesmas</label>
                            <input type="text" id="setNamaPusk" class="w-full px-4 py-2.5 bg-gray-50 border border-gray-200 rounded-xl font-bold">
                        </div>
                        <div>
                            <label class="block text-xs font-bold text-gray-600 uppercase mb-1.5 ml-1">Alamat Instansi</label>
                            <textarea id="setAlamatPusk" rows="3" class="w-full px-4 py-2.5 bg-gray-50 border border-gray-200 rounded-xl font-semibold text-sm" placeholder="Contoh: Jl. ... Kecamatan ... Kabupaten Sintang"></textarea>
                        </div>
                        <div class="bg-blue-50 p-4 rounded-xl border border-blue-100">
                            <p class="text-xs font-bold text-pusk-blue mb-3 uppercase"><i class="fa-solid fa-signature mr-1"></i> Penanda Tangan Laporan PDF</p>
                            <div class="space-y-3">
                                <div><label class="block text-xs font-semibold text-gray-600 mb-1 ml-1">Nama Pejabat</label><input type="text" id="setKapusNama" class="w-full px-3 py-2 border border-blue-200 rounded-lg text-sm font-semibold"></div>
                                <div><label class="block text-xs font-semibold text-gray-600 mb-1 ml-1">NIP Pejabat</label><input type="text" id="setKapusNip" class="w-full px-3 py-2 border border-blue-200 rounded-lg text-sm font-mono"></div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100">
                    <h3 class="text-lg font-extrabold text-gray-800 mb-5 pb-3 border-b border-gray-100"><i class="fa-solid fa-map-location-dot text-pusk-blue mr-2"></i> Area Absen Induk</h3>
                    <div class="grid grid-cols-2 gap-4 mb-4">
                        <div><label class="block text-xs font-bold text-gray-600 uppercase mb-1.5 ml-1">Latitude</label><input type="text" id="setLatPusk" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg text-sm font-mono"></div>
                        <div><label class="block text-xs font-bold text-gray-600 uppercase mb-1.5 ml-1">Longitude</label><input type="text" id="setLngPusk" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg text-sm font-mono"></div>
                    </div>
                    <div class="mb-5"><label class="block text-xs font-bold text-gray-600 uppercase mb-1.5 ml-1">Radius Maksimal (m)</label><input type="number" id="setRadPusk" class="w-full px-3 py-2.5 bg-gray-50 border border-gray-200 rounded-xl text-sm font-bold text-pusk-blue"></div>
                    <button onclick="getCurrentLocFor('Pusk')" class="w-full text-xs font-bold text-blue-700 bg-blue-50 border border-blue-200 py-2.5 rounded-xl transition"><i class="fa-solid fa-location-crosshairs mr-1"></i> Ambil Koordinat Sekarang</button>
                </div>

                <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100">
                    <h3 class="text-lg font-extrabold text-gray-800 mb-5 pb-3 border-b border-gray-100"><i class="fa-solid fa-clock text-pusk-blue mr-2"></i> Kunci Jam Absensi</h3>
                    <p class="text-xs text-gray-500 mb-4">Atur jam operasional scan masuk/pulang. Di luar rentang ini, scan akan ditolak otomatis.</p>
                    <div class="space-y-4">
                        <div class="bg-emerald-50 border border-emerald-100 rounded-xl p-3">
                            <p class="text-[11px] font-extrabold text-emerald-700 mb-2 uppercase">Jam Absen Masuk</p>
                            <div class="grid grid-cols-2 gap-3">
                                <div><label class="block text-[10px] font-bold text-gray-600 mb-1">Mulai</label><input type="time" id="setJamMasukMulai" class="w-full px-3 py-2 bg-white border border-emerald-200 rounded-lg text-sm font-bold"></div>
                                <div><label class="block text-[10px] font-bold text-gray-600 mb-1">Akhir</label><input type="time" id="setJamMasukAkhir" class="w-full px-3 py-2 bg-white border border-emerald-200 rounded-lg text-sm font-bold"></div>
                            </div>
                        </div>
                        <div class="bg-orange-50 border border-orange-100 rounded-xl p-3">
                            <p class="text-[11px] font-extrabold text-orange-700 mb-2 uppercase">Jam Absen Pulang</p>
                            <div class="grid grid-cols-2 gap-3">
                                <div><label class="block text-[10px] font-bold text-gray-600 mb-1">Mulai</label><input type="time" id="setJamPulangMulai" class="w-full px-3 py-2 bg-white border border-orange-200 rounded-lg text-sm font-bold"></div>
                                <div><label class="block text-[10px] font-bold text-gray-600 mb-1">Akhir</label><input type="time" id="setJamPulangAkhir" class="w-full px-3 py-2 bg-white border border-orange-200 rounded-lg text-sm font-bold"></div>
                            </div>
                        </div>
                        <div class="bg-amber-50 border border-amber-100 rounded-xl p-3">
                            <p class="text-[11px] font-extrabold text-amber-700 mb-2 uppercase">Jam Absen Pulang Khusus Jumat</p>
                            <div class="grid grid-cols-2 gap-3">
                                <div><label class="block text-[10px] font-bold text-gray-600 mb-1">Mulai</label><input type="time" id="setJamPulangJumatMulai" class="w-full px-3 py-2 bg-white border border-amber-200 rounded-lg text-sm font-bold"></div>
                                <div><label class="block text-[10px] font-bold text-gray-600 mb-1">Akhir</label><input type="time" id="setJamPulangJumatAkhir" class="w-full px-3 py-2 bg-white border border-amber-200 rounded-lg text-sm font-bold"></div>
                            </div>
                        </div>
                        <label class="flex items-start gap-3 bg-blue-50 border border-blue-100 rounded-xl p-3 cursor-pointer">
                            <input type="checkbox" id="setTampilRataJam" class="mt-1 accent-pusk-blue">
                            <span><b class="text-xs text-pusk-blue uppercase">Tampilkan kartu rata-rata jam di landing</b><br><span class="text-[11px] text-gray-500 font-semibold">Jika aktif, landing menampilkan rata-rata jam masuk dan pulang hari ini dengan animasi modern.</span></span>
                        </label>
                        <div class="bg-indigo-50 border border-indigo-100 rounded-xl p-4 space-y-3">
                            <p class="text-[11px] font-extrabold text-indigo-700 uppercase"><i class="fa-solid fa-bell mr-1"></i> Notifikasi Update Saat Masuk</p>
                            <label class="flex items-start gap-3 cursor-pointer">
                                <input type="checkbox" id="setUpdateNotifAktif" class="mt-1 accent-indigo-600">
                                <span><b class="text-xs text-indigo-800 uppercase">Tampilkan popup update di halaman awal</b><br><span class="text-[11px] text-gray-500 font-semibold">Jika aktif, pengguna akan melihat dialog modern berisi pengumuman update saat pertama kali membuka aplikasi.</span></span>
                            </label>
                            <div><label class="block text-[10px] font-bold text-indigo-700 uppercase mb-1">Judul Notifikasi</label><input type="text" id="setUpdateNotifJudul" class="w-full px-3 py-2 bg-white border border-indigo-200 rounded-lg text-sm font-bold" placeholder="🚀 Update Sistem Absensi"></div>
                            <div><label class="block text-[10px] font-bold text-indigo-700 uppercase mb-1">Versi / Label Update</label><input type="text" id="setUpdateNotifVersi" class="w-full px-3 py-2 bg-white border border-indigo-200 rounded-lg text-sm font-semibold" placeholder="v1.0.0"></div>
                            <div><label class="block text-[10px] font-bold text-indigo-700 uppercase mb-1">Pesan Notifikasi</label><textarea id="setUpdateNotifPesan" rows="3" class="w-full px-3 py-2 bg-white border border-indigo-200 rounded-lg text-sm font-semibold" placeholder="Tuliskan info update terbaru..."></textarea></div>
                        </div>
                    </div>
                </div>

                <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 lg:col-span-2">
                    <h3 class="text-lg font-extrabold text-gray-800 mb-5 pb-3 border-b border-gray-100"><i class="fa-solid fa-house-medical-circle-check text-pusk-blue mr-2"></i> Tagging Lokasi Polindes</h3>
                    <p class="text-xs text-gray-500 mb-4"><i class="fa-solid fa-info-circle text-blue-500 mr-1"></i> Isi koordinat/radius per lokasi induk Polindes agar scan menyesuaikan otomatis.</p>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div class="bg-purple-50 border border-purple-100 rounded-xl p-3">
                            <p class="text-[11px] font-extrabold text-purple-700 mb-2 uppercase">Polindes Tebing Raya</p>
                            <div class="grid grid-cols-3 gap-2">
                                <input type="text" id="setLatPolTebing" placeholder="Lat" class="w-full px-2 py-2 bg-white border border-purple-200 rounded-lg text-xs font-mono">
                                <input type="text" id="setLngPolTebing" placeholder="Lng" class="w-full px-2 py-2 bg-white border border-purple-200 rounded-lg text-xs font-mono">
                                <input type="number" id="setRadPolTebing" placeholder="Rad(m)" class="w-full px-2 py-2 bg-white border border-purple-200 rounded-lg text-xs font-bold">
                            </div>
                        </div>
                        <div class="bg-purple-50 border border-purple-100 rounded-xl p-3">
                            <p class="text-[11px] font-extrabold text-purple-700 mb-2 uppercase">Polindes Mungguk Bantok</p>
                            <div class="grid grid-cols-3 gap-2">
                                <input type="text" id="setLatPolMungguk" placeholder="Lat" class="w-full px-2 py-2 bg-white border border-purple-200 rounded-lg text-xs font-mono">
                                <input type="text" id="setLngPolMungguk" placeholder="Lng" class="w-full px-2 py-2 bg-white border border-purple-200 rounded-lg text-xs font-mono">
                                <input type="number" id="setRadPolMungguk" placeholder="Rad(m)" class="w-full px-2 py-2 bg-white border border-purple-200 rounded-lg text-xs font-bold">
                            </div>
                        </div>
                        <div class="bg-purple-50 border border-purple-100 rounded-xl p-3">
                            <p class="text-[11px] font-extrabold text-purple-700 mb-2 uppercase">Polindes Kebiau Baru</p>
                            <div class="grid grid-cols-3 gap-2">
                                <input type="text" id="setLatPolKebiau" placeholder="Lat" class="w-full px-2 py-2 bg-white border border-purple-200 rounded-lg text-xs font-mono">
                                <input type="text" id="setLngPolKebiau" placeholder="Lng" class="w-full px-2 py-2 bg-white border border-purple-200 rounded-lg text-xs font-mono">
                                <input type="number" id="setRadPolKebiau" placeholder="Rad(m)" class="w-full px-2 py-2 bg-white border border-purple-200 rounded-lg text-xs font-bold">
                            </div>
                        </div>
                        <div class="bg-purple-50 border border-purple-100 rounded-xl p-3">
                            <p class="text-[11px] font-extrabold text-purple-700 mb-2 uppercase">Polindes Jerora Satu</p>
                            <div class="grid grid-cols-3 gap-2">
                                <input type="text" id="setLatPolJerora" placeholder="Lat" class="w-full px-2 py-2 bg-white border border-purple-200 rounded-lg text-xs font-mono">
                                <input type="text" id="setLngPolJerora" placeholder="Lng" class="w-full px-2 py-2 bg-white border border-purple-200 rounded-lg text-xs font-mono">
                                <input type="number" id="setRadPolJerora" placeholder="Rad(m)" class="w-full px-2 py-2 bg-white border border-purple-200 rounded-lg text-xs font-bold">
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="mt-8 flex justify-end">
                <button onclick="simpanSetting()" class="bg-pusk-blue hover:bg-blue-800 text-white font-extrabold py-3.5 px-8 rounded-xl shadow-lg transition"><i class="fa-solid fa-save mr-2"></i> Simpan Pembaruan Sistem</button>
            </div>
        </section>

        <!-- VIEW: LAPORAN BULANAN (PDF) -->
        <section id="laporanView" class="page-section">
            <h2 class="text-2xl font-extrabold text-pusk-blue mb-2">Cetak Dokumen Rekapitulasi Bulanan</h2>

            <div class="bg-white p-8 rounded-3xl shadow-sm border border-gray-100 mt-6 relative overflow-hidden">
                <div class="absolute top-0 right-0 p-8 opacity-5 text-9xl text-pusk-blue"><i class="fa-solid fa-file-pdf"></i></div>

                <div class="grid grid-cols-1 md:grid-cols-3 gap-5 items-end relative z-10 mb-6">
                    <div>
                        <label class="block text-xs font-bold text-gray-600 uppercase mb-2">Pilih Bulan</label>
                        <select id="lapBulan" onchange="hitungHariKerjaOtomatis(false)" class="w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl font-bold text-pusk-blue">
                            <option value="JANUARI">Januari</option><option value="FEBRUARI">Februari</option><option value="MARET" selected>Maret</option>
                            <option value="APRIL">April</option><option value="MEI">Mei</option><option value="JUNI">Juni</option>
                            <option value="JULI">Juli</option><option value="AGUSTUS">Agustus</option><option value="SEPTEMBER">September</option>
                            <option value="OKTOBER">Oktober</option><option value="NOVEMBER">November</option><option value="DESEMBER">Desember</option>
                        </select>
                    </div>
                    <div>
                        <label class="block text-xs font-bold text-gray-600 uppercase mb-2">Pilih Tahun</label>
                        <select id="lapTahun" onchange="hitungHariKerjaOtomatis(false)" class="w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl font-bold text-pusk-blue">
                            <option value="2024">2024</option><option value="2025">2025</option><option value="2026" selected>2026</option><option value="2027">2027</option>
                        </select>
                    </div>
                    <div>
                        <label class="block text-xs font-bold text-red-600 uppercase mb-2"><i class="fa-solid fa-asterisk"></i> Target Hari Kerja</label>
                        <input type="number" id="lapHariKerja" class="w-full px-4 py-3 bg-red-50 border-2 border-red-200 rounded-xl font-extrabold text-red-700 text-center text-lg" value="17">
                    </div>
                    <div>
                        <label class="block text-xs font-bold text-orange-600 uppercase mb-2"><i class="fa-solid fa-wand-magic-sparkles"></i> Database Libur + Manual</label>
                        <input type="number" id="lapLiburManual" onchange="hitungHariKerjaOtomatis(true)" class="w-full px-4 py-3 bg-orange-50 border-2 border-orange-200 rounded-xl font-extrabold text-orange-700 text-center text-lg" value="0">
                    </div>
                </div>
                <div id="liburInfoBox" class="bg-gradient-to-br from-blue-50 via-white to-orange-50 border border-blue-100 rounded-2xl p-4 mb-6 text-xs shadow-sm">
                    <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-2 mb-3">
                        <p class="font-black text-pusk-blue uppercase tracking-wider"><i class="fa-solid fa-wand-magic-sparkles mr-1 text-orange-500"></i>Database Libur Nasional</p>
                        <span id="liburAiBadge" class="inline-flex w-fit items-center gap-2 px-3 py-1 rounded-full bg-white border border-blue-100 text-[10px] font-black text-blue-700"><span class="w-2 h-2 rounded-full bg-blue-400 animate-pulse"></span>Database siap dibaca</span>
                    </div>
                    <p id="liburSummary" class="text-gray-700 font-semibold mt-1">Memuat data libur nasional...</p>
                    <div id="liburDetailList" class="mt-3 grid grid-cols-1 md:grid-cols-2 gap-2 max-h-36 overflow-y-auto pr-1"></div>
                    <p class="text-[10px] text-gray-500 mt-3"><i class="fa-solid fa-circle-info mr-1"></i>Input libur dapat dikoreksi manual. Sistem akan menghitung target hari kerja dari weekday dikurangi total libur pada input.</p>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-4 relative z-10">
                    <button onclick="generatePDF()" class="w-full bg-gradient-to-r from-red-600 to-red-500 hover:from-red-700 text-white font-extrabold py-3.5 px-4 rounded-xl shadow-lg transition">
                        <i class="fa-solid fa-download mr-2"></i> Laporan Rekap Kehadiran
                    </button>
                    <button onclick="generatePDFDetail()" class="w-full bg-gradient-to-r from-blue-500 to-blue-400 hover:from-blue-600 text-white font-extrabold py-3.5 px-4 rounded-xl shadow-lg transition">
                        <i class="fa-solid fa-business-time mr-2"></i> Laporan Total Jam Kerja
                    </button>
                    <button onclick="generatePDFASN()" class="w-full bg-gradient-to-r from-emerald-600 to-emerald-500 hover:from-emerald-700 text-white font-extrabold py-3.5 px-4 rounded-xl shadow-lg transition">
                        <i class="fa-solid fa-user-shield mr-2"></i> Laporan Khusus ASN
                    </button>
                    <button onclick="generatePDFPPPK()" class="w-full bg-gradient-to-r from-violet-600 to-indigo-500 hover:from-violet-700 text-white font-extrabold py-3.5 px-4 rounded-xl shadow-lg transition">
                        <i class="fa-solid fa-id-card-clip mr-2"></i> Laporan Khusus PPPK
                    </button>
                </div>
            </div>
        </section>

    </main>

    <footer class="gov-app-footer text-white">
        <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-2.5 grid grid-cols-1 lg:grid-cols-[minmax(0,1fr)_auto] items-center gap-3">
            <div class="flex items-center gap-3 min-w-0">
                <div class="gov-footer-logo w-10 h-10 rounded-2xl bg-white flex items-center justify-center shrink-0 overflow-hidden">
                    <img id="footerLogoImg" src="" alt="Logo" class="hidden w-full h-full object-contain p-1">
                    <i id="footerLogoIcon" class="fa-solid fa-hospital text-pusk-blue text-lg"></i>
                </div>
                <div class="min-w-0">
                    <div class="flex flex-wrap items-center gap-x-2 gap-y-1">
                        <p id="footerAppName" class="footer-title text-xs sm:text-sm font-black uppercase tracking-[0.18em] truncate">PORTAL ABSENSI</p>
                        <span class="gov-footer-dot w-2.5 h-2.5 rounded-full bg-emerald-300 shadow-[0_0_18px_rgba(110,231,183,.9)]"></span>
                        <p class="text-[9px] font-black uppercase tracking-[0.18em] text-pusk-yellow">Government Smart Service</p>
                    </div>
                    <p id="footerUnitName" class="footer-subtitle text-[10px] sm:text-[11px] text-blue-100 font-bold truncate">Sistem Absensi Digital Pemerintahan</p>
                    <p id="footerAddress" class="gov-footer-address text-[10px] text-blue-100/90 font-semibold leading-snug"><i class="fa-solid fa-location-dot text-pusk-yellow mr-1"></i>Alamat instansi belum diatur</p>
                </div>
            </div>
            <div class="footer-secondary flex flex-wrap items-center justify-start lg:justify-end gap-2 text-[10px] font-extrabold">
                <span class="gov-footer-chip text-emerald-100"><i class="fa-solid fa-right-to-bracket text-emerald-300"></i>Masuk <b id="footerJamMasuk" class="text-white">06:00-10:00</b></span>
                <span class="gov-footer-chip text-orange-100"><i class="fa-solid fa-right-from-bracket text-orange-300"></i>Pulang <b id="footerJamPulang" class="text-white">12:00-23:59</b></span>
                <span class="gov-footer-chip text-blue-100"><i class="fa-solid fa-shield-halved text-pusk-yellow"></i>Aman • Realtime • Presisi</span>
            </div>
        </div>
    </footer>

    <!-- Modal Form Pegawai LENGKAP -->
    <div id="modalTambahPegawai" class="hidden fixed inset-0 z-[60] flex items-center justify-center p-4">
        <div class="absolute inset-0 bg-gray-900/60 backdrop-blur-sm" onclick="document.getElementById('modalTambahPegawai').classList.add('hidden')"></div>
        <div class="bg-white rounded-3xl shadow-2xl w-full max-w-lg overflow-hidden animate-[fadeIn_0.3s] relative z-10 border border-gray-100">
            <div class="bg-pusk-blue text-white px-8 py-5 flex justify-between items-center">
                <div><h3 class="font-extrabold text-lg" id="modalTitlePegawai">Tambah Pegawai Baru</h3></div>
                <button onclick="document.getElementById('modalTambahPegawai').classList.add('hidden')" class="w-8 h-8 flex items-center justify-center bg-white/10 hover:bg-white/20 rounded-full transition"><i class="fa-solid fa-xmark"></i></button>
            </div>
            <div class="p-6 space-y-4 max-h-[70vh] overflow-y-auto">
                <input type="hidden" id="inIdQR">
                <div><label class="block text-xs font-bold text-gray-600 uppercase mb-2">Nama Lengkap & Gelar</label><input type="text" id="inNama" class="w-full px-4 py-2 bg-gray-50 border border-gray-200 rounded-lg focus:ring-2 focus:ring-pusk-blue outline-none" placeholder="Contoh: dr. Budi Santoso"></div>
                <div><label class="block text-xs font-bold text-gray-600 uppercase mb-2">NIP</label><input type="text" id="inNip" class="w-full px-4 py-2 bg-gray-50 border border-gray-200 rounded-lg focus:ring-2 focus:ring-pusk-blue outline-none" placeholder="Kosongkan jika non-ASN"></div>

                <div>
                    <label class="block text-xs font-bold text-gray-600 uppercase mb-2">Pangkat / Golongan</label>
                    <select id="inGolongan" class="w-full px-4 py-2 bg-gray-50 border border-gray-200 rounded-lg font-semibold text-sm">
                        <option value="-">Tidak Ada / Non-ASN</option>
                        <optgroup label="Golongan I (Juru)">
                            <option value="Juru Muda (I/a)">I/a - Juru Muda</option><option value="Juru Muda Tk. I (I/b)">I/b - Juru Muda Tk. I</option>
                            <option value="Juru (I/c)">I/c - Juru</option><option value="Juru Tk. I (I/d)">I/d - Juru Tk. I</option>
                        </optgroup>
                        <optgroup label="Golongan II (Pengatur)">
                            <option value="Pengatur Muda (II/a)">II/a - Pengatur Muda</option><option value="Pengatur Muda Tk. I (II/b)">II/b - Pengatur Muda Tk. I</option>
                            <option value="Pengatur (II/c)">II/c - Pengatur</option><option value="Pengatur Tk. I (II/d)">II/d - Pengatur Tk. I</option>
                        </optgroup>
                        <optgroup label="Golongan III (Penata)">
                            <option value="Penata Muda (III/a)">III/a - Penata Muda</option><option value="Penata Muda Tk. I (III/b)">III/b - Penata Muda Tk. I</option>
                            <option value="Penata (III/c)">III/c - Penata</option><option value="Penata Tk. I (III/d)">III/d - Penata Tk. I</option>
                        </optgroup>
                        <optgroup label="Golongan IV (Pembina)">
                            <option value="Pembina (IV/a)">IV/a - Pembina</option><option value="Pembina Tk. I (IV/b)">IV/b - Pembina Tk. I</option>
                            <option value="Pembina Utama Muda (IV/c)">IV/c - Pembina Utama Muda</option><option value="Pembina Utama Madya (IV/d)">IV/d - Pembina Utama Madya</option><option value="Pembina Utama (IV/e)">IV/e - Pembina Utama</option>
                        </optgroup>
                    </select>
                </div>

                <div><label class="block text-xs font-bold text-gray-600 uppercase mb-2">Posisi Jabatan</label><input type="text" id="inJabatan" class="w-full px-4 py-2 bg-gray-50 border border-gray-200 rounded-lg focus:ring-2 focus:ring-pusk-blue outline-none"></div>
                <div class="grid grid-cols-3 gap-4">
                    <div>
                        <label class="block text-xs font-bold text-gray-600 uppercase mb-2">Status Ikatan</label>
                        <select id="inStatus" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg font-semibold text-sm">
                            <option value="ASN">ASN</option><option value="PPPK">PPPK</option><option value="PPPKPW">PPPKPW</option>
                            <option value="Kontrak BLUD" selected>Kontrak BLUD</option><option value="Magang">Magang</option><option value="Mahasiswa Magang">Mahasiswa Magang</option>
                        </select>
                    </div>
                    <div><label class="block text-xs font-bold text-gray-600 uppercase mb-2">Lokasi Induk</label><select id="inLokasi" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg font-semibold text-sm"><option value="Puskesmas">Puskesmas</option><option value="Polindes Tebing Raya">Polindes Tebing Raya</option><option value="Polindes Mungguk Bantok">Polindes Mungguk Bantok</option><option value="Polindes Kebiau Baru">Polindes Kebiau Baru</option><option value="Polindes Jerora Satu">Polindes Jerora Satu</option></select></div>
                    <div><label class="block text-xs font-bold text-gray-600 uppercase mb-2">Kunci Jam</label><select id="inKunci" class="w-full px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg font-semibold text-sm"><option value="IYA">IYA</option><option value="TIDAK">TIDAK</option></select></div>
                </div>
            </div>
            <div class="bg-gray-50 px-6 py-4 flex justify-end gap-3 border-t border-gray-100">
                <button onclick="document.getElementById('modalTambahPegawai').classList.add('hidden')" class="px-5 py-2 text-gray-600 hover:bg-gray-200 rounded-lg font-bold text-sm">Batal</button>
                <button onclick="simpanDataPegawai()" class="px-6 py-2 text-white bg-pusk-blue hover:bg-blue-800 rounded-lg font-bold text-sm shadow-md">Simpan Data</button>
            </div>
        </div>
    </div>

    <!-- Modal Preview ID Card -->
    <div id="modalPreviewCard" class="hidden fixed inset-0 z-[70] flex items-center justify-center p-4">
        <div class="absolute inset-0 bg-gray-900/80 backdrop-blur-sm" onclick="document.getElementById('modalPreviewCard').classList.add('hidden')"></div>
        <div class="bg-white rounded-2xl shadow-2xl overflow-hidden animate-[fadeIn_0.3s] relative z-10 w-full max-w-sm">
            <div class="bg-pusk-blue text-white px-6 py-4 flex justify-between items-center">
                <h3 class="font-bold text-md">Preview Kartu Absensi</h3>
                <button onclick="document.getElementById('modalPreviewCard').classList.add('hidden')" class="text-white hover:text-red-400 transition"><i class="fa-solid fa-xmark fa-xl"></i></button>
            </div>
            <div class="p-6 bg-gray-100 flex justify-center items-center overflow-auto" id="previewContainerWrapper">
                <div id="previewCardContent" class="shadow-md" style="background:#fff;"></div>
            </div>
            <div class="bg-white px-6 py-4 flex flex-col sm:flex-row justify-center gap-3 border-t border-gray-200">
                <button onclick="downloadCardImage()" class="w-full sm:w-auto px-4 py-2 text-white bg-green-600 hover:bg-green-700 rounded-lg font-bold text-sm shadow transition"><i class="fa-solid fa-download mr-1"></i> Simpan PNG</button>
                <button onclick="printSingleCard()" class="w-full sm:w-auto px-4 py-2 text-white bg-pusk-blue hover:bg-blue-800 rounded-lg font-bold text-sm shadow transition"><i class="fa-solid fa-print mr-1"></i> Cetak</button>
            </div>
        </div>
    </div>

    <div id="printArea" class="hidden"></div>

    <!-- ======================= -->
    <!-- SCRIPT LOGIKA FRONTEND -->
    <!-- ======================= -->
    <script>
        // Deklarasi Variabel Global & Pagination State
        let isAdminLoggedIn = false;
        let currentUserLat = null, currentUserLng = null, currentUserAcc = null;

        let listPegawaiRaw = [], dataAbsenHarianRaw = [], dataPegawaiKegiatan = [];
        let filteredHarian = [], filteredPegawai = [];
        let popupScannerWindow = null;
        let appSettingsCache = {};
        let isManualCheckingLocation = false;
        let hasShownUpdateNotif = false;

        let currentPgHarian = 1, rowsPerPageHarian = 10;
        let currentPgPegawai = 1, rowsPerPagePegawai = 10;

        function getLocalToday() {
            let d = new Date();
            let year = d.getFullYear();
            let month = ('0' + (d.getMonth() + 1)).slice(-2);
            let day = ('0' + d.getDate()).slice(-2);
            return `${year}-${month}-${day}`;
        }

        document.addEventListener('DOMContentLoaded', () => {
            dapatkanLokasiUser();
            let todayStr = getLocalToday();
            document.getElementById('filterTglHarian').value = todayStr;
            document.getElementById('kegTgl').value = todayStr;
            jalankanJam();
            muatIdentitasAplikasi();
            hitungHariKerjaOtomatis();
            const savedUser = localStorage.getItem('pusk_saved_user');
            if (savedUser) document.getElementById('logUser').value = savedUser;
            window.addEventListener('message', handlePopupScanMessage);
        });

        // 1. Fungsi Core / Init
        function jalankanJam() {
            const elTime = document.getElementById('liveTime'), elDate = document.getElementById('liveDate');
            setInterval(() => {
                const now = new Date();
                elTime.innerText = now.toLocaleTimeString('id-ID', { hour12: false });
                elDate.innerText = now.toLocaleDateString('id-ID', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' });
            }, 1000);
        }

        function navTo(viewId) {
            const adminRoutes = ['dashView', 'pegawaiView', 'settingView', 'laporanView', 'harianView', 'kegiatanView'];
            if(adminRoutes.includes(viewId) && !isAdminLoggedIn) viewId = 'loginView';

            document.querySelectorAll('.page-section').forEach(el => el.classList.remove('active'));
            document.getElementById(viewId).classList.add('active');

            if(viewId === 'pegawaiView') loadDataPegawai();
            if(viewId === 'settingView') loadPengaturan();
            if(viewId === 'harianView') loadAbsenHarian();
            if(viewId === 'laporanView') setTimeout(() => hitungHariKerjaOtomatis(false), 150);
            if(viewId === 'scanView' || viewId === 'loginView') muatIdentitasAplikasi();
        }

        function muatIdentitasAplikasi() {
            google.script.run.withSuccessHandler(res => {
                res = res || {};
                // Tampilkan notifikasi update sedini mungkin saat data pengaturan sudah diterima.
                maybeShowUpdateNotif(res);
                if(res.App_Name) {
                    document.getElementById('appNameTitle').innerText = res.App_Name;
                    document.getElementById('loginAppName').innerText = res.App_Name;
                    const footerAppName = document.getElementById('footerAppName'); if (footerAppName) footerAppName.innerText = res.App_Name;
                }

                const footerUnitName = document.getElementById('footerUnitName');
                if(res.App_Header_Name) {
                    document.getElementById('appHeaderName').innerText = res.App_Header_Name;
                    document.getElementById('previewAppSubText').innerText = res.App_Header_Name;
                    if (footerUnitName) footerUnitName.innerText = res.App_Header_Name;
                } else if (res.Nama_Puskesmas) {
                    let singkatan = res.Nama_Puskesmas.replace("UPTD PUSKESMAS", "Puskesmas").substring(0, 25);
                    document.getElementById('appHeaderName').innerText = singkatan;
                    document.getElementById('previewAppSubText').innerText = singkatan;
                    if (footerUnitName) footerUnitName.innerText = singkatan;
                }
                document.getElementById('runningTextMsg').innerText = res.Running_Text || "Info layanan digital UPTD Puskesmas Tanjung Puri • sehat bersama kami • masyarakat berprestasi • scan cepat, data akurat";

                if(res.App_Logo && res.App_Logo.trim() !== "") {
                    document.getElementById('appLogoIcon').classList.add('hidden');
                    document.getElementById('loginLogoIcon').classList.add('hidden');
                    let img = document.getElementById('appLogoImg'); img.src = res.App_Logo; img.classList.remove('hidden');
                    let imgLog = document.getElementById('loginLogoImg'); imgLog.src = res.App_Logo; imgLog.classList.remove('hidden');
                    const footerLogoIcon = document.getElementById('footerLogoIcon'); if (footerLogoIcon) footerLogoIcon.classList.add('hidden');
                    const footerLogoImg = document.getElementById('footerLogoImg'); if (footerLogoImg) { footerLogoImg.src = res.App_Logo; footerLogoImg.classList.remove('hidden'); }
                    const fav = document.getElementById('appFavicon'); if (fav) fav.href = res.App_Logo;
                } else {
                    document.getElementById('appLogoIcon').classList.remove('hidden');
                    document.getElementById('loginLogoIcon').classList.remove('hidden');
                    document.getElementById('appLogoImg').classList.add('hidden');
                    document.getElementById('loginLogoImg').classList.add('hidden');
                    const footerLogoIcon = document.getElementById('footerLogoIcon'); if (footerLogoIcon) footerLogoIcon.classList.remove('hidden');
                    const footerLogoImg = document.getElementById('footerLogoImg'); if (footerLogoImg) footerLogoImg.classList.add('hidden');
                }
                updateFooterInfo(res);
                appSettingsCache = res;
                updateJamOperasionalInfo();
                updateKartuRataJamLanding();
                if(res.App_Login_Bg && res.App_Login_Bg.trim() !== "") {
                    document.getElementById('loginBgImg').src = res.App_Login_Bg;
                } else {
                    document.getElementById('loginBgImg').src = "https://images.unsplash.com/photo-1519494026892-80bbd2d6fd0d?q=80&w=1000&auto=format&fit=crop";
                }
            }).getPengaturan();
        }

        function maybeShowUpdateNotif(settings) {
            if (hasShownUpdateNotif) return;
            hasShownUpdateNotif = true;
            const cfg = settings || {};
            const aktif = String(cfg.Update_Notif_Aktif || "YA").toUpperCase() !== "TIDAK";
            const pesan = String(cfg.Update_Notif_Pesan || "").trim();
            if (!aktif || !pesan) return;
            const judul = String(cfg.Update_Notif_Judul || "🚀 Update Sistem Absensi").trim();
            const versi = String(cfg.Update_Notif_Versi || "").trim();
            const labelVersi = versi ? `<span class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-indigo-50 border border-indigo-100 text-[11px] font-extrabold text-indigo-700"><i class="fa-solid fa-sparkles"></i>${escapeHtml(versi)}</span>` : "";
            Swal.fire({
                title: judul,
                html: `<div class="text-left space-y-3">${labelVersi}<p class="text-sm text-slate-600 font-semibold leading-relaxed">${escapeHtml(pesan).replace(/\n/g, '<br>')}</p></div>`,
                icon: 'info',
                confirmButtonText: 'Siap, Lanjutkan',
                confirmButtonColor: '#3730a3',
                background: '#ffffff',
                customClass: { popup: 'rounded-3xl shadow-2xl', title: 'text-indigo-800 font-extrabold text-xl' }
            });
        }

        // PENGATURAN LOAD & SIMPAN
        function loadPengaturan() {
            google.script.run.withSuccessHandler(res => {
                res = res || {};
                const setValue = (id, value) => { const el = document.getElementById(id); if (el) el.value = value; };
                const setChecked = (id, value) => { const el = document.getElementById(id); if (el) el.checked = !!value; };
                setValue('setAppName', res.App_Name || 'PORTAL ABSENSI');
                document.getElementById('setAppHeaderName').value = res.App_Header_Name || '';
                document.getElementById('setAppLogo').value = res.App_Logo || '';
                document.getElementById('setAppLoginBg').value = res.App_Login_Bg || '';
                document.getElementById('setRunningText').value = res.Running_Text || '';
                document.getElementById('setPesanMasuk').value = res.Pesan_Masuk_Berhasil || 'Absen MASUK berhasil. Selamat bekerja, {nama}! Semoga hari ini penuh manfaat dan pelayanan terbaik.';
                document.getElementById('setPesanPulang').value = res.Pesan_Pulang_Berhasil || 'Absen PULANG berhasil. Terima kasih, {nama}. Semoga perjalanan pulang aman dan sehat selalu.';
                document.getElementById('setNamaPusk').value = res.Nama_Puskesmas || '';
                document.getElementById('setAlamatPusk').value = res.Alamat_Puskesmas || '';
                document.getElementById('setKapusNama').value = res.Kepala_Nama || '';
                document.getElementById('setKapusNip').value = res.Kepala_NIP || '';
                document.getElementById('setLatPusk').value = res.Lat_Puskesmas || '';
                document.getElementById('setLngPusk').value = res.Lng_Puskesmas || '';
                document.getElementById('setRadPusk').value = res.Rad_Puskesmas || '';
                document.getElementById('setLatPolTebing').value = res.Lat_Pol_TebingRaya || '';
                document.getElementById('setLngPolTebing').value = res.Lng_Pol_TebingRaya || '';
                document.getElementById('setRadPolTebing').value = res.Rad_Pol_TebingRaya || '';
                document.getElementById('setLatPolMungguk').value = res.Lat_Pol_MunggukBantok || '';
                document.getElementById('setLngPolMungguk').value = res.Lng_Pol_MunggukBantok || '';
                document.getElementById('setRadPolMungguk').value = res.Rad_Pol_MunggukBantok || '';
                document.getElementById('setLatPolKebiau').value = res.Lat_Pol_KebiauBaru || '';
                document.getElementById('setLngPolKebiau').value = res.Lng_Pol_KebiauBaru || '';
                document.getElementById('setRadPolKebiau').value = res.Rad_Pol_KebiauBaru || '';
                document.getElementById('setLatPolJerora').value = res.Lat_Pol_JeroraSatu || '';
                document.getElementById('setLngPolJerora').value = res.Lng_Pol_JeroraSatu || '';
                document.getElementById('setRadPolJerora').value = res.Rad_Pol_JeroraSatu || '';
                document.getElementById('setJamMasukMulai').value = res.Jam_Masuk_Mulai || '06:00';
                document.getElementById('setJamMasukAkhir').value = res.Jam_Masuk_Akhir || '10:00';
                document.getElementById('setJamPulangMulai').value = res.Jam_Pulang_Mulai || '12:00';
                document.getElementById('setJamPulangAkhir').value = res.Jam_Pulang_Akhir || '23:59';
                setValue('setJamPulangJumatMulai', res.Jam_Pulang_Jumat_Mulai || res.Jam_Pulang_Mulai || '12:00');
                setValue('setJamPulangJumatAkhir', res.Jam_Pulang_Jumat_Akhir || res.Jam_Pulang_Akhir || '23:59');
                setChecked('setTampilRataJam', String(res.Tampil_Kartu_Rata_Jam || 'YA').toUpperCase() !== 'TIDAK');
                setChecked('setUpdateNotifAktif', String(res.Update_Notif_Aktif || 'YA').toUpperCase() !== 'TIDAK');
                setValue('setUpdateNotifJudul', res.Update_Notif_Judul || '🚀 Update Sistem Absensi');
                setValue('setUpdateNotifVersi', res.Update_Notif_Versi || '');
                setValue('setUpdateNotifPesan', res.Update_Notif_Pesan || '');
            }).withFailureHandler(err => {
                Swal.fire('Gagal Memuat Pengaturan', (err && err.message) ? err.message : 'Data pengaturan tidak bisa dimuat.', 'error');
            }).getPengaturan();
        }

        function simpanSetting() {
            let data = {
                App_Name: document.getElementById('setAppName').value,
                App_Header_Name: document.getElementById('setAppHeaderName').value,
                App_Logo: document.getElementById('setAppLogo').value,
                App_Login_Bg: document.getElementById('setAppLoginBg').value,
                Running_Text: document.getElementById('setRunningText').value,
                Pesan_Masuk_Berhasil: document.getElementById('setPesanMasuk').value,
                Pesan_Pulang_Berhasil: document.getElementById('setPesanPulang').value,
                Nama_Puskesmas: document.getElementById('setNamaPusk').value,
                Alamat_Puskesmas: document.getElementById('setAlamatPusk').value,
                Kepala_Nama: document.getElementById('setKapusNama').value,
                Kepala_NIP: document.getElementById('setKapusNip').value,
                Lat_Puskesmas: document.getElementById('setLatPusk').value,
                Lng_Puskesmas: document.getElementById('setLngPusk').value,
                Rad_Puskesmas: document.getElementById('setRadPusk').value,
                Lat_Pol_TebingRaya: document.getElementById('setLatPolTebing').value,
                Lng_Pol_TebingRaya: document.getElementById('setLngPolTebing').value,
                Rad_Pol_TebingRaya: document.getElementById('setRadPolTebing').value,
                Lat_Pol_MunggukBantok: document.getElementById('setLatPolMungguk').value,
                Lng_Pol_MunggukBantok: document.getElementById('setLngPolMungguk').value,
                Rad_Pol_MunggukBantok: document.getElementById('setRadPolMungguk').value,
                Lat_Pol_KebiauBaru: document.getElementById('setLatPolKebiau').value,
                Lng_Pol_KebiauBaru: document.getElementById('setLngPolKebiau').value,
                Rad_Pol_KebiauBaru: document.getElementById('setRadPolKebiau').value,
                Lat_Pol_JeroraSatu: document.getElementById('setLatPolJerora').value,
                Lng_Pol_JeroraSatu: document.getElementById('setLngPolJerora').value,
                Rad_Pol_JeroraSatu: document.getElementById('setRadPolJerora').value,
                Jam_Masuk_Mulai: document.getElementById('setJamMasukMulai').value,
                Jam_Masuk_Akhir: document.getElementById('setJamMasukAkhir').value,
                Jam_Pulang_Mulai: document.getElementById('setJamPulangMulai').value,
                Jam_Pulang_Akhir: document.getElementById('setJamPulangAkhir').value,
                Jam_Pulang_Jumat_Mulai: document.getElementById('setJamPulangJumatMulai').value,
                Jam_Pulang_Jumat_Akhir: document.getElementById('setJamPulangJumatAkhir').value,
                Tampil_Kartu_Rata_Jam: document.getElementById('setTampilRataJam').checked ? 'YA' : 'TIDAK',
                Update_Notif_Aktif: document.getElementById('setUpdateNotifAktif').checked ? 'YA' : 'TIDAK',
                Update_Notif_Judul: document.getElementById('setUpdateNotifJudul').value,
                Update_Notif_Versi: document.getElementById('setUpdateNotifVersi').value,
                Update_Notif_Pesan: document.getElementById('setUpdateNotifPesan').value
            };
            Swal.fire({title:'Menyimpan...', didOpen:()=>Swal.showLoading()});
            google.script.run.withSuccessHandler(res => {
                Swal.fire('Tersimpan', res.message, 'success');
                muatIdentitasAplikasi();
                loadPengaturan();
            }).withFailureHandler(err => {
                Swal.fire('Gagal Menyimpan', (err && err.message) ? err.message : 'Data pengaturan gagal disimpan.', 'error');
            }).simpanPengaturan(data);
        }

        function getCurrentLocFor(tipe) {
            if(!currentUserLat) return Swal.fire('Error', 'GPS belum terkunci.', 'error');
            if(tipe === 'Pusk') {
                document.getElementById('setLatPusk').value = currentUserLat;
                document.getElementById('setLngPusk').value = currentUserLng;
            }
            Swal.fire({icon:'success', title:'Disalin!', toast:true, position:'top-end', timer:2000, showConfirmButton:false});
        }

        document.getElementById('setAppName')?.addEventListener('input', function() { document.getElementById('previewAppText').innerText = this.value || "PORTAL ABSENSI"; });
        document.getElementById('setAppHeaderName')?.addEventListener('input', function() { document.getElementById('previewAppSubText').innerText = this.value || "Sistem Digital"; });

        function dapatkanLokasiUser() {
            if (navigator.geolocation) {
                navigator.geolocation.watchPosition(
                    (pos) => {
                        currentUserLat = pos.coords.latitude; currentUserLng = pos.coords.longitude; currentUserAcc = pos.coords.accuracy || 0;
                        let acc = Math.round(pos.coords.accuracy);
                        document.getElementById('gpsIcon').classList.replace('bg-gray-100', 'bg-green-100');
                        document.getElementById('gpsIcon').classList.replace('text-gray-500', 'text-green-600');
                        document.getElementById('gpsIcon').innerHTML = '<i class="fa-solid fa-satellite-dish"></i>';
                        document.getElementById('gpsTitle').innerText = "Lokasi Terhubung";
                        document.getElementById('gpsTitle').classList.replace('text-gray-800', 'text-green-700');
                        document.getElementById('gpsDetails').innerHTML = `Akurasi: <b class="text-green-700">${acc}m</b>. Siap absen.`;
                    },
                    (err) => {
                        document.getElementById('gpsIcon').innerHTML = '<i class="fa-solid fa-triangle-exclamation"></i>';
                        document.getElementById('gpsIcon').classList.replace('bg-gray-100', 'bg-red-100');
                        document.getElementById('gpsIcon').classList.replace('text-gray-500', 'text-red-500');
                        document.getElementById('gpsTitle').innerText = "Akses Lokasi Diblokir";
                        document.getElementById('gpsDetails').innerHTML = "Mohon nyalakan GPS & Izinkan akses lokasi di browser.";
                        const text = document.getElementById('distanceText'); const badge = document.getElementById('distanceBadge');
                        if (text) text.innerText = "GPS tidak tersedia.";
                        if (badge) { badge.innerText = "GPS Error"; badge.className = "text-[10px] font-extrabold px-2 py-1 rounded-full bg-red-100 text-red-700"; }
                    }, { enableHighAccuracy: true, timeout: 10000, maximumAge: 0 }
                );
            }
        }

        function updateKartuRataJamLanding() {
            const card = document.getElementById('avgJamCard');
            if (!card) return;
            const tampil = String(appSettingsCache.Tampil_Kartu_Rata_Jam || 'YA').toUpperCase() !== 'TIDAK';
            if (!tampil) { card.classList.add('hidden'); return; }
            card.classList.remove('hidden');
            google.script.run.withSuccessHandler(res => {
                res = res || {};
                document.getElementById('avgJamMasuk').innerText = res.rataMasuk || '--:--';
                document.getElementById('avgJamPulang').innerText = res.rataPulang || '--:--';
                document.getElementById('avgJamMasukCount').innerText = `${res.totalMasuk || 0} data masuk`;
                document.getElementById('avgJamPulangCount').innerText = `${res.totalPulang || 0} data pulang`;
            }).withFailureHandler(() => {
                document.getElementById('avgJamMasukCount').innerText = 'Belum tersedia';
                document.getElementById('avgJamPulangCount').innerText = 'Belum tersedia';
            }).getRataJamHariIni();
        }

        function updateFooterInfo(settings) {
            const cfg = settings || {};
            const alamat = String(cfg.Alamat_Puskesmas || '').trim();
            const footerAddress = document.getElementById('footerAddress');
            if (footerAddress) {
                footerAddress.innerHTML = `<i class="fa-solid fa-location-dot text-pusk-yellow mr-1"></i>${escapeHtml(alamat || 'Alamat instansi belum diatur')}`;
            }
            const masuk = `${cfg.Jam_Masuk_Mulai || '06:00'}-${cfg.Jam_Masuk_Akhir || '10:00'}`;
            const pulang = `${cfg.Jam_Pulang_Mulai || '12:00'}-${cfg.Jam_Pulang_Akhir || '23:59'}`;
            const footerJamMasuk = document.getElementById('footerJamMasuk'); if (footerJamMasuk) footerJamMasuk.innerText = masuk;
            const footerJamPulang = document.getElementById('footerJamPulang'); if (footerJamPulang) footerJamPulang.innerText = pulang;
        }

        function updateJamOperasionalInfo() {
            const el = document.getElementById('jamOperasionalInfo');
            if (!el) return;
            const jm1 = appSettingsCache.Jam_Masuk_Mulai || '06:00';
            const jm2 = appSettingsCache.Jam_Masuk_Akhir || '10:00';
            const jp1 = appSettingsCache.Jam_Pulang_Mulai || '12:00';
            const jp2 = appSettingsCache.Jam_Pulang_Akhir || '23:59';
            const jpf1 = appSettingsCache.Jam_Pulang_Jumat_Mulai || jp1;
            const jpf2 = appSettingsCache.Jam_Pulang_Jumat_Akhir || jp2;
            el.innerHTML = `<i class="fa-solid fa-clock mr-1"></i>Senin - Kamis: Masuk <b>${jm1} - ${jm2}</b>, Pulang <b>${jp1} - ${jp2}</b> &nbsp;|&nbsp; Jumat: Pulang <b>${jpf1} - ${jpf2}</b>`;
        }

        function updateDistanceInfo() {
            const text = document.getElementById('distanceText');
            const badge = document.getElementById('distanceBadge');
            const list = document.getElementById('distanceList');
            if (!text || !badge || !list || !currentUserLat || !currentUserLng) return;
            if (!isManualCheckingLocation) return;

            const lokasiRefs = [
                { nama: "Puskesmas", lat: parseFloat(appSettingsCache.Lat_Puskesmas || 0), lng: parseFloat(appSettingsCache.Lng_Puskesmas || 0), rad: parseFloat(appSettingsCache.Rad_Puskesmas || 100) },
                { nama: "Polindes Tebing Raya", lat: parseFloat(appSettingsCache.Lat_Pol_TebingRaya || 0), lng: parseFloat(appSettingsCache.Lng_Pol_TebingRaya || 0), rad: parseFloat(appSettingsCache.Rad_Pol_TebingRaya || 100) },
                { nama: "Polindes Mungguk Bantok", lat: parseFloat(appSettingsCache.Lat_Pol_MunggukBantok || 0), lng: parseFloat(appSettingsCache.Lng_Pol_MunggukBantok || 0), rad: parseFloat(appSettingsCache.Rad_Pol_MunggukBantok || 100) },
                { nama: "Polindes Kebiau Baru", lat: parseFloat(appSettingsCache.Lat_Pol_KebiauBaru || 0), lng: parseFloat(appSettingsCache.Lng_Pol_KebiauBaru || 0), rad: parseFloat(appSettingsCache.Rad_Pol_KebiauBaru || 100) },
                { nama: "Polindes Jerora Satu", lat: parseFloat(appSettingsCache.Lat_Pol_JeroraSatu || 0), lng: parseFloat(appSettingsCache.Lng_Pol_JeroraSatu || 0), rad: parseFloat(appSettingsCache.Rad_Pol_JeroraSatu || 100) }
            ];

            let terdekat = { nama: '-', jarak: Number.POSITIVE_INFINITY, efektif: Number.POSITIVE_INFINITY, rad: 100 };
            const htmlRows = lokasiRefs.map(l => {
                if (!l.lat || !l.lng) return `<div class="px-2 py-1 rounded-lg bg-gray-100 text-gray-500"><b>${l.nama}:</b> Koordinat belum diset</div>`;
                const d = hitungJarakClient(currentUserLat, currentUserLng, l.lat, l.lng);
                const toleransi = Math.min(currentUserAcc || 0, Math.max(50, l.rad));
                const efektif = Math.max(0, d - toleransi);
                if (efektif < terdekat.efektif) terdekat = { nama: l.nama, jarak: d, efektif: efektif, rad: l.rad };
                let cls = efektif <= l.rad ? 'bg-green-50 text-green-700 border border-green-100' : 'bg-amber-50 text-amber-700 border border-amber-100';
                return `<div class="px-2 py-1 rounded-lg ${cls}"><b>${l.nama}:</b> ${Math.round(d)} m <span class="opacity-70">(akurasi ±${Math.round(currentUserAcc || 0)}m, toleransi ${Math.round(toleransi)}m, efektif ${Math.round(efektif)}m, R ${Math.round(l.rad)}m)</span></div>`;
            }).join('');
            list.innerHTML = htmlRows;
            list.classList.remove('hidden');
            const loading = document.getElementById('distanceLoading');
            if (loading) { loading.classList.add('hidden'); loading.classList.remove('flex'); }
            const btn = document.getElementById('btnCekLokasi');
            if (btn) { btn.disabled = false; btn.innerHTML = '<i class="fa-solid fa-rotate-right mr-1"></i>Cek Ulang Lokasi'; }

            text.innerHTML = `Terdekat saat ini: <b>${terdekat.nama}</b> (${Math.round(terdekat.jarak)}m, efektif ${Math.round(terdekat.efektif)}m).`;
            if (terdekat.efektif <= terdekat.rad) {
                badge.innerText = "Siap Absen";
                badge.className = "text-[10px] font-extrabold px-2 py-1 rounded-full bg-green-100 text-green-700";
            } else if (terdekat.efektif <= terdekat.rad * 2) {
                badge.innerText = "Perlu Mendekat";
                badge.className = "text-[10px] font-extrabold px-2 py-1 rounded-full bg-amber-100 text-amber-700";
            } else {
                badge.innerText = "Terlalu Jauh";
                badge.className = "text-[10px] font-extrabold px-2 py-1 rounded-full bg-red-100 text-red-700";
            }
        }

        function cekLokasiSekarang() {
            isManualCheckingLocation = true;
            const loading = document.getElementById('distanceLoading');
            const list = document.getElementById('distanceList');
            const badge = document.getElementById('distanceBadge');
            const text = document.getElementById('distanceText');
            const btn = document.getElementById('btnCekLokasi');
            if (loading) { loading.classList.remove('hidden'); loading.classList.add('flex'); }
            if (list) list.classList.add('hidden');
            if (badge) { badge.innerText = 'Mengecek...'; badge.className = 'text-[10px] font-extrabold px-2 py-1 rounded-full bg-slate-200 text-slate-700'; }
            if (text) text.innerText = 'Sedang mengambil posisi terbaru...';
            if (btn) { btn.disabled = true; btn.innerHTML = '<i class="fa-solid fa-spinner fa-spin mr-1"></i>Memproses...'; }

            if (!navigator.geolocation) return finishCekLokasiError('Browser tidak mendukung GPS.');
            navigator.geolocation.getCurrentPosition((pos) => {
                currentUserLat = pos.coords.latitude;
                currentUserLng = pos.coords.longitude;
                currentUserAcc = pos.coords.accuracy || 0;
                updateDistanceInfo();
            }, () => finishCekLokasiError('Gagal mendapatkan lokasi. Pastikan GPS aktif.'), { enableHighAccuracy: true, timeout: 12000, maximumAge: 0 });
        }

        function finishCekLokasiError(msg) {
            const loading = document.getElementById('distanceLoading');
            const badge = document.getElementById('distanceBadge');
            const text = document.getElementById('distanceText');
            const btn = document.getElementById('btnCekLokasi');
            if (loading) { loading.classList.add('hidden'); loading.classList.remove('flex'); }
            if (badge) { badge.innerText = 'GPS Error'; badge.className = 'text-[10px] font-extrabold px-2 py-1 rounded-full bg-red-100 text-red-700'; }
            if (text) text.innerText = msg;
            if (btn) { btn.disabled = false; btn.innerHTML = '<i class="fa-solid fa-location-crosshairs mr-1"></i>Cek Lokasi Sekarang'; }
        }

        function hitungJarakClient(lat1, lon1, lat2, lon2) {
            const R = 6371e3;
            const p1 = lat1 * Math.PI / 180, p2 = lat2 * Math.PI / 180;
            const dp = (lat2 - lat1) * Math.PI / 180, dl = (lon2 - lon1) * Math.PI / 180;
            const a = Math.sin(dp / 2) ** 2 + Math.cos(p1) * Math.cos(p2) * Math.sin(dl / 2) ** 2;
            return R * (2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a)));
        }

        // 2. Keamanan & Scan Absensi User
        function playScanBeep() {
            try {
                const AudioCtx = window.AudioContext || window.webkitAudioContext;
                if (!AudioCtx) return;
                const ctx = new AudioCtx();
                const osc = ctx.createOscillator();
                const gain = ctx.createGain();
                osc.type = 'sine';
                osc.frequency.setValueAtTime(880, ctx.currentTime);
                osc.frequency.exponentialRampToValueAtTime(1320, ctx.currentTime + 0.08);
                gain.gain.setValueAtTime(0.0001, ctx.currentTime);
                gain.gain.exponentialRampToValueAtTime(0.18, ctx.currentTime + 0.01);
                gain.gain.exponentialRampToValueAtTime(0.0001, ctx.currentTime + 0.16);
                osc.connect(gain);
                gain.connect(ctx.destination);
                osc.start();
                osc.stop(ctx.currentTime + 0.17);
                setTimeout(() => ctx.close(), 260);
            } catch (_) {}
        }

        function handlePopupScanMessage(event) {
            if (event.origin !== window.location.origin) return;
            const payload = event.data || {};
            if (payload.type !== 'PUSK_QR_RESULT') return;
            if (!payload.decodedText || !payload.actionType) return;
            playScanBeep();
            handleScanResult(payload.decodedText, payload.actionType);
        }

        function isScanKeterangan(actionType) { return ['IZIN', 'SAKIT', 'TL'].includes(String(actionType || '').toUpperCase()); }

        function mulaiLiveScan(actionType) {
            if(!isScanKeterangan(actionType) && (!currentUserLat || !currentUserLng)) {
                Swal.fire('Peringatan GPS', 'Menunggu sinyal GPS...', 'warning');
                return;
            }
            bukaScannerOutFrame(actionType);
        }

        function bukaScannerOutFrame(actionType) {
            popupScannerWindow = window.open('', 'puskesmasLiveScanner', 'width=720,height=860,resizable=yes,scrollbars=yes');
            if (!popupScannerWindow) {
                Swal.fire('Popup Diblokir', 'Izinkan popup browser untuk membuka scanner out-frame.', 'warning');
                return;
            }
            popupScannerWindow.document.open();
            popupScannerWindow.document.write('<!doctype html><html lang="id"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>Menyiapkan Scanner</title><style>body{margin:0;min-height:100vh;display:grid;place-items:center;background:#003366;color:#fff;font-family:Arial,sans-serif}.box{text-align:center;padding:24px}.pulse{width:42px;height:42px;margin:0 auto 14px;border-radius:50%;border:4px solid rgba(255,255,255,.25);border-top-color:#FFD700;animation:spin 1s linear infinite}@keyframes spin{to{transform:rotate(360deg)}}</style></head><body><div class="box"><div class="pulse"></div><b>Menyiapkan Live Scanner...</b><p>Mohon tunggu sebentar.</p></div></body></html>');
            popupScannerWindow.document.close();
            const appLogoImgEl = document.getElementById('appLogoImg');
            const popupLogoUrl = String((appSettingsCache && appSettingsCache.App_Logo) || (appLogoImgEl ? appLogoImgEl.src : '') || '').trim();
            const appNameEl = document.getElementById('appNameTitle');
            const headerNameEl = document.getElementById('appHeaderName');
            const popupAppName = escapeHtml((appNameEl && appNameEl.innerText) || appSettingsCache.App_Name || 'PORTAL ABSENSI');
            const popupUnitName = escapeHtml((headerNameEl && headerNameEl.innerText) || appSettingsCache.App_Header_Name || appSettingsCache.Nama_Puskesmas || 'Sistem Digital Pemerintahan');
            const safeActionType = escapeHtml(actionType);
            const popupLogoHtml = popupLogoUrl
                ? `<img src="${escapeHtml(popupLogoUrl)}" alt="Logo Puskesmas" class="brand-logo-img">`
                : `<span class="brand-logo-icon">🏥</span>`;
            const popupHtml = `
<!doctype html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Live Scanner QR - Absensi</title>
  <style>
    *{box-sizing:border-box}
    body{margin:0;min-height:100vh;background:radial-gradient(circle at 15% 15%,rgba(255,215,0,.22),transparent 28%),radial-gradient(circle at 85% 8%,rgba(56,189,248,.2),transparent 24%),linear-gradient(135deg,#001b3f,#003366 48%,#075985);color:#e2e8f0;font-family:'Plus Jakarta Sans','Segoe UI',Arial,sans-serif;overflow-x:hidden}
    body::before{content:"";position:fixed;inset:-45%;background:conic-gradient(from 90deg,transparent,rgba(255,215,0,.2),rgba(14,165,233,.18),transparent 45%);animation:scannerOrbit 16s linear infinite;pointer-events:none}
    .wrap{position:relative;z-index:1;max-width:760px;margin:0 auto;padding:18px 14px 28px}
    .shell{position:relative;overflow:hidden;border:1px solid rgba(255,255,255,.18);border-radius:32px;background:rgba(255,255,255,.1);box-shadow:0 28px 80px rgba(0,0,0,.34);backdrop-filter:blur(18px)}
    .shell::after{content:"";position:absolute;inset:0;background:linear-gradient(120deg,transparent,rgba(255,255,255,.14),transparent);transform:translateX(-120%);animation:scannerShine 5.8s ease-in-out infinite;pointer-events:none}
    .brand{display:flex;align-items:center;gap:14px;padding:18px;border-bottom:1px solid rgba(255,255,255,.14);background:linear-gradient(90deg,rgba(255,255,255,.14),rgba(255,255,255,.05))}
    .brand-logo{width:58px;height:58px;border-radius:22px;background:#fff;display:flex;align-items:center;justify-content:center;overflow:hidden;flex-shrink:0;box-shadow:0 0 0 4px rgba(255,255,255,.16),0 0 32px rgba(255,215,0,.42)}
    .brand-logo-img{width:100%;height:100%;object-fit:contain;padding:6px}.brand-logo-icon{color:#003366;font-size:25px}
    .eyebrow{display:inline-flex;align-items:center;gap:7px;border:1px solid rgba(52,211,153,.35);background:rgba(16,185,129,.14);color:#bbf7d0;border-radius:999px;padding:5px 10px;font-size:10px;font-weight:900;letter-spacing:.16em;text-transform:uppercase}.pulse{width:8px;height:8px;border-radius:999px;background:#34d399;box-shadow:0 0 18px #34d399;animation:pulse 1.7s ease-in-out infinite}
    h1{font-size:24px;line-height:1.1;margin:9px 0 3px;color:#fff;font-weight:900}.unit{font-size:11px;color:#fde68a;font-weight:900;text-transform:uppercase;letter-spacing:.12em}.small{font-size:12px;color:#dbeafe;font-weight:700;line-height:1.55;margin-top:8px}
    .scanner-zone{padding:16px}.reader-frame{position:relative;border-radius:28px;padding:10px;background:linear-gradient(135deg,rgba(255,215,0,.9),rgba(56,189,248,.55),rgba(255,255,255,.18));box-shadow:inset 0 0 0 1px rgba(255,255,255,.2),0 20px 55px rgba(0,0,0,.3)}
    #reader{border-radius:22px;overflow:hidden;background:#020617;min-height:320px}.scan-corners{position:absolute;inset:24px;pointer-events:none;border-radius:22px;background:linear-gradient(#facc15,#facc15) left top/46px 4px no-repeat,linear-gradient(#facc15,#facc15) left top/4px 46px no-repeat,linear-gradient(#38bdf8,#38bdf8) right top/46px 4px no-repeat,linear-gradient(#38bdf8,#38bdf8) right top/4px 46px no-repeat,linear-gradient(#38bdf8,#38bdf8) left bottom/46px 4px no-repeat,linear-gradient(#38bdf8,#38bdf8) left bottom/4px 46px no-repeat,linear-gradient(#facc15,#facc15) right bottom/46px 4px no-repeat,linear-gradient(#facc15,#facc15) right bottom/4px 46px no-repeat;filter:drop-shadow(0 0 12px rgba(250,204,21,.7))}
    .meta{display:grid;grid-template-columns:1fr 1fr;gap:10px;padding:0 16px 16px}.meta-card{border:1px solid rgba(255,255,255,.14);background:rgba(255,255,255,.1);border-radius:20px;padding:12px}.meta-label{font-size:9px;color:#bfdbfe;text-transform:uppercase;letter-spacing:.14em;font-weight:900}.meta-value{font-size:12px;color:#fff;font-weight:900;margin-top:4px}.actions{display:flex;gap:10px;padding:0 16px 18px}.btn{width:100%;padding:13px 14px;border:none;border-radius:18px;background:linear-gradient(135deg,#ef4444,#b91c1c);color:#fff;font-weight:900;cursor:pointer;box-shadow:0 12px 26px rgba(185,28,28,.28)}
    @keyframes scannerOrbit{to{transform:rotate(360deg)}}@keyframes scannerShine{0%,45%{transform:translateX(-120%)}75%,100%{transform:translateX(120%)}}@keyframes pulse{0%,100%{transform:scale(.9);opacity:.7}50%{transform:scale(1.2);opacity:1}}
    @media(max-width:520px){.wrap{padding:10px}.brand{padding:14px}.brand-logo{width:50px;height:50px;border-radius:18px}h1{font-size:20px}.meta{grid-template-columns:1fr}.scanner-zone{padding:12px}#reader{min-height:280px}.shell{border-radius:26px}}
  </style>
</head>
<body>
  <div class="wrap">
    <div class="shell">
      <div class="brand">
        <div class="brand-logo">${popupLogoHtml}</div>
        <div>
          <div class="eyebrow"><span class="pulse"></span> Live Secure Scanner</div>
          <h1>Live Scan ${safeActionType}</h1>
          <div class="unit">${popupUnitName}</div>
          <div class="small">${popupAppName} siap membaca QR ID Card. Arahkan kamera ke kode QR dengan cahaya cukup; hasil akan otomatis terkirim dan jendela tertutup.</div>
        </div>
      </div>
      <div class="scanner-zone">
        <div class="reader-frame"><div id="reader"></div><div class="scan-corners"></div></div>
      </div>
      <div class="meta">
        <div class="meta-card"><div class="meta-label">Mode Scan</div><div class="meta-value">${safeActionType}</div></div>
        <div class="meta-card"><div class="meta-label">Status Kamera</div><div class="meta-value" id="cameraStatus">Mengaktifkan kamera...</div></div>
      </div>
      <div class="actions"><button class="btn" id="btnStop">✕ Tutup Scanner</button></div>
    </div>
  </div>
  <script>
    let scanner;
    function beep() {
      try {
        const AudioCtx = window.AudioContext || window.webkitAudioContext;
        if (!AudioCtx) return;
        const ctx = new AudioCtx();
        const osc = ctx.createOscillator();
        const gain = ctx.createGain();
        osc.frequency.value = 1040;
        gain.gain.value = 0.12;
        osc.connect(gain); gain.connect(ctx.destination);
        osc.start(); osc.stop(ctx.currentTime + 0.14);
        setTimeout(() => ctx.close(), 220);
      } catch(_) {}
    }
    function setStatus(text) { const el = document.getElementById('cameraStatus'); if (el) el.textContent = text; }
    function kirimHasil(text) {
      beep(); setStatus('QR terbaca, mengirim data...');
      try {
        if (window.opener && !window.opener.closed) {
          window.opener.postMessage({ type: 'PUSK_QR_RESULT', decodedText: text, actionType: '${safeActionType}' }, window.location.origin);
        }
      } catch(_) {}
      setTimeout(() => window.close(), 250);
    }
    function loadScannerLibrary() {
      return new Promise((resolve, reject) => {
        if (window.Html5Qrcode) { resolve(); return; }
        setStatus('Memuat mesin scanner...');
        const script = document.createElement('script');
        script.src = 'https://unpkg.com/html5-qrcode';
        script.async = true;
        script.onload = () => resolve();
        script.onerror = () => reject(new Error('Library scanner gagal dimuat'));
        document.head.appendChild(script);
        setTimeout(() => { if (!window.Html5Qrcode) reject(new Error('Library scanner timeout')); }, 15000);
      });
    }
    async function runScanner() {
      await loadScannerLibrary();
      scanner = new Html5Qrcode('reader');
      try {
        await scanner.start(
          { facingMode: { exact: 'environment' } },
          { fps: 14, qrbox: { width: 260, height: 260 } },
          (decodedText) => { scanner.stop().finally(() => kirimHasil(decodedText)); }
        );
        setStatus('Kamera belakang aktif');
      } catch (e1) {
        await scanner.start(
          { facingMode: 'environment' },
          { fps: 12, qrbox: { width: 250, height: 250 } },
          (decodedText) => { scanner.stop().finally(() => kirimHasil(decodedText)); }
        );
        setStatus('Kamera aktif');
      }
    }
    document.getElementById('btnStop').addEventListener('click', async () => {
      try { if (scanner) await scanner.stop(); } catch(_) {}
      window.close();
    });
    runScanner().catch((err) => { setStatus('Scanner gagal dimuat / kamera ditolak'); alert((err && err.message ? err.message + '. ' : '') + 'Pastikan internet aktif dan izin kamera diberikan.'); });
  <\/script>
</body>
</html>`;

            popupScannerWindow.document.open();
            popupScannerWindow.document.write(popupHtml);
            popupScannerWindow.document.close();
            popupScannerWindow.focus();
        }

        function resetDeviceBinding() {
            Swal.fire({ title: 'Reset Kunci HP?', text: 'Ini akan menghapus ikatan QR Code pada HP ini. Siapapun bisa absen lagi menggunakan HP ini.', icon: 'warning', showCancelButton: true, confirmButtonColor: '#d33', confirmButtonText: 'Ya, Reset'
            }).then((result) => { if(result.isConfirmed) { localStorage.removeItem('pusk_sintang_bound_qr'); Swal.fire('Berhasil', 'Kunci perangkat di HP ini telah dihapus.', 'success'); } });
        }

        function handleScanResult(decodedText, actionType, onFinish = null) {
            let boundQr = localStorage.getItem('pusk_sintang_bound_qr');
            if (boundQr && boundQr !== decodedText) {
                Swal.fire({ icon: 'error', title: 'Titip Absen Ditolak! 🚫', text: 'HP ini sudah dikunci untuk akun pegawai lain. Anda harus menggunakan HP Anda sendiri.', confirmButtonColor: '#d33' });
                if (onFinish) onFinish();
                return;
            }
            if (!boundQr) {
                Swal.fire({
                    title: 'Kunci Perangkat', html: '<p class="text-sm">Ini adalah absen pertama Anda di HP ini.<br><br>Sistem akan <b>MENGUNCI</b> HP ini khusus untuk akun Anda.</p>', icon: 'warning', showCancelButton: true, confirmButtonText: 'Ya, Kunci & Absen', confirmButtonColor: '#003366'
                }).then((result) => {
                    if (result.isConfirmed) {
                        localStorage.setItem('pusk_sintang_bound_qr', decodedText);
                        prosesKeBackend(decodedText, actionType, onFinish);
                    } else if (onFinish) onFinish();
                });
                return;
            }
            if (isScanKeterangan(actionType)) {
                tampilkanFormScanKeterangan(decodedText, actionType, onFinish);
                return;
            }
            prosesKeBackend(decodedText, actionType, onFinish);
        }

        function tampilkanFormScanKeterangan(decodedText, actionType, onFinish = null) {
            const labelMap = { IZIN: 'Izin', SAKIT: 'Sakit', TL: 'Tugas Luar (TL)' };
            Swal.fire({
                title: `Scan ${labelMap[actionType] || actionType}`,
                html: `<div class="text-left"><p class="text-xs text-slate-600 font-semibold mb-3">Status ini dicatat melalui tombol scan khusus dan tidak dianggap absen masuk.</p><label class="block text-xs font-black text-slate-500 uppercase mb-1">Catatan / alasan</label><textarea id="swal-scan-ket-alasan" class="swal2-textarea !mx-0 !mt-0 w-full" rows="4" placeholder="Tulis alasan jelas..."></textarea></div>`,
                showCancelButton: true,
                confirmButtonText: 'Simpan Status',
                confirmButtonColor: actionType === 'IZIN' ? '#d97706' : (actionType === 'SAKIT' ? '#e11d48' : '#2563eb'),
                preConfirm: () => {
                    const alasan = document.getElementById('swal-scan-ket-alasan').value.trim();
                    if (!alasan) { Swal.showValidationMessage('Catatan/alasan wajib diisi.'); return false; }
                    return alasan;
                }
            }).then(res => {
                if (!res.isConfirmed) { if (onFinish) onFinish(); return; }
                Swal.fire({ title: 'Menyimpan status...', allowOutsideClick: false, didOpen: () => Swal.showLoading() });
                google.script.run.withSuccessHandler(r => {
                    if (r && r.status) Swal.fire({ icon: 'success', title: 'Status Tersimpan', text: r.message || 'Status berhasil disimpan.', confirmButtonColor: '#003366' });
                    else Swal.fire({ icon: 'error', title: 'Status Ditolak', text: (r && r.message) ? r.message : 'Status gagal disimpan.', confirmButtonColor: '#dc2626' });
                    if (onFinish) onFinish();
                }).withFailureHandler(err => {
                    Swal.fire('Gagal', (err && err.message) ? err.message : 'Status gagal disimpan.', 'error');
                    if (onFinish) onFinish();
                }).prosesScanKeterangan(decodedText, actionType, res.value, currentUserLat || '', currentUserLng || '');
            });
        }

        function tampilkanAlertBerhasilAbsen(res, tipe) {
            const isMasuk = tipe === 'MASUK';
            const tone = isMasuk ? {
                icon: 'bg-emerald-100 text-emerald-600',
                label: 'text-emerald-600',
                shell: 'bg-gradient-to-br from-emerald-50 via-white to-sky-50 border-emerald-100',
                button: '#059669'
            } : {
                icon: 'bg-orange-100 text-orange-600',
                label: 'text-orange-600',
                shell: 'bg-gradient-to-br from-orange-50 via-white to-sky-50 border-orange-100',
                button: '#ea580c'
            };
            const ikon = isMasuk ? 'fa-person-walking-arrow-right' : 'fa-house-chimney-heart';
            const judul = isMasuk ? 'Masuk Tercatat!' : 'Pulang Tercatat!';
            const sub = isMasuk ? 'Selamat memulai pelayanan hari ini' : 'Terima kasih atas dedikasi hari ini';
            const jamKerjaFinal = res.jamKerja || res.totalJamKerja || '-';
            const jamInfo = isMasuk ? '' : `
                <div class="mt-4 rounded-2xl border border-orange-200 bg-gradient-to-r from-orange-50 via-amber-50 to-yellow-50 p-4 shadow-sm">
                    <p class="text-[10px] uppercase tracking-[0.2em] font-black text-orange-700 mb-2">Total Jam Kerja Hari Ini</p>
                    <div class="flex items-center justify-center gap-2">
                        <span class="w-10 h-10 rounded-xl bg-orange-100 text-orange-600 flex items-center justify-center text-lg"><i class="fa-solid fa-business-time"></i></span>
                        <span class="text-2xl md:text-3xl font-black text-orange-700 leading-none">${jamKerjaFinal}</span>
                    </div>
                </div>`;
            const lokasiInfo = `<div class="mt-4 grid grid-cols-1 gap-2 text-left text-xs font-bold"><div class="bg-slate-50 border border-slate-100 rounded-2xl p-3"><span class="text-slate-400 uppercase text-[10px]">Lokasi Tugas Induk</span><br><span class="text-slate-700">${res.lokasiInduk || '-'}</span></div><div class="bg-slate-50 border border-slate-100 rounded-2xl p-3"><span class="text-slate-400 uppercase text-[10px]">Tagging Lokasi</span><br><span class="text-slate-700">${res.lokasiTag || '-'}</span></div></div>`;
            Swal.fire({
                icon: 'success',
                title: judul,
                html: `
                    <div class="relative overflow-hidden rounded-[1.5rem] ${tone.shell} border p-5 text-center">
                        <div class="mx-auto w-16 h-16 rounded-3xl ${tone.icon} flex items-center justify-center text-3xl shadow-inner mb-3"><i class="fa-solid ${ikon}"></i></div>
                        <p class="text-[10px] font-black uppercase tracking-[0.25em] ${tone.label} mb-2">${sub}</p>
                        <div class="text-4xl font-black font-mono text-slate-900 tracking-tight">${res.waktu || '-'}</div>
                        ${jamInfo}
                        <p class="mt-4 text-sm font-bold text-slate-700 leading-relaxed">${res.message || 'Absensi berhasil disimpan.'}</p>
                        ${lokasiInfo}
                    </div>`,
                confirmButtonText: 'Mantap, Terima Kasih',
                confirmButtonColor: tone.button
            });
        }

        function prosesKeBackend(decodedText, actionType, onFinish = null) {
            let actionText = actionType === 'MASUK' ? 'Check-In' : 'Check-Out';
            Swal.fire({ title: `Memproses ${actionText}...`, allowOutsideClick: false, didOpen: () => Swal.showLoading() });

            google.script.run.withSuccessHandler(function(res) {
                if(res.status === 'SUCCESS_MASUK') { tampilkanAlertBerhasilAbsen(res, 'MASUK'); }
                else if (res.status === 'SUCCESS_PULANG') { tampilkanAlertBerhasilAbsen(res, 'PULANG'); }
                else if (res.status === 'OUT_OF_RADIUS') {
                    Swal.fire({ icon: 'error', title: 'Di Luar Area Resmi', text: res.message || 'Scan ditolak karena berada di luar lokasi Puskesmas/Polindes.', confirmButtonColor: '#dc2626' });
                }
                else if (res.status === 'NEED_PREVIOUS_PULANG') {
                    tampilkanDialogPulangSebelumnya(res, decodedText, actionType, onFinish);
                    return;
                }
                else { Swal.fire({ icon: 'error', title: 'Gagal', text: res.message }); }
                if (onFinish) onFinish();
            }).withFailureHandler(function(err) { Swal.fire('Error', 'Jaringan lambat.', 'error'); if (onFinish) onFinish(); })
            .prosesAbsen(decodedText, currentUserLat, currentUserLng, actionType, currentUserAcc || 0);
        }

        function tampilkanDialogPulangSebelumnya(res, decodedText, actionType, onFinish = null) {
            Swal.fire({
                title: 'Lengkapi Absen Pulang Sebelumnya',
                html: `
                    <div class="text-left p-4 rounded-2xl bg-gradient-to-br from-amber-50 to-white border border-amber-100">
                        <div class="flex items-center gap-3 mb-3">
                            <div class="w-11 h-11 rounded-xl bg-amber-100 text-amber-700 flex items-center justify-center"><i class="fa-solid fa-clock-rotate-left"></i></div>
                            <div>
                                <p class="text-[10px] font-extrabold text-amber-600 uppercase tracking-wider">Absen pulang tertunda</p>
                                <p class="text-sm font-extrabold text-gray-800">${res.nama || '-'}</p>
                            </div>
                        </div>
                        <p class="text-xs text-gray-600 mb-3 leading-relaxed">${res.message || ''}<br>Isi jam pulang aktual terlebih dahulu. Setelah tersimpan, sistem akan melanjutkan proses absen hari ini.</p>
                        <div class="grid grid-cols-2 gap-2 mb-3 text-xs">
                            <div class="bg-white border border-amber-100 rounded-xl p-2"><b>Tanggal</b><br>${res.tanggal || '-'}</div>
                            <div class="bg-white border border-amber-100 rounded-xl p-2"><b>Jam Masuk</b><br>${res.waktuMasuk || '-'}</div>
                        </div>
                        <label class="block text-[11px] font-bold text-gray-600 mb-1 uppercase">Jam Pulang Aktual</label>
                        <input type="time" id="swal-jam-pulang-prev" class="w-full px-4 py-3 bg-white border border-amber-200 rounded-xl text-lg font-extrabold text-amber-700 outline-none" value="17:00">
                    </div>
                `,
                showCancelButton: true,
                confirmButtonText: 'Simpan & Lanjutkan',
                cancelButtonText: 'Batal',
                confirmButtonColor: '#d97706',
                preConfirm: () => {
                    const jam = document.getElementById('swal-jam-pulang-prev').value;
                    if (!jam) { Swal.showValidationMessage('Jam pulang wajib diisi.'); return false; }
                    return jam;
                }
            }).then(result => {
                if (!result.isConfirmed) { if (onFinish) onFinish(); return; }
                Swal.fire({ title: 'Menyimpan jam pulang...', allowOutsideClick: false, didOpen: () => Swal.showLoading() });
                google.script.run.withSuccessHandler(r => {
                    if (!r.status) { Swal.fire('Gagal', r.message, 'error'); if (onFinish) onFinish(); return; }
                    if (actionType === 'MASUK') {
                        Swal.fire({ title: 'Berhasil', text: 'Jam pulang sebelumnya tersimpan. Melanjutkan absen masuk...', icon: 'success', timer: 1200, showConfirmButton: false });
                        setTimeout(() => prosesKeBackend(decodedText, 'MASUK', onFinish), 1200);
                    } else {
                        Swal.fire('Berhasil', `${r.message}. Silakan lakukan Live Scan MASUK untuk absen hari ini.`, 'success');
                        if (onFinish) onFinish();
                    }
                }).withFailureHandler(() => { Swal.fire('Error', 'Gagal menyimpan jam pulang sebelumnya.', 'error'); if (onFinish) onFinish(); })
                .setPulangHariSebelumnya(res.idQR, res.tanggal, result.value);
            });
        }

        // 3. Login Admin
        function togglePassword() {
            let p = document.getElementById("logPass"); let icon = document.getElementById("eyeIcon");
            if (p.type === "password") { p.type = "text"; icon.classList.replace("fa-eye", "fa-eye-slash"); }
            else { p.type = "password"; icon.classList.replace("fa-eye-slash", "fa-eye"); }
        }

        function doLogin() {
            let u = document.getElementById('logUser').value, p = document.getElementById('logPass').value;
            if(!u || !p) return Swal.fire('Perhatian', 'Isi Username & Password.', 'warning');
            Swal.fire({title: 'Memverifikasi...', didOpen: () => Swal.showLoading()});
            google.script.run.withSuccessHandler(res => {
                if(res.status) { localStorage.setItem('pusk_saved_user', u); Swal.close(); isAdminLoggedIn = true; navTo('dashView'); document.getElementById('adminMenu').style.display = 'flex'; }
                else Swal.fire('Ditolak', res.message, 'error');
            }).cekLogin(u, p);
        }

        function logoutAdmin() {
            isAdminLoggedIn = false; document.getElementById('adminMenu').style.display = 'none';
            document.getElementById('logPass').value = '';
            navTo('scanView'); Swal.fire({icon:'success', title:'Logout Berhasil', toast:true, position:'top-end', timer:2000, showConfirmButton:false});
        }

        function formatTanggalLiburIndo(ymd) {
            const parts = String(ymd || '').split('-');
            if (parts.length !== 3) return escapeHtml(ymd || '-');
            const bulan = ['Januari','Februari','Maret','April','Mei','Juni','Juli','Agustus','September','Oktober','November','Desember'];
            const idx = parseInt(parts[1], 10) - 1;
            return `${String(parseInt(parts[2], 10)).padStart(2, '0')} ${bulan[idx] || parts[1]} ${parts[0]}`;
        }

        function hitungHariKerjaOtomatis(manualMode = false) {
            const blnEl = document.getElementById('lapBulan');
            const thnEl = document.getElementById('lapTahun');
            const bln = blnEl ? blnEl.value : '';
            const thn = thnEl ? thnEl.value : '';
            if (!bln || !thn) return;
            const summary = document.getElementById('liburSummary');
            const list = document.getElementById('liburDetailList');
            const badge = document.getElementById('liburAiBadge');
            const inputLibur = document.getElementById('lapLiburManual');
            const applyResult = (res, keepManual) => {
                res = res || {};
                window.liburKerjaCache = res;
                const aiCount = parseInt(res.totalLiburNasionalWeekday || 0, 10) || 0;
                if (!keepManual && inputLibur) inputLibur.value = aiCount;
                const totalLiburInput = parseInt((inputLibur && inputLibur.value) || aiCount || 0, 10) || 0;
                const weekday = parseInt(res.totalWeekdays || 0, 10) || 0;
                const targetFinal = Math.max(0, weekday - totalLiburInput);
                document.getElementById('lapHariKerja').value = targetFinal;
                if (badge) badge.innerHTML = `<span class="w-2 h-2 rounded-full bg-emerald-400 animate-pulse"></span>${escapeHtml(res.sumber || 'Database Libur')} • ${aiCount} hari libur kerja`;
                if (summary) summary.innerHTML = `Hari kerja dasar: <b>${weekday}</b> • Database libur: <b>${aiCount}</b> • Total libur dipakai: <b>${totalLiburInput}</b> • Target final: <b>${targetFinal}</b><br><span class="text-[10px] text-blue-600 font-black uppercase">${escapeHtml(res.rekomendasiAI || 'Database libur membaca data nasional, nilai tetap bisa diedit manual.')}</span>`;
                if (list) {
                    const items = res.detailLibur || [];
                    list.innerHTML = items.length ? items.map((h, i) => `<div class="holiday-card-modern rounded-2xl p-3 flex items-start gap-3" style="animation-delay:${Math.min(i * 45, 360)}ms"><div class="holiday-date-pill shrink-0 rounded-2xl px-3 py-2 text-center text-white min-w-[92px]"><p class="text-[10px] font-black uppercase tracking-wider opacity-80">Tanggal</p><p class="text-xs font-black leading-tight">${formatTanggalLiburIndo(h.tgl)}</p></div><div class="relative z-10 min-w-0"><p class="text-[10px] font-black uppercase tracking-[0.18em] text-orange-500 mb-1"><i class="fa-solid fa-calendar-check mr-1"></i>Database Libur</p><p class="text-sm text-slate-800 font-extrabold leading-snug break-words">${escapeHtml(h.nama)}</p></div></div>`).join('') : '<div class="rounded-2xl border border-emerald-100 bg-white px-4 py-3 font-bold text-emerald-700 shadow-sm"><i class="fa-solid fa-circle-check mr-1"></i>Tidak ada libur nasional pada hari kerja bulan ini.</div>';
                }
            };

            if (manualMode && window.liburKerjaCache) {
                applyResult(window.liburKerjaCache, true);
                return;
            }

            if (summary) summary.innerText = 'Membaca database libur untuk bulan pilihan...';
            if (list) list.innerHTML = '';
            if (badge) badge.innerHTML = `<span class="w-2 h-2 rounded-full bg-blue-400 animate-pulse"></span>Membaca database libur...`;
            google.script.run.withSuccessHandler(res => applyResult(res, manualMode)).withFailureHandler(() => {
                if (summary) summary.innerText = 'Gagal membaca database libur. Silakan isi total libur secara manual.';
                if (list) list.innerHTML = '<div class="rounded-xl border border-red-100 bg-white px-3 py-2 font-bold text-red-600">Database libur gagal dibaca. Input libur manual tetap dapat digunakan.</div>';
                if (badge) badge.innerHTML = `<span class="w-2 h-2 rounded-full bg-red-400"></span>Mode manual aktif`;
            }).getKalenderKerja(bln, thn);
        }

        function renderJarakBadge(jarakInfo) {
            const m = String(jarakInfo || '').match(/(\d+)\s*m/i);
            if (!m) return `<span class="inline-flex mt-1 px-2 py-0.5 rounded-full text-[10px] font-bold bg-gray-100 text-gray-600">Jarak: -</span>`;
            const d = parseInt(m[1],10);
            if (d <= 100) return `<span class="inline-flex mt-1 px-2 py-0.5 rounded-full text-[10px] font-bold bg-green-100 text-green-700">Jarak ${d}m (Dekat)</span>`;
            if (d <= 300) return `<span class="inline-flex mt-1 px-2 py-0.5 rounded-full text-[10px] font-bold bg-orange-100 text-orange-700">Jarak ${d}m (Sedang)</span>`;
            return `<span class="inline-flex mt-1 px-2 py-0.5 rounded-full text-[10px] font-bold bg-red-100 text-red-700">Jarak ${d}m (Jauh)</span>`;
        }

        function tampilkanJarakHarian(targetId, cacheKey) {
            const target = document.getElementById(targetId);
            if (!target) return;
            const jarakInfo = (window.jarakHarianCache || {})[cacheKey] || '';
            target.innerHTML = `${renderJarakBadge(jarakInfo)} <button type="button" onclick="tutupJarakHarian('${targetId}', '${cacheKey}')" class="distance-reveal-btn inline-flex mt-1 ml-1 items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-extrabold bg-white text-slate-500 border border-slate-200 hover:bg-slate-50"><i class="fa-solid fa-xmark"></i> Tutup</button>`;
        }

        function tutupJarakHarian(targetId, cacheKey) {
            const target = document.getElementById(targetId);
            if (!target) return;
            target.innerHTML = renderTombolJarakHarian(targetId, cacheKey);
        }

        function renderTombolJarakHarian(targetId, cacheKey) {
            return `<button type="button" onclick="tampilkanJarakHarian('${targetId}', '${cacheKey}')" class="distance-reveal-btn inline-flex mt-1 items-center gap-1 px-2.5 py-1 rounded-full text-[10px] font-extrabold bg-slate-50 text-slate-600 border border-slate-200 hover:bg-slate-100"><i class="fa-solid fa-location-dot"></i> Lihat Jarak</button>`;
        }

        // ==========================================
        // FITUR PAGINASI GLOBAL
        // ==========================================
        function drawPagination(containerId, totalPages, currentPg, funcName) {
            let html = '';
            if(totalPages <= 1) { document.getElementById(containerId).innerHTML = ''; return; }

            html += `<button onclick="${funcName}(${currentPg - 1})" class="px-2 py-1 border border-gray-200 rounded text-gray-600 bg-white hover:bg-gray-50 ${currentPg === 1 ? 'opacity-50 pointer-events-none' : ''}"><i class="fa-solid fa-chevron-left"></i></button>`;

            let startP = Math.max(1, currentPg - 2);
            let endP = Math.min(totalPages, currentPg + 2);
            if(startP > 1) html += `<button onclick="${funcName}(1)" class="px-3 py-1 border border-gray-200 rounded text-gray-600 bg-white hover:bg-gray-50">1</button><span class="px-1 text-gray-400">...</span>`;

            for(let i=startP; i<=endP; i++) {
                if(i === currentPg) html += `<button class="px-3 py-1 border border-pusk-blue rounded bg-pusk-blue text-white font-bold">${i}</button>`;
                else html += `<button onclick="${funcName}(${i})" class="px-3 py-1 border border-gray-200 rounded text-gray-600 bg-white hover:bg-gray-50">${i}</button>`;
            }

            if(endP < totalPages) html += `<span class="px-1 text-gray-400">...</span><button onclick="${funcName}(${totalPages})" class="px-3 py-1 border border-gray-200 rounded text-gray-600 bg-white hover:bg-gray-50">${totalPages}</button>`;

            html += `<button onclick="${funcName}(${currentPg + 1})" class="px-2 py-1 border border-gray-200 rounded text-gray-600 bg-white hover:bg-gray-50 ${currentPg === totalPages ? 'opacity-50 pointer-events-none' : ''}"><i class="fa-solid fa-chevron-right"></i></button>`;

            document.getElementById(containerId).innerHTML = html;
        }

        // ==========================================
        // ABSENSI HARIAN (MONITORING, PAGINASI & MANUAL)
        // ==========================================
        function loadAbsenHarian() {
            const tgl = document.getElementById('filterTglHarian').value;
            if(!tgl) return;
            document.getElementById('tbHarian').innerHTML = `<tr><td colspan="6" class="text-center py-10"><i class="fa-solid fa-spinner fa-spin text-pusk-blue fa-2x"></i></td></tr>`;

            google.script.run.withSuccessHandler(function(res) {
                res = res || {};
                dataAbsenHarianRaw = Array.isArray(res.data) ? res.data : [];
                window.kapusParams = { puskesmas: res.namaPuskesmas || '', nama: res.kapusNama || '', nip: res.kapusNip || '' };
                renderTableHarian(true);
            }).withFailureHandler(function(err) {
                dataAbsenHarianRaw = [];
                document.getElementById('tbHarian').innerHTML = `<tr><td colspan="6" class="text-center py-10 text-red-500 font-bold"><i class="fa-solid fa-triangle-exclamation mr-2"></i>Gagal memuat data harian. Klik reload untuk mencoba lagi.</td></tr>`;
                Swal.fire('Gagal Memuat', (err && err.message) ? err.message : 'Koneksi lambat atau server Apps Script belum merespons.', 'error');
            }).getAbsenHarian(tgl);
        }

        function gantiHalHarian(pg) { currentPgHarian = pg; renderTableHarian(false); }

        function escapeHtml(value) {
            return String(value || '')
                .replace(/&/g, '&amp;')
                .replace(/</g, '&lt;')
                .replace(/>/g, '&gt;')
                .replace(/"/g, '&quot;')
                .replace(/'/g, '&#039;');
        }

        function renderCatatanBadge(alasan, status) {
            const note = String(alasan || '').trim();
            if(!note) {
                return `<span class="note-chip-modern note-chip-empty rounded-2xl border border-slate-200 bg-slate-50 px-3 py-2 text-[11px] font-bold text-slate-400"><i class="fa-regular fa-circle-dot"></i><span>Tidak ada catatan</span></span>`;
            }

            const ket = String(status || '').toUpperCase();
            let tone = 'border-sky-200 bg-sky-50 text-sky-700';
            let icon = 'fa-solid fa-note-sticky';
            if(ket.includes('ALPA')) { tone = 'border-rose-200 bg-rose-50 text-rose-700'; icon = 'fa-solid fa-triangle-exclamation'; }
            else if(ket.includes('CUTI')) { tone = 'border-purple-200 bg-purple-50 text-purple-700'; icon = 'fa-solid fa-umbrella-beach'; }
            else if(ket.includes('SAKIT')) { tone = 'border-blue-200 bg-blue-50 text-blue-700'; icon = 'fa-solid fa-notes-medical'; }
            else if(ket.includes('IZIN')) { tone = 'border-amber-200 bg-amber-50 text-amber-700'; icon = 'fa-solid fa-file-signature'; }
            else if(ket.includes('TL') || ket.includes('TUGAS')) { tone = 'border-indigo-200 bg-indigo-50 text-indigo-700'; icon = 'fa-solid fa-briefcase'; }

            const safeNote = escapeHtml(note);
            return `<span class="note-chip-modern rounded-2xl border ${tone} px-3 py-2 text-[11px] font-extrabold shadow-sm shadow-slate-100" title="${safeNote}"><i class="${icon}"></i><span>${safeNote}</span></span>`;
        }

        function renderTableHarian(resetPg = false) {
            if(resetPg) currentPgHarian = 1;
            const filterKet = document.getElementById('filterKetHarian').value;
            const searchVal = document.getElementById('searchHarian').value.toLowerCase();

            filteredHarian = dataAbsenHarianRaw;
            if(filterKet !== "ALL") filteredHarian = filteredHarian.filter(d => String(d.keterangan || '').toUpperCase() === filterKet.toUpperCase());
            if(searchVal) filteredHarian = filteredHarian.filter(d => String(d.nama || '').toLowerCase().includes(searchVal) || String(d.jabatan || '').toLowerCase().includes(searchVal));

            // Stats
           // ====================== STATS BARU (SUDAH HADIR BENAR) ======================
document.getElementById('statTotal').innerText = dataAbsenHarianRaw.length;

let cHadirMasuk = 0;     // TOTAL yang sudah absen MASUK (termasuk belum pulang)
let cBelumAbsen   = 0;   // Belum absen masuk sama sekali
let cBelumPulang  = 0;   // Sudah masuk tapi belum pulang
let cLain         = 0;   // Izin / Sakit / TL / Alpa dll.

dataAbsenHarianRaw.forEach(d => {
    const ket = d.keterangan || '';

    if (ket === 'HADIR' || ket === 'BELUM ABSEN PULANG') {
        cHadirMasuk++;                  // Semua yang sudah absen masuk
        if (ket === 'BELUM ABSEN PULANG') cBelumPulang++;
    }
    else if (ket === 'BELUM ABSEN') {
        cBelumAbsen++;
    }
    else if (ket !== 'ALPA') {
        cLain++;                        // Izin, Sakit, TL, dll.
    }
});

document.getElementById('statHadir').innerText      = cHadirMasuk;   // ← INI YANG DIPERBAIKI
document.getElementById('statBelum').innerText      = cBelumAbsen;
document.getElementById('statBelumPulang').innerText = cBelumPulang;
document.getElementById('statLain').innerText       = cLain;

            // Pagination Math
            let tRows = filteredHarian.length;
            let tPages = rowsPerPageHarian === 'ALL' ? 1 : Math.ceil(tRows / rowsPerPageHarian);
            let sIdx = rowsPerPageHarian === 'ALL' ? 0 : (currentPgHarian - 1) * rowsPerPageHarian;
            let eIdx = rowsPerPageHarian === 'ALL' ? tRows : sIdx + parseInt(rowsPerPageHarian);
            let pagedData = filteredHarian.slice(sIdx, eIdx);

            let html = '';
            if(tRows === 0) { html = `<tr><td colspan="6" class="text-center py-10 text-gray-500 font-medium">Tidak ada data.</td></tr>`; }
            else {
                window.jarakHarianCache = {};
                pagedData.forEach((d, idx) => {
                   let badgeClass = "bg-gray-100 text-gray-600";
if(d.keterangan === "HADIR") badgeClass = "bg-green-50 text-green-700 border border-green-200";
else if(d.keterangan === "BELUM ABSEN PULANG") badgeClass = "bg-amber-100 text-amber-700 border border-amber-200 font-medium";
else if(d.keterangan === "SAKIT" || d.keterangan === "IZIN" || d.keterangan === "TL" || d.keterangan === "TUGAS LUAR") badgeClass = "bg-blue-50 text-blue-700 border border-blue-200";
else if(d.keterangan === "CUTI") badgeClass = "bg-purple-50 text-purple-700 border border-purple-200";
else if(d.keterangan === "ALPA") badgeClass = "bg-red-50 text-red-700 border border-red-200";
                    let eIdQr = d.id_qr || '';
                    let eNama = (d.nama || '').replace(/'/g, "\\'").replace(/"/g, "&quot;");
                    const safeRowKey = String(eIdQr || d.nama || idx).replace(/[^a-zA-Z0-9_-]/g, '-');
                    const jarakCacheKey = `jarak-${currentPgHarian}-${idx}-${safeRowKey}`;
                    const jarakMasukTarget = `${jarakCacheKey}-masuk`;
                    const jarakPulangTarget = `${jarakCacheKey}-pulang`;
                    window.jarakHarianCache[jarakCacheKey] = d.jarakInfo || '';

                   html += `<tr class="hover:bg-gray-50 transition border-b border-gray-100">
    <td class="px-4 py-3"><div class="font-extrabold text-gray-800 text-[12px]">${d.nama}</div><div class="text-[10px] text-gray-400 uppercase tracking-wider">${d.jabatan}</div></td>

    <!-- JAM MASUK - BADGE HIJAU MODERN -->
    <td class="px-4 py-3 text-center">
        <span class="inline-flex items-center px-3 py-1 text-xs font-mono font-bold rounded-2xl bg-emerald-100 text-emerald-700 border border-emerald-200 shadow-inner">
            ${d.wktMasuk || '-'}
        </span>
        <div id="${jarakMasukTarget}">${renderTombolJarakHarian(jarakMasukTarget, jarakCacheKey)}</div>
    </td>

    <!-- JAM PULANG - BADGE ORANGE MODERN -->
    <td class="px-4 py-3 text-center">
        <span class="inline-flex items-center px-3 py-1 text-xs font-mono font-bold rounded-2xl bg-orange-100 text-orange-700 border border-orange-200 shadow-inner">
            ${d.wktPulang || '-'}
        </span>
        <div id="${jarakPulangTarget}">${renderTombolJarakHarian(jarakPulangTarget, jarakCacheKey)}</div>
    </td>

    <td class="px-4 py-3 text-center">
        <span class="px-3 py-1 rounded-2xl text-[10px] font-bold uppercase ${badgeClass}">${d.keterangan}</span>
    </td>
    <td class="px-4 py-3 align-top min-w-[180px]">${renderCatatanBadge(d.alasan, d.keterangan)}</td>
    <td class="px-4 py-3 text-center"><button onclick="bukaModalSetManual('${eIdQr}', '${eNama}')" class="bg-indigo-50 text-indigo-600 hover:bg-indigo-100 px-2 py-1 rounded-lg text-[10px] font-bold shadow-sm"><i class="fa-solid fa-pen"></i> Set</button></td>
</tr>`;
                });
            }
            document.getElementById('tbHarian').innerHTML = html;

            // Render Paginasi Info & Button
            document.getElementById('infoPageHarian').innerText = tRows === 0 ? `0 dari 0` : `${sIdx + 1}-${Math.min(eIdx, tRows)} dari ${tRows}`;
            drawPagination('pagControlsHarian', tPages, currentPgHarian, 'gantiHalHarian');
        }

        // FUNGSI ADMIN SET MANUAL ABSEN
        function bukaModalSetManual(idQR, nama) {
            const tgl = document.getElementById('filterTglHarian').value;
            Swal.fire({
                title: 'Set Status Manual',
                html: `
                    <div class="text-left mb-3"><p class="text-xs font-bold text-gray-500 uppercase">Pegawai</p><p class="text-sm font-extrabold text-pusk-blue">${nama}</p></div>
                    <div class="text-left">
                        <label class="block text-xs font-bold text-gray-600 mb-1 ml-1">Pilih Status Baru</label>
                        <select id="swal-manual-status" class="swal2-select w-full mb-3 !mx-0 !mt-0" onchange="toggleManualFields(this.value)">
                            <option value="HADIR_MASUK">Hadir (Set Jam Masuk Saat Ini)</option><option value="HADIR_PULANG">Hadir (Set Jam Pulang Saat Ini)</option>
                            <option value="SAKIT">Sakit</option><option value="IZIN">Izin</option><option value="TL">Tugas Luar (TL)</option><option value="CUTI">Cuti (Rentang Tanggal)</option><option value="ALPA">Alpa</option>
                        </select>
                        <div id="manual-alasan-container" style="display:none;"><label class="block text-xs font-bold text-gray-600 mb-1 ml-1">Catatan Tambahan</label><input type="text" id="swal-manual-alasan" class="swal2-input w-full !mx-0 !mt-0" placeholder="Masukkan keterangan..."></div>
                        <div id="manual-cuti-container" style="display:none;">
                            <label class="block text-xs font-bold text-purple-700 mb-1 ml-1 mt-2">Kalender Pilihan Tanggal Cuti</label>
                            <div class="rounded-2xl border border-purple-100 bg-gradient-to-br from-purple-50 to-white p-3 shadow-sm">
                                <div class="flex items-center justify-between mb-3">
                                    <button type="button" onclick="shiftCutiCalendarMonth(-1)" class="w-8 h-8 rounded-xl bg-white border border-purple-100 text-purple-700 font-extrabold hover:bg-purple-100"><i class="fa-solid fa-chevron-left"></i></button>
                                    <div id="cuti-calendar-title" class="text-sm font-extrabold text-purple-800"></div>
                                    <button type="button" onclick="shiftCutiCalendarMonth(1)" class="w-8 h-8 rounded-xl bg-white border border-purple-100 text-purple-700 font-extrabold hover:bg-purple-100"><i class="fa-solid fa-chevron-right"></i></button>
                                </div>
                                <div class="grid grid-cols-7 gap-1 text-center text-[10px] font-extrabold text-purple-500 uppercase mb-1">
                                    <div>Min</div><div>Sen</div><div>Sel</div><div>Rab</div><div>Kam</div><div>Jum</div><div>Sab</div>
                                </div>
                                <div id="cuti-calendar-grid" class="grid grid-cols-7 gap-1"></div>
                            </div>
                            <p class="text-[10px] text-purple-600 mt-2 ml-1 font-semibold"><i class="fa-solid fa-wand-magic-sparkles mr-1"></i> Centang tanggal langsung di kalender. Bisa pilih acak untuk melewati libur, jadwal lepas, atau tanggal yang tidak dihitung cuti.</p>
                            <div id="cuti-date-list" class="mt-2 flex flex-wrap gap-1"></div>
                            <div id="cuti-day-counter" class="mt-2 ml-1 inline-flex items-center gap-2 bg-gradient-to-r from-purple-50 to-fuchsia-50 text-purple-700 border border-purple-200 px-3 py-1.5 rounded-full text-[11px] font-extrabold">
                                <i class="fa-solid fa-calendar-days"></i>
                                <span id="cuti-day-counter-text">Total cuti: 0 hari</span>
                            </div>
                        </div>
                    </div>
                `,
                showCancelButton: true, confirmButtonText: 'Simpan Status', confirmButtonColor: '#003366', cancelButtonText: 'Batal',
                didOpen: () => {
                    window.selectedCutiDatesForManual = [tgl];
                    window.cutiCalendarCursor = new Date(tgl + 'T00:00:00');
                    renderCutiCalendar();
                    renderSelectedCutiDates();
                    toggleManualFields(document.getElementById('swal-manual-status').value);
                },
                preConfirm: () => {
                    const status = document.getElementById('swal-manual-status').value;
                    const alasan = document.getElementById('swal-manual-alasan').value;
                    const cutiDates = window.selectedCutiDatesForManual || [];
                    if (status === 'CUTI' && cutiDates.length === 0) {
                        Swal.showValidationMessage('Pilih minimal 1 tanggal cuti.'); return false;
                    }
                    return { status, alasan, cutiDates };
                }
            }).then(res => {
                if(res.isConfirmed) {
                    Swal.fire({title:'Menyimpan...', didOpen:()=>Swal.showLoading()});
                    if (res.value.status === 'CUTI') {
                        google.script.run.withSuccessHandler(r => { Swal.fire('Berhasil', r.message, 'success'); loadAbsenHarian(); })
                        .setCutiTanggalPilihanAdmin(idQR, res.value.cutiDates, res.value.alasan);
                    } else {
                        google.script.run.withSuccessHandler(r => { Swal.fire('Berhasil', r.message, 'success'); loadAbsenHarian(); })
                        .setManualAbsenAdmin(idQR, tgl, res.value.status, res.value.alasan);
                    }
                }
            });
        }

        function toggleManualFields(val) {
            const alasanEl = document.getElementById('manual-alasan-container');
            const cutiEl = document.getElementById('manual-cuti-container');
            const tampilAlasan = (val !== 'HADIR_MASUK' && val !== 'HADIR_PULANG' && val !== 'ALPA');
            if (alasanEl) alasanEl.style.display = tampilAlasan ? 'block' : 'none';
            if (cutiEl) cutiEl.style.display = val === 'CUTI' ? 'block' : 'none';
            if (val === 'CUTI') updateCutiDayCounter();
        }

        function formatCutiYmd(dateObj) {
            const y = dateObj.getFullYear();
            const m = String(dateObj.getMonth() + 1).padStart(2, '0');
            const d = String(dateObj.getDate()).padStart(2, '0');
            return `${y}-${m}-${d}`;
        }

        function shiftCutiCalendarMonth(delta) {
            const base = window.cutiCalendarCursor || new Date();
            window.cutiCalendarCursor = new Date(base.getFullYear(), base.getMonth() + delta, 1);
            renderCutiCalendar();
        }

        function toggleCutiCalendarDate(ymd) {
            const selected = new Set(window.selectedCutiDatesForManual || []);
            if (selected.has(ymd)) selected.delete(ymd); else selected.add(ymd);
            window.selectedCutiDatesForManual = Array.from(selected).sort();
            renderCutiCalendar();
            renderSelectedCutiDates();
        }

        function renderCutiCalendar() {
            const grid = document.getElementById('cuti-calendar-grid');
            const title = document.getElementById('cuti-calendar-title');
            if (!grid || !title) return;
            const monthNames = ['Januari','Februari','Maret','April','Mei','Juni','Juli','Agustus','September','Oktober','November','Desember'];
            const cursor = window.cutiCalendarCursor || new Date();
            const year = cursor.getFullYear();
            const month = cursor.getMonth();
            title.innerText = `${monthNames[month]} ${year}`;

            const first = new Date(year, month, 1);
            const last = new Date(year, month + 1, 0);
            const selected = new Set(window.selectedCutiDatesForManual || []);
            let html = '';
            for (let i = 0; i < first.getDay(); i++) html += '<div></div>';
            for (let day = 1; day <= last.getDate(); day++) {
                const d = new Date(year, month, day);
                const ymd = formatCutiYmd(d);
                const checked = selected.has(ymd);
                const isWeekend = d.getDay() === 0 || d.getDay() === 6;
                const cls = checked ? 'bg-purple-600 text-white border-purple-700 shadow-md scale-[1.03]' : (isWeekend ? 'bg-rose-50 text-rose-500 border-rose-100' : 'bg-white text-gray-700 border-purple-100 hover:bg-purple-50');
                const icon = checked ? '<i class="fa-solid fa-check text-[9px]"></i>' : '<span class="w-2 h-2 rounded border border-current opacity-50 inline-block"></span>';
                html += `<button type="button" onclick="toggleCutiCalendarDate('${ymd}')" class="relative h-10 rounded-xl border ${cls} transition flex flex-col items-center justify-center text-xs font-extrabold"><span>${day}</span><span class="absolute top-1 right-1">${icon}</span></button>`;
            }
            grid.innerHTML = html;
        }

        function removeCutiDate(value) {
            window.selectedCutiDatesForManual = (window.selectedCutiDatesForManual || []).filter(d => d !== value);
            renderCutiCalendar();
            renderSelectedCutiDates();
        }

        function renderSelectedCutiDates() {
            const list = document.getElementById('cuti-date-list');
            const textEl = document.getElementById('cuti-day-counter-text');
            const dates = window.selectedCutiDatesForManual || [];
            if (list) {
                list.innerHTML = dates.length ? dates.map(d => `<span class="inline-flex items-center gap-1 px-2 py-1 rounded-full bg-purple-100 text-purple-700 text-[10px] font-extrabold">${d}<button type="button" onclick="removeCutiDate('${d}')" class="text-red-500">×</button></span>`).join('') : '<span class="text-[10px] text-gray-400">Belum ada tanggal dipilih.</span>';
            }
            if (textEl) textEl.innerText = `Total cuti: ${dates.length} hari`;
        }

        function updateCutiDayCounter() { renderCutiCalendar(); renderSelectedCutiDates(); }

        function terapkanBulkAlpa() {
            const tgl = document.getElementById('filterTglHarian').value;
            Swal.fire({ title: 'Konfirmasi Bulk ALPA', html: "Tetapkan status <b>ALPA</b> pada semua pegawai yang belum absen?", icon: 'warning', showCancelButton: true, confirmButtonColor: '#d33', confirmButtonText: 'Ya'
            }).then((res) => { if (res.isConfirmed) { Swal.fire({title: 'Memproses...', didOpen:()=>Swal.showLoading()}); google.script.run.withSuccessHandler(r => { Swal.fire('Selesai', r.message, 'success'); loadAbsenHarian(); }).setBulkAlpa(tgl); } });
        }

        // ==========================================
        // KEGIATAN MANUAL
        // ==========================================
        function loadPegawaiKegiatan() {
            const tgl = document.getElementById('kegTgl').value;
            if(!tgl) return Swal.fire('Error', 'Pilih tanggal!', 'error');
            document.getElementById('listPegawaiKegiatan').innerHTML = `<p class="col-span-2 text-center py-4"><i class="fa-solid fa-spinner fa-spin mr-2"></i>Mencari pegawai...</p>`;
            google.script.run.withSuccessHandler(res => {
                dataPegawaiKegiatan = res.eligibleData; window.kapusParams = { puskesmas: res.namaPuskesmas, nama: res.kapusNama, nip: res.kapusNip };
                document.getElementById('kegCountTersedia').innerText = res.counts.tersedia; document.getElementById('kegCountTL').innerText = res.counts.tl;
                document.getElementById('kegCountSakit').innerText = res.counts.sakit; document.getElementById('kegCountIzin').innerText = res.counts.izin;
                document.getElementById('kegCountCuti').innerText = res.counts.cuti || 0;
                renderListKegiatan();
            }).getPegawaiEligibleKegiatan(tgl);
        }

        function renderListKegiatan() {
            let html = '';
            let searchVal = document.getElementById('searchKegiatan').value.toLowerCase();
            let filteredPegawai = dataPegawaiKegiatan;

            if(searchVal) {
                filteredPegawai = filteredPegawai.filter(p => p.nama.toLowerCase().includes(searchVal) || p.jabatan.toLowerCase().includes(searchVal));
            }

            if(dataPegawaiKegiatan.length === 0) {
                html = `<p class="col-span-2 text-center py-4 text-red-500 font-bold">Tidak ada pegawai tersedia.</p>`;
            } else if (filteredPegawai.length === 0) {
                html = `<p class="col-span-2 text-center py-4 text-gray-500 font-medium">Pegawai tidak ditemukan.</p>`;
            } else {
                filteredPegawai.forEach((p) => {
                    let origIdx = dataPegawaiKegiatan.findIndex(orig => orig.id_qr === p.id_qr);
                    html += `<label class="flex items-center gap-3 p-3 border border-gray-100 rounded-xl hover:bg-indigo-50 cursor-pointer transition"><input type="checkbox" class="keg-check custom-checkbox" value="${origIdx}"><div><p class="text-sm font-bold text-gray-800">${p.nama}</p><p class="text-[10px] text-gray-500">${p.jabatan}</p></div></label>`;
                });
            }
            document.getElementById('listPegawaiKegiatan').innerHTML = html; document.getElementById('checkAllKeg').checked = false;
        }

        function toggleAllKeg(el) { document.querySelectorAll('.keg-check').forEach(c => c.checked = el.checked); }

        function cetakPDFKegiatan() {
            const tgl = document.getElementById('kegTgl').value, namaKeg = document.getElementById('kegNama').value;
            if(!tgl || !namaKeg) return Swal.fire('Error', 'Tanggal dan Nama wajib diisi!', 'warning');
            let selectedIdx = []; document.querySelectorAll('.keg-check:checked').forEach(c => selectedIdx.push(c.value));
            if(selectedIdx.length === 0) return Swal.fire('Error', 'Pilih minimal 1 pegawai!', 'warning');

            let selectedPegawai = selectedIdx.map(i => dataPegawaiKegiatan[i]);
            Swal.fire({title: 'Memproses...', allowOutsideClick: false, didOpen: () => Swal.showLoading()});

            google.script.run.withSuccessHandler(function(res) { generatePDFDaftarHadir(tgl, namaKeg, selectedPegawai); }).simpanKegiatanManual(tgl, namaKeg, selectedPegawai);
        }

    function generatePDFDaftarHadir(tgl, namaKeg, list) {
    const { jsPDF } = window.jspdf;
    const doc = new jsPDF({ orientation: "portrait" });
    const pageWidth = doc.internal.pageSize.width;

    // Format Tanggal Profesional
    const monthNames = ["Januari", "Februari", "Maret", "April", "Mei", "Juni", "Juli", "Agustus", "September", "Oktober", "November", "Desember"];
    const dayNames = ["Minggu", "Senin", "Selasa", "Rabu", "Kamis", "Jumat", "Sabtu"];
    let parts = tgl.split('-');
    let d = new Date(parts[0], parseInt(parts[1])-1, parts[2]);
    let tglFormatted = `${dayNames[d.getDay()]}, ${parseInt(parts[2])} ${monthNames[parseInt(parts[1])-1]} ${parts[0]}`;

    // ==================== HEADER ====================
    doc.setFontSize(13);
    doc.setFont("helvetica", "bold");
    doc.text("DAFTAR HADIR KEGIATAN", pageWidth/2, 18, { align: 'center' });

    // Tabel kecil Nama Kegiatan & Tanggal (titik dua sejajar)
    doc.autoTable({
        startY: 28,
        body: [
            ['Nama Kegiatan', ':', namaKeg.toUpperCase()],
            ['Tanggal',       ':', tglFormatted]
        ],
        theme: 'plain',
        styles: { fontSize: 10, cellPadding: 1.5, textColor: [0, 0, 0] },
        columnStyles: {
            0: { cellWidth: 32, fontStyle: 'normal' },
            1: { cellWidth: 5, halign: 'center' },
            2: { fontStyle: 'normal' }
        },
        margin: { left: 14 }
    });

    // ==================== TABEL UTAMA (PEPET + FONT LEBIH KECIL) ====================
    let tableData = [];
    list.forEach((row, index) => {
        let nipText = row.nip ? `\n${row.nip}` : "";
        let golText = row.golongan && row.golongan !== "-" ? `\n${row.golongan}` : "";
        let namaGabung = row.nama + golText + nipText;

        let sig = (index % 2 === 0)
            ? `${index+1}. ......................`
            : `                  ${index+1}. ......................`;

        tableData.push([
            index + 1,
            namaGabung,
            row.jabatan,
            sig
        ]);
    });

    doc.autoTable({
        startY: doc.lastAutoTable.finalY + 4,   // lebih rapat ke atas
        head: [['NO', 'NAMA / PANGKAT / GOL / NIP', 'JABATAN', 'TANDA TANGAN']],
        body: tableData,
        theme: 'grid',
        headStyles: {
            fillColor: [255, 255, 255],
            textColor: [0, 0, 0],
            lineColor: [0, 0, 0],
            lineWidth: 0.1,
            fontStyle: 'bold',
            halign: 'center',
            minCellHeight: 11
        },
        styles: {
            fontSize: 8.5,        // font dikecilkan lagi
            cellPadding: 2.5,     // padding lebih pepet
            valign: 'middle',
            lineColor: [0, 0, 0],
            lineWidth: 0.1,
            textColor: [0, 0, 0]
        },
        columnStyles: {
            0: { halign: 'center', cellWidth: 13 },
            1: { cellWidth: 80 },
            2: { cellWidth: 40 },
            3: { cellWidth: 48 }                         // TTD dikecilkan (tidak mentok di samping)
        }
    });

    // ==================== BAGIAN MENGETAHUI (LEBIH RAPAT) ====================
    const finalY = doc.lastAutoTable.finalY || 45;
    let rightX = pageWidth - 48;

    doc.setFontSize(8.5);
    doc.setFont("helvetica", "normal");
    doc.text(`Sintang, ${tglFormatted.split(',')[1].trim()}`, rightX, finalY + 12, { align: 'center' });
    doc.text(`Mengetahui,`, rightX, finalY + 18, { align: 'center' });

    doc.setFont("helvetica", "bold");
    let strPrefix = (window.kapusParams.puskesmas || "PUSKESMAS").toUpperCase().replace("UPTD ", "");
    doc.text(`KEPALA ${strPrefix}`, rightX, finalY + 24, { align: 'center' });

    doc.text(window.kapusParams.nama, rightX, finalY + 38, { align: 'center' });
    doc.setLineWidth(0.4);
    doc.line(rightX - 38, finalY + 39, rightX + 38, finalY + 39);

    doc.setFont("helvetica", "normal");
    doc.setFontSize(7.5);
    doc.text(window.kapusParams.nip, rightX, finalY + 42, { align: 'center' });

    doc.save(`Absen_Kegiatan_${tgl}.pdf`);
    Swal.fire('Berhasil!', 'PDF Daftar Hadir sudah rapi, kolom TTD tidak mentok & tabel lebih pepet.', 'success');
}

        // ==========================================
        // KELOLA PEGAWAI (PAGINASI)
        // ==========================================
        function loadDataPegawai() {
            document.getElementById('tbPegawai').innerHTML = `<tr><td colspan="4" class="text-center py-10"><i class="fa-solid fa-spinner fa-spin text-pusk-blue fa-2x"></i></td></tr>`;
            google.script.run.withSuccessHandler(data => { listPegawaiRaw = data; renderTablePegawai(true);populateLokasiFilter(); }).getListPegawai();
        }


function populateLokasiFilter() {
    const select = document.getElementById('filterLokasi');
    const lokasiSet = new Set();

    listPegawaiRaw.forEach(p => {
        if (p[5] && p[5].toString().trim() !== '') {
            lokasiSet.add(p[5].toString().trim());
        }
    });

    select.innerHTML = '<option value="SEMUA">Semua Lokasi</option>';

    Array.from(lokasiSet).sort().forEach(lok => {
        let opt = document.createElement('option');
        opt.value = lok;
        opt.textContent = lok;
        select.appendChild(opt);
    });
}

        function gantiHalPegawai(pg) { currentPgPegawai = pg; renderTablePegawai(false); }

function renderTablePegawai(resetPg = false) {
    if(resetPg) currentPgPegawai = 1;

    const searchVal = document.getElementById('searchPegawai').value.toLowerCase().trim();
    const filterLok = document.getElementById('filterLokasi').value;

    filteredPegawai = listPegawaiRaw;

    // Filter Lokasi
    if (filterLok !== 'SEMUA') {
        filteredPegawai = filteredPegawai.filter(p => {
            let lokasi = (p[5] || '').toString().trim();
            return lokasi === filterLok;
        });
    }

    // Filter Search (Nama atau NIP)
    if(searchVal) {
        filteredPegawai = filteredPegawai.filter(p =>
            (p[1] || '').toString().toLowerCase().includes(searchVal) ||
            (p[2] || '').toString().toLowerCase().includes(searchVal)
        );
    }

    let tRows = filteredPegawai.length;
    let tPages = rowsPerPagePegawai === 'ALL' ? 1 : Math.ceil(tRows / rowsPerPagePegawai);
    let sIdx = rowsPerPagePegawai === 'ALL' ? 0 : (currentPgPegawai - 1) * rowsPerPagePegawai;
    let eIdx = rowsPerPagePegawai === 'ALL' ? tRows : sIdx + parseInt(rowsPerPagePegawai);
    let pagedData = filteredPegawai.slice(sIdx, eIdx);

    let html = '';
    if(tRows === 0) {
        html = `<tr><td colspan="4" class="text-center py-10 text-gray-500">Data tidak ditemukan</td></tr>`;
    }
    else {
        pagedData.forEach(p => {
            let golVal = p[6] && p[6] !== "-" ? p[6] : "-";
            let badgeLoc = p[5].includes("Polindes") ? 'bg-purple-50 text-purple-700' : 'bg-blue-50 text-blue-700';

            let eId = p[0] || '';
            let eNama = (p[1] || '').toString().replace(/'/g, "\\'").replace(/"/g, "&quot;");
            let eNip = (p[2] || '').toString().replace(/'/g, "\\'").replace(/"/g, "&quot;");
            let eJab = (p[3] || '').toString().replace(/'/g, "\\'").replace(/"/g, "&quot;");
            let eStat = (p[4] || '').toString().replace(/'/g, "\\'").replace(/"/g, "&quot;");
            let eLok = (p[5] || '').toString().replace(/'/g, "\\'").replace(/"/g, "&quot;");
            let eGol = (p[6] || '').toString().replace(/'/g, "\\'").replace(/"/g, "&quot;");
            let eKunci = (p[7] || 'IYA').toString().replace(/'/g, "\\'").replace(/"/g, "&quot;");

            html += `<tr class="hover:bg-gray-50 border-b border-gray-100">
                <td class="px-4 py-3"><div class="font-bold text-gray-800 text-[12px]">${p[1]}</div><div class="text-[10px] font-mono text-gray-500">${p[2] || '-'}</div></td>
                <td class="px-4 py-3"><div class="text-xs font-bold text-pusk-blue">${golVal}</div><span class="px-2 py-0.5 text-[9px] font-bold uppercase rounded bg-gray-100 text-gray-600">${p[4]}</span></td>
                <td class="px-4 py-3"><div class="text-[11px] text-gray-700 font-bold mb-1">${p[3]}</div><span class="px-2 py-1 text-[9px] font-bold uppercase rounded border ${badgeLoc}">${p[5]}</span></td>
                <td class="px-4 py-3 text-center"><div class="flex justify-center gap-1">
                    <button onclick="bukaModalEdit('${eId}','${eNama}','${eNip}','${eJab}','${eStat}','${eLok}', '${eGol}','${eKunci}')" class="text-white bg-yellow-500 hover:bg-yellow-600 px-2 py-1.5 rounded text-xs"><i class="fa-solid fa-pen"></i></button>
                    <button onclick="hapusPegawai('${eId}')" class="text-white bg-red-500 hover:bg-red-600 px-2 py-1.5 rounded text-xs"><i class="fa-solid fa-trash"></i></button>
                    <button onclick="previewCard('${eId}', '${eNama}', '${eNip}')" class="text-pusk-blue bg-blue-50 border border-blue-200 px-2 py-1.5 rounded text-xs font-bold"><i class="fa-solid fa-id-badge"></i></button>
                </div></td></tr>`;
        });
    }
    document.getElementById('tbPegawai').innerHTML = html;

    document.getElementById('infoPagePegawai').innerText = tRows === 0 ? `0 dari 0` : `${sIdx + 1}-${Math.min(eIdx, tRows)} dari ${tRows}`;
    drawPagination('pagControlsPegawai', tPages, currentPgPegawai, 'gantiHalPegawai');
}

        function bukaModalTambah() {
            document.getElementById('modalTitlePegawai').innerText = "Tambah Pegawai Baru"; document.getElementById('inIdQR').value = "";
            document.getElementById('inNama').value = ""; document.getElementById('inNip').value = ""; document.getElementById('inJabatan').value = ""; document.getElementById('inGolongan').value = "-"; document.getElementById('inKunci').value = "IYA";
            document.getElementById('modalTambahPegawai').classList.remove('hidden');
        }

        function bukaModalEdit(id, nama, nip, jabatan, status, lokasi, golongan, kunci) {
            document.getElementById('modalTitlePegawai').innerText = "Edit Data Pegawai";
            document.getElementById('inIdQR').value = id;
            document.getElementById('inNama').value = nama;
            document.getElementById('inNip').value = nip !== '-' ? nip : '';
            document.getElementById('inJabatan').value = jabatan;

            status = status ? status.trim() : "";
            lokasi = lokasi ? lokasi.trim() : "";
            golongan = golongan ? golongan.trim() : "";

            let statEl = document.getElementById('inStatus');
            let statOpts = Array.from(statEl.options).map(o => o.value);
            if(statOpts.includes(status)) statEl.value = status; else statEl.selectedIndex = 0;

            let lokEl = document.getElementById('inLokasi');
            let lokOpts = Array.from(lokEl.options).map(o => o.value);
            if(lokOpts.includes(lokasi)) lokEl.value = lokasi; else lokEl.selectedIndex = 0;

            let golEl = document.getElementById('inGolongan');
            let golOpts = Array.from(golEl.options).map(o => o.value);
            if(golOpts.includes(golongan)) golEl.value = golongan; else golEl.value = "-";
            document.getElementById('inKunci').value = (kunci || "IYA").toUpperCase() === "TIDAK" ? "TIDAK" : "IYA";

            document.getElementById('modalTambahPegawai').classList.remove('hidden');
        }

        function simpanDataPegawai() {
            let data = { idQR: document.getElementById('inIdQR').value, nama: document.getElementById('inNama').value, nip: document.getElementById('inNip').value, jabatan: document.getElementById('inJabatan').value, status: document.getElementById('inStatus').value, lokasi: document.getElementById('inLokasi').value, golongan: document.getElementById('inGolongan').value, kunci: document.getElementById('inKunci').value };
            if(!data.nama || !data.jabatan) return Swal.fire('Error', 'Nama dan Jabatan wajib', 'warning');
            Swal.fire({title:'Menyimpan Data...', didOpen:()=>Swal.showLoading()});
            if(data.idQR) { google.script.run.withSuccessHandler(res => { Swal.fire('Berhasil', res.message, 'success'); document.getElementById('modalTambahPegawai').classList.add('hidden'); loadDataPegawai(); }).editPegawai(data); }
            else { google.script.run.withSuccessHandler(res => { Swal.fire('Berhasil', res.message, 'success'); document.getElementById('modalTambahPegawai').classList.add('hidden'); loadDataPegawai(); }).tambahPegawai(data); }
        }

        function hapusPegawai(id) {
            Swal.fire({ title: 'Hapus Pegawai?', text: "Data tidak bisa dikembalikan!", icon: 'warning', showCancelButton: true, confirmButtonColor: '#d33', confirmButtonText: 'Ya'
            }).then((res) => { if (res.isConfirmed) { Swal.fire({title:'Menghapus...', didOpen:()=>Swal.showLoading()}); google.script.run.withSuccessHandler(r => { Swal.fire('Dihapus', r.message, 'success'); loadDataPegawai(); }).hapusPegawai(id); } });
        }

        // ==========================================
        // ID CARD GENERATOR (PREVIEW, DOWNLOAD, BATCH)
        // ==========================================
        function getUniversalCardHTML(id, nama, nip) {
            let puskName = document.getElementById('appHeaderName').innerText || "PUSKESMAS";
            return `
                <div class="universal-id-card">
                    <div class="id-header">
                        <h3>${puskName}</h3>
                        <p>Kartu E-Absensi Pegawai</p>
                    </div>
                    <div class="id-body">
                        <div id="qr-render-${id}" class="id-qr"></div>
                        <div class="nama-pegawai">${nama}</div>
                        <div class="nip-pegawai">${nip || 'NON-ASN'}</div>
                    </div>
                    <div class="id-footer">
                        <p>Wajib dibawa saat bertugas</p>
                    </div>
                </div>
            `;
        }

        function previewCard(id, nama, nip) {
            document.getElementById('previewCardContent').innerHTML = getUniversalCardHTML(id, nama, nip);
            new QRCode(document.getElementById(`qr-render-${id}`), { text: id, width: 120, height: 120, colorDark : "#003366", colorLight : "#ffffff", correctLevel : QRCode.CorrectLevel.H });
            document.getElementById('modalPreviewCard').classList.remove('hidden');
        }

        function downloadCardImage() {
            const cardEl = document.getElementById('previewCardContent').querySelector('.universal-id-card');
            Swal.fire({title: 'Memproses Gambar...', allowOutsideClick:false, didOpen:()=>{Swal.showLoading()}});

            // Bypass transform for crisp render
            cardEl.style.transform = "none";

            html2canvas(cardEl, { scale: 4, useCORS: true, backgroundColor: '#ffffff' }).then(canvas => {
                let link = document.createElement('a');
                let nameStr = cardEl.querySelector('.nama-pegawai').innerText.trim().replace(/\s+/g, '_');
                link.download = `IDCard_${nameStr}.png`;
                link.href = canvas.toDataURL('image/png', 1.0);
                link.click();
                Swal.close();
            }).catch(err => {
                Swal.fire('Error', 'Gagal memproses gambar.', 'error');
            });
        }

        function printSingleCard() {
            const printDiv = document.getElementById('printArea');
            printDiv.innerHTML = document.getElementById('previewCardContent').innerHTML;
            printDiv.classList.remove('hidden');
            setTimeout(()=>{ window.print(); printDiv.classList.add('hidden'); printDiv.innerHTML=''; }, 500);
        }

        function cetakSemuaID() {
            if(listPegawaiRaw.length === 0) return Swal.fire('Kosong','Tidak ada data pegawai.','info');
            Swal.fire({ title: 'Menyiapkan Batch Print...', allowOutsideClick: false, didOpen: () => { Swal.showLoading() } });

            const printDiv = document.getElementById('printArea'); printDiv.innerHTML = '';
            listPegawaiRaw.forEach(e => { printDiv.innerHTML += getUniversalCardHTML(e[0], e[1], e[2]); });
            printDiv.classList.remove('hidden');

            listPegawaiRaw.forEach(e => { new QRCode(document.getElementById(`qr-render-${e[0]}`),{ text: e[0], width: 120, height: 120, colorDark : "#003366", colorLight : "#ffffff", correctLevel : QRCode.CorrectLevel.H }); });
            setTimeout(() => { Swal.close(); window.print(); printDiv.classList.add('hidden'); printDiv.innerHTML = ''; }, 1500);
        }

        // ==========================================
        // PDF REPORT (REKAP & DETAIL JAM KERJA - RAPI)
        // ==========================================
    function cetakPDFHarian() {
    const tgl = document.getElementById('filterTglHarian').value;
    if(!tgl || dataAbsenHarianRaw.length === 0) return Swal.fire('Error', 'Pilih tanggal dan pastikan ada data.', 'warning');
    Swal.fire({title: 'Memproses PDF...', didOpen: () => Swal.showLoading()});

    const { jsPDF } = window.jspdf;
    const doc = new jsPDF({ orientation: "portrait" });
    const pageWidth = doc.internal.pageSize.width;

    // FORMAT TANGGAL BARU: Hari, DD MMMM YYYY
    const monthNames = ["Januari","Februari","Maret","April","Mei","Juni","Juli","Agustus","September","Oktober","November","Desember"];
    const dayNames = ["Minggu","Senin","Selasa","Rabu","Kamis","Jumat","Sabtu"];
    let parts = tgl.split('-');
    let d = new Date(parts[0], parseInt(parts[1])-1, parts[2]);
    let tglFormatted = `${dayNames[d.getDay()]}, ${parseInt(parts[2])} ${monthNames[parseInt(parts[1])-1]} ${parts[0]}`;

    doc.setFontSize(11);
    doc.setFont("helvetica", "bold");
    doc.text("LAPORAN ABSENSI HARIAN PEGAWAI", 14, 15);
    doc.text((window.kapusParams.puskesmas || "PUSKESMAS").toUpperCase(), 14, 21);

    // Tanggal dengan format yang diminta
    doc.setFont("helvetica", "normal");
    doc.text(`TANGGAL: ${tglFormatted}`, 14, 27);

    let tableData = [];
    dataAbsenHarianRaw.forEach((row, i) => {
        let statusText = row.keterangan;
        if (row.alasan && row.alasan !== "-" && row.alasan !== "") {
            statusText += ` (${row.alasan})`;
        }
        tableData.push([
            i+1,
            row.nama + "\n(" + row.jabatan + ")",
            row.wktMasuk || "-",
            row.wktPulang || "-",
            row.jamKerja || "-",
            statusText
        ]);
    });

    doc.autoTable({
        startY: 33,
        head: [['NO', 'NAMA / JABATAN', 'JAM MASUK', 'JAM PULANG', 'DURASI', 'STATUS / ALASAN']],
        body: tableData,
        theme: 'grid',
        headStyles: { fillColor: [255,255,255], textColor: [0,0,0], lineColor: [0,0,0], lineWidth: 0.1, fontStyle: 'bold', halign: 'center' },
        styles: { fontSize: 8, cellPadding: 3, valign: 'middle', textColor: [0,0,0], lineColor: [0,0,0], lineWidth: 0.1 },
        columnStyles: {
            0: { halign: 'center', cellWidth: 10 },
            1: { cellWidth: 55 },
            2: { halign: 'center', cellWidth: 22 },
            3: { halign: 'center', cellWidth: 22 },
            4: { halign: 'center', cellWidth: 25 },
            5: { halign: 'center', fontStyle: 'bold' }
        }
    });

    tambahTandaTangan(doc, window.kapusParams, tglFormatted, tglFormatted, pageWidth, true);
    doc.save(`Absen_Harian_${tgl}.pdf`);
    Swal.close();
}

    async function cetakPDFStatusHarian() {
    const tgl = document.getElementById('filterTglHarian').value;
    if(!tgl || dataAbsenHarianRaw.length === 0) return Swal.fire('Error', 'Pilih tanggal dan pastikan ada data.', 'warning');
    const pick = await Swal.fire({
        title: 'Pilih Status untuk PDF',
        html: `<div class="text-left text-sm space-y-2">
            <label><input type="checkbox" class="st-pdf" value="BELUM ABSEN" checked> Belum Absen</label><br>
            <label><input type="checkbox" class="st-pdf" value="SAKIT" checked> Sakit</label><br>
            <label><input type="checkbox" class="st-pdf" value="IZIN" checked> Izin</label><br>
            <label><input type="checkbox" class="st-pdf" value="TL" checked> TL</label><br>
            <label><input type="checkbox" class="st-pdf" value="TUGAS LUAR" checked> Tugas Luar</label><br>
            <label><input type="checkbox" class="st-pdf" value="ALPA" checked> ALPA</label>
        </div>`,
        showCancelButton: true,
        confirmButtonText: 'Generate PDF',
        preConfirm: () => Array.from(document.querySelectorAll('.st-pdf:checked')).map(e => e.value)
    });
    if (!pick.isConfirmed) return;
    const allowed = pick.value || [];
    if (allowed.length === 0) return Swal.fire('Perhatian', 'Pilih minimal 1 status.', 'warning');
    const filtered = dataAbsenHarianRaw.filter(r => allowed.includes(String(r.keterangan || '').toUpperCase()));
    if(filtered.length === 0) return Swal.fire('Info', 'Tidak ada data sesuai status yang dipilih.', 'info');
    Swal.fire({title: 'Memproses PDF Status...', didOpen: () => Swal.showLoading()});
    const { jsPDF } = window.jspdf; const doc = new jsPDF({ orientation: "portrait" });
    doc.setFontSize(11); doc.setFont("helvetica", "bold"); doc.text("LAPORAN STATUS KEHADIRAN HARIAN", 14, 15);
    doc.setFont("helvetica", "normal"); doc.text(`Tanggal: ${tgl}`, 14, 21);
    const rows = filtered.map((r,i)=>[i+1, r.nama, r.jabatan, r.keterangan, r.alasan || '-']);
    doc.autoTable({ startY: 26, head: [['No','Nama','Jabatan','Status','Catatan']], body: rows, styles:{fontSize:8}, headStyles:{fillColor:[245,158,11]} });
    doc.save(`Status_Harian_${tgl}_${allowed.join('-').replace(/\s+/g,'_')}.pdf`);
    Swal.close();
}

        function generatePDF() {
            const bln = document.getElementById('lapBulan').value; const thn = document.getElementById('lapTahun').value; const hariKerja = document.getElementById('lapHariKerja').value;
            if(!hariKerja) return Swal.fire('Error', 'Isi Target Hari Kerja.', 'warning');

            Swal.fire({title: 'Menyusun Laporan...', didOpen: () => Swal.showLoading()});
            google.script.run.withSuccessHandler(function(res) {
                if(!res.rekapData || res.rekapData.length === 0) return Swal.fire('Data Kosong', 'Tidak ada absensi.', 'info');

                const { jsPDF } = window.jspdf; const doc = new jsPDF({ orientation: "landscape" }); const pW = doc.internal.pageSize.width;
                doc.setFontSize(11); doc.setFont("helvetica", "bold");
                doc.text("DAFTAR REKAPITULASI ABSENSI TENAGA ASN/PPPK/BLUD PENUH WAKTU DI LINGKUNGAN", 14, 15);
                doc.text(res.namaPuskesmas.toUpperCase(), 14, 21);
                doc.text(`TAHUN ${thn}`, 14, 27); doc.text(`BULAN: ${bln} ${thn}`, 14, 35);

                let tableData = [];
                res.rekapData.forEach((r, i) => {
                    tableData.push([ i + 1, r.nama_nip, r.gol, r.jabatan + "\n" + r.status, r.hari_kerja || 0, r.hadir || 0, r.sakit || 0, r.izin || 0, r.cuti || 0, r.tl || 0, r.alpa || 0 ]);
                });

                doc.autoTable({
                    startY: 40,
                    head: [[{content: 'NO', rowSpan: 2, styles: {halign: 'center', valign: 'middle'}}, {content: 'NAMA / NIP', rowSpan: 2, styles: {halign: 'center', valign: 'middle'}}, {content: 'PANGKAT\n/ GOL', rowSpan: 2, styles: {halign: 'center', valign: 'middle'}}, {content: 'JABATAN', rowSpan: 2, styles: {halign: 'center', valign: 'middle'}}, {content: 'HARI\nKERJA', rowSpan: 2, styles: {halign: 'center', valign: 'middle'}}, {content: 'ABSENSI', colSpan: 6, styles: {halign: 'center'}}], ['HADIR', 'SAKIT', 'IZIN', 'CUTI', 'TL', 'ALPA']],
                    body: tableData, theme: 'grid',
                    headStyles: { fillColor: [255, 255, 255], textColor: [0, 0, 0], lineColor: [0, 0, 0], lineWidth: 0.1, fontStyle: 'bold' },
                    styles: { fontSize: 8, cellPadding: 2, valign: 'middle', textColor: [0, 0, 0], lineColor: [0, 0, 0], lineWidth: 0.1 },
                    columnStyles: { 0: { halign: 'center', cellWidth: 10 }, 2: { halign: 'center', cellWidth: 20 }, 4: { halign: 'center', fontStyle: 'bold' }, 5: { halign: 'center' }, 6: { halign: 'center' }, 7: { halign: 'center' }, 8: { halign: 'center' }, 9: { halign: 'center' }, 10: { halign: 'center', fontStyle: 'bold', textColor: [220, 38, 38] } }
                });

                tambahTandaTangan(doc, res, bln, thn, pW, false); doc.save(`Rekap_Kehadiran_${bln}_${thn}.pdf`); Swal.close();
            }).getRekapitulasi(bln, thn, hariKerja);
        }

        function generatePDFASN() {
            generatePDFKhususStatus('ASN', 'ASN', 'Rekap_ASN');
        }

        function generatePDFPPPK() {
            generatePDFKhususStatus('PPPK', 'PPPK', 'Rekap_PPPK');
        }

        function generatePDFKhususStatus(statusKey, label, filePrefix) {
            const bln = document.getElementById('lapBulan').value;
            const thn = document.getElementById('lapTahun').value;
            const hariKerja = document.getElementById('lapHariKerja').value;
            if(!hariKerja) return Swal.fire('Error', 'Isi Target Hari Kerja.', 'warning');
            Swal.fire({title: `Menyusun Laporan ${label}...`, didOpen: () => Swal.showLoading()});
            google.script.run.withSuccessHandler(function(res) {
                const key = String(statusKey || '').toUpperCase();
                const rows = (res.rekapData || []).filter(r => {
                    const st = String(r.status || '').toUpperCase().trim();
                    return key === 'PPPK' ? st === 'PPPK' : st.includes(key);
                });
                if(rows.length === 0) return Swal.fire('Data Kosong', `Tidak ada data ${label} pada periode ini.`, 'info');

                const { jsPDF } = window.jspdf;
                const doc = new jsPDF({ orientation: "landscape", unit: "mm", format: "a4" });
                const pW = doc.internal.pageSize.width;
                const pH = doc.internal.pageSize.height;
                const marginX = 10;
                const bulanText = document.getElementById('lapBulan').options[document.getElementById('lapBulan').selectedIndex].text.toUpperCase();

                doc.setFont("helvetica", "bold");
                doc.setFontSize(9.5);
                doc.text(`REKAPITULASI BULANAN CAPAIAN PRODUKTIVITAS KERJA PEGAWAI (${label})`, marginX, 12);
                doc.setFontSize(8.5);
                doc.text((res.namaPuskesmas || 'PUSKESMAS').toUpperCase(), marginX, 17);
                doc.setFont("helvetica", "normal");
                doc.text(`BULAN ${bulanText} ${thn}  •  Target Hari Kerja: ${hariKerja}`, marginX, 22);
                doc.setDrawColor(0, 51, 102); doc.setLineWidth(.4); doc.line(marginX, 25, pW - marginX, 25);

                const tableData = rows.map((r, i) => [
                    i + 1,
                    r.nama_nip,
                    r.gol || '-',
                    `${r.jabatan || '-'}
${r.status || '-'}`,
                    r.hari_kerja || 0,
                    r.hadir || 0,
                    r.sakit || 0,
                    r.izin || 0,
                    r.cuti || 0,
                    r.alpa || 0
                ]);

                doc.autoTable({
                    startY: 29,
                    margin: { left: marginX, right: marginX, bottom: 33 },
                    tableWidth: pW - (marginX * 2),
                    head: [[
                        {content: 'NO', rowSpan: 2, styles: {halign: 'center', valign: 'middle'}},
                        {content: 'NAMA / NIP', rowSpan: 2, styles: {halign: 'center', valign: 'middle'}},
                        {content: 'PANGKAT\n/ GOL', rowSpan: 2, styles: {halign: 'center', valign: 'middle'}},
                        {content: 'JABATAN / STATUS', rowSpan: 2, styles: {halign: 'center', valign: 'middle'}},
                        {content: 'HARI\nKERJA', rowSpan: 2, styles: {halign: 'center', valign: 'middle'}},
                        {content: 'ABSENSI', colSpan: 5, styles: {halign: 'center'}}
                    ], ['HADIR', 'SAKIT', 'IZIN', 'CUTI', 'ALPA']],
                    body: tableData,
                    theme: 'grid',
                    styles: { fontSize: 6.8, cellPadding: 1.15, valign: 'middle', overflow: 'linebreak', lineColor: [0, 0, 0], lineWidth: 0.1, textColor: [0, 0, 0], minCellHeight: 5.2 },
                    headStyles: { fillColor: [255, 255, 255], textColor: [0, 0, 0], lineColor: [0, 0, 0], lineWidth: 0.15, fontStyle: 'bold', fontSize: 6.8 },
                    alternateRowStyles: { fillColor: [248, 250, 252] },
                    columnStyles: {
                        0: {halign:'center', cellWidth: 9},
                        1: {cellWidth: 72},
                        2: {halign:'center', cellWidth: 25},
                        3: {cellWidth: 78},
                        4: {halign:'center', cellWidth: 17, fontStyle:'bold'},
                        5: {halign:'center', cellWidth: 15},
                        6: {halign:'center', cellWidth: 15},
                        7: {halign:'center', cellWidth: 15},
                        8: {halign:'center', cellWidth: 15},
                        9: {halign:'center', cellWidth: 15, fontStyle:'bold', textColor:[185,28,28]}
                    },
                    didDrawPage: function() {
                        doc.setFontSize(7);
                        doc.setTextColor(100);
                        doc.text(`Dicetak otomatis oleh sistem absensi digital • ${label}`, marginX, pH - 8);
                        doc.text(`Hal. ${doc.internal.getNumberOfPages()}`, pW - marginX, pH - 8, { align: 'right' });
                        doc.setTextColor(0);
                    }
                });

                tambahTandaTanganRingkas(doc, res, bln, thn, pW, pH);
                doc.save(`${filePrefix}_${bln}_${thn}.pdf`);
                Swal.close();
            }).getRekapitulasi(bln, thn, hariKerja);
        }

        function tambahTandaTanganRingkas(doc, res, bln, thn, pW, pH) {
            const finalY = doc.lastAutoTable.finalY || 35;
            const y = Math.min(finalY + 9, pH - 38);
            const rightX = pW - 48;
            const bulanText = document.getElementById('lapBulan').options[document.getElementById('lapBulan').selectedIndex].text;
            doc.setFontSize(7.5); doc.setFont("helvetica", "normal");
            doc.text(`Sintang, ${new Date().getDate()} ${bulanText} ${thn}`, rightX, y, { align: 'center' });
            doc.setFont("helvetica", "bold");
            let puskNameStr = res.namaPuskesmas || res.puskesmas || "PUSKESMAS";
            doc.text(`KEPALA ${puskNameStr.toUpperCase().replace("UPTD ", "")}`, rightX, y + 5, { align: 'center' });
            doc.text(res.kapusNama || res.nama || "...................", rightX, y + 24, { align: 'center' });
            doc.setLineWidth(0.2); doc.line(rightX - 28, y + 25, rightX + 28, y + 25);
            doc.setFont("helvetica", "normal"); doc.text(res.kapusNip || res.nip || "NIP. .................", rightX, y + 29, { align: 'center' });
        }

        function generatePDFDetail() {
            const bln = document.getElementById('lapBulan').value; const thn = document.getElementById('lapTahun').value; const hariKerja = document.getElementById('lapHariKerja').value;
            if(!hariKerja) return Swal.fire('Error', 'Isi Target Hari Kerja.', 'warning');

            Swal.fire({title: 'Menyusun Laporan...', didOpen: () => Swal.showLoading()});
            google.script.run.withSuccessHandler(function(res) {
                if(!res.rekapData || res.rekapData.length === 0) return Swal.fire('Data Kosong', 'Tidak ada absensi.', 'info');

                const { jsPDF } = window.jspdf; const doc = new jsPDF({ orientation: "portrait" }); const pW = doc.internal.pageSize.width;
                doc.setFontSize(11); doc.setFont("helvetica", "bold");
                doc.text("LAPORAN RINCIAN TOTAL JAM KERJA PEGAWAI", 14, 15);
                doc.text(res.namaPuskesmas.toUpperCase(), 14, 21);
                doc.text(`BULAN: ${bln} ${thn}`, 14, 27);

                let tableData = [];
                res.rekapData.forEach((r, i) => { tableData.push([ i + 1, r.nama_nip, r.jabatan + "\n" + r.status, r.hadir || 0, r.alpa || 0, r.total_jam ]); });

                doc.autoTable({
                    startY: 32, head: [['NO', 'NAMA / IDENTITAS', 'JABATAN & STATUS', 'HADIR', 'ALPA', 'TOTAL JAM KERJA']],
                    body: tableData, theme: 'grid',
                    headStyles: { fillColor: [255, 255, 255], textColor: [0, 0, 0], lineColor: [0, 0, 0], lineWidth: 0.1, fontStyle: 'bold', halign: 'center' },
                    styles: { fontSize: 8, cellPadding: 3, valign: 'middle', textColor: [0, 0, 0], lineColor: [0, 0, 0], lineWidth: 0.1 },
                    columnStyles: { 0: { halign: 'center', cellWidth: 10 }, 3: { halign: 'center' }, 4: { halign: 'center', textColor: [220, 38, 38] }, 5: { halign: 'center', fontStyle: 'bold' } }
                });

                tambahTandaTangan(doc, res, bln, thn, pW, false); doc.save(`Detail_Jam_Kerja_${bln}_${thn}.pdf`); Swal.close();
            }).getRekapitulasi(bln, thn, hariKerja);
        }

        function tambahTandaTangan(doc, res, bln, thn, pW, isHarian) {
            const finalY = doc.lastAutoTable.finalY || 40; let rightX = pW - 50;
            doc.setFontSize(10); doc.setFont("helvetica", "normal");
            const tglTulis = isHarian ? bln.split('-').reverse().join('-') : new Date().getDate() + ' ' + document.getElementById('lapBulan').options[document.getElementById('lapBulan').selectedIndex].text + ' ' + thn;

            doc.text(`Sintang, ${tglTulis}`, rightX, finalY + 15, { align: 'center' });
            doc.setFont("helvetica", "bold");
            let puskNameStr = res.namaPuskesmas || res.puskesmas || "PUSKESMAS";
            let strPrefix = puskNameStr.toUpperCase().replace("UPTD ", "");
            doc.text(`KEPALA ${strPrefix}`, rightX, finalY + 20, { align: 'center' });
            doc.text(res.kapusNama || res.nama || "...................", rightX, finalY + 45, { align: 'center' });
            doc.setLineWidth(0.3); doc.line(rightX - 35, finalY + 46, rightX + 35, finalY + 46);
            doc.setFont("helvetica", "normal"); doc.text(res.kapusNip || res.nip || "NIP. .................", rightX, finalY + 50, { align: 'center' });
        }

    </script>
</body>
</html>
